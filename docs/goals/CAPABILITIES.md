# Capabilities — the loop's only success metric

Append-only inventory of **real, user-exercisable** Muse
capabilities. One line per capability: what the user can do, the
exact command/surface, and the executable check that proves it.

Rules (enforced by `.claude/rules/iteration-loop.md`):

- Every shipped outward goal MUST append exactly one new line here.
  The `<check>` MUST be a concrete automated test or smoke id that
  actually runs green under `pnpm check` / `pnpm smoke:broad` /
  `pnpm smoke:live` and asserts the *capability* end-to-end (not
  that code compiles). Prose with no runnable id is not a check.
- **Cross-time falsification:** every iteration's first action is
  to run the newest line's check and prove it still passes. A
  faked/broken line = the next iteration's whole job is to fix it.
- **Regression sweep:** every 10th iteration re-runs ALL checks;
  any regression = next iteration must restore it.
- A request/response-path capability whose `smoke:live` did NOT
  actually run is tagged `[UNVERIFIED-LIVE]`; it does not count
  until a later iteration runs the live check and drops the tag.
- Append-only. Never delete or weaken a line.
- **The success metric is NOT this line count.** It is
  `OUTWARD-TARGETS.md` bullets flipped `[ ]`→`[x]`. A line here is
  the *evidence* for a flip (cite the bullet it delivers); a line
  that adds no bullet flip is thin and does not satisfy the metric.
  No bullet flipped in the last 5 iterations ⇒ next iteration's
  sole mandate is to flip one end-to-end. Flat bullets =
  degeneration; act on it — never stop, never ask a human.

Format: `- [<axis>] <capability> — <command/surface> — <runnable check id> — P<n> bullet`
axis ∈ Reach | Anticipation | Autonomy | Presence

## Inventory

P0–P21's full verified ledger is preserved in
`archive/CAPABILITIES-through-2026-05-27.md` (human reset
2026-05-27 to keep the loop-facing file lean — history intact, not
deleted). The lines below are the delivered foundation that the
active P22 target extends; new P22 lines append here.

- [Anticipation] Proactive notice loop (upcoming events / reminders) — `apps/api` proactive daemon tick — `apps/api` proactive-tick tests
- [Anticipation] Self-queued follow-up promises fire later — followups daemon tick — `runDueFollowups` tests
- [Autonomy] Standing-objective evaluation fires its actuator — objectives daemon tick — `runDueObjectives` tests
- [Anticipation] Ambient OS signal drives a proactive notice — `runAmbientNoticeTick` + `MacOsActiveWindowSource` — ambient-notice-loop tests
- [Anticipation] Page-change web-watch fires an edge-triggered notice — `createWebWatchRunner` (HTTP + Chrome source) — web-watch-runner / web-watch-chrome tests
- [Anticipation] Proactive notice delivered to a real channel — `ProactiveNoticeSink` over the messaging registry (Telegram) — `sendWithRetry` + proactive-notice-loop tests
- [Anticipation] One-process daemon launcher fires a real proactive tick — `muse daemon --once` — `apps/cli/src/commands-daemon.test.ts` (imminent task delivered to a contract-faithful messaging sink; quiet tick sends nothing; unknown provider fails closed) — P22-1a bullet
- [Presence] Episode forgetting is now importance-aware — a pivotal old session resists being vacuumed away while a trivial recent one fades (FadeMem, arXiv 2601.18642: biologically-inspired forgetting, decay modulated by importance) — `vacuumEpisodes` dropped purely-oldest-by-`endedAt` at the cap; now `computeEpisodeRetention` scores each episode `exp(-ageDays / (halfLife·(1 + w·importance/10)))` so importance EXTENDS the half-life (importance-10 ⇒ ~3× slower fade), and `selectRetainedEpisodes` keeps the highest-retention `cap` (newest-then-id tie-break). Back-compatible: an UNSCORED corpus reduces to recency ordering, so chronological vacuum is byte-identical until importance is present — `@muse/mcp` episode-retention.test.ts (age decay; importance slows fade; unscored→recency; unparseable→0; importance tips comparable ages: a 35-day importance-10 session out-retains a 25-day importance-1 one at the cap; deterministic ties) + existing vacuum suite stays green (unscored episodes still pruned oldest-first) — research-applied slice (no new dep, deterministic/local; builds on the importance arc)
- [Presence] Auto-extracted memory now resolves each fact with an explicit operation instead of blind-overwriting (Mem0, arXiv 2504.19413: per-candidate ADD/UPDATE/DELETE/NOOP) — `classifyMemoryOperation(existing, incoming)` is deterministic over the extractor's output (no extra model call): NOOP when a value re-confirms what's stored → SKIPS the redundant write + provenance entry (a re-mention no longer logs a fresh "learned" event); DELETE when the value is a no-value/retraction token (none/n/a/unknown/없음/모름/…) → the key is FORGOTTEN via the store's `forget` instead of storing junk like "unknown"; ADD/UPDATE upsert + record provenance as before; the auto-extract persist reads existing memory once and routes each fact/preference through it — `@muse/memory` memory-operation.test.ts (classifier: add/noop/update/delete EN+KO tokens; auto-extract integration: re-confirm → no new provenance, changed → upsert+provenance, retraction → key forgotten) + full memory suite 213 green (new facts still ADD as before, back-compatible) — research-applied slice (arXiv id cited in code; deterministic, no new dep, no extra LLM call)
- [Reach] A natural-language tool-selection path is available + measured against the native one (Natural Language Tools, arXiv 2510.14453: stating the tool choice in prose + parsing it deterministically beats forcing JSON on small/open-weight models) — `parseNaturalLanguageToolSelection(text, toolNames)` deterministically maps a model's prose answer to one known tool (earliest-named wins; whole-token match so `time_now` ≠ `my_time_now_helper`; explicit no-tool/none/없음 → none) and `pnpm eval:tools:nl` compares NATIVE (Hermes JSON tool-call) vs NL one-shot accuracy on the confusable time set — `@muse/tools` nl-tool-selection.test.ts (single pick, "use A not B"→A, no-tool EN+KO, embedded-name not matched, empty) + LIVE qwen3:8b: native 7/7 (100%) AND NL 7/7 (100%) on the confusable time set — verified finding: Muse's tuned tool names are ALREADY at the selection ceiling, so the NL path is kept as a reusable parser + comparison gate rather than wired (evidence-based: add NL complexity only when a confusable set's native accuracy drops below ceiling) — research-applied slice (arXiv id cited in code; no new dep, core tool path untouched)
- [Presence] Muse applies LEARNED STRATEGIES from past feedback, not just avoidances — a self-improving playbook (ACE — Agentic Context Engineering, arXiv 2510.04618: a frozen model improves by accumulating small strategy deltas in an evolving context, no fine-tuning) — the POSITIVE counterpart to veto-avoidance: `applyPlaybook` injects a `[Learned Strategies]` system block (mirrors the veto-avoidance seam; conservative — zero strategies/no userId ⇒ exact no-op; fail-open) wired into the live agent-runtime context pipeline behind a duck-typed `PlaybookProvider`; the durable `~/.muse/playbook.json` store (`@muse/mcp`, atomic/tolerant/capped) is adapted by `buildPlaybookProvider` (MUSE_PLAYBOOK default-on) and populated via `muse playbook add|list|remove` — `@muse/agent-core` playbook.test.ts (conservative/fail-open/inject + injection-collapse + LIVE-runtime wiring: a recorded strategy reaches a real createAgentRuntime run's system prompt, none → no-op) + LIVE qwen3:8b EFFECT MEASUREMENT: with the strategy "answer in at most 6 words" injected, a question that drew a long multi-paragraph reply WITHOUT it returned a 7-word "Paris. Famous for art, fashion, and culture." WITH it — the learned strategy measurably changed the model's output — research-applied slice (arXiv id cited in code; no new dep; mirrors the proven veto-avoidance pattern end-to-end)
- [Anticipation] The daemon launcher also fires due follow-ups in the same process — `muse daemon --once` (proactive + followup ticks) — `apps/cli/src/commands-daemon.test.ts` (a DUE followup is synthesized + delivered to a contract-faithful sink alongside the proactive tick; proactive-only cases stay hermetic) — P22-1b bullet
- [Anticipation] The daemon launcher also fires ambient (perception) rule matches in the same process — `muse daemon --once` (proactive + followup + ambient ticks) — `apps/cli/src/commands-daemon.test.ts` (a matching ambient rule delivers a notice to a contract-faithful sink; ambient skipped cleanly when no MUSE_AMBIENT_RULES) — P22-1c bullet
- [Anticipation] The daemon launcher also runs read-only web-watch polling in the same process — `muse daemon --once` (proactive + followup + ambient + web-watch ticks) — `apps/cli/src/commands-daemon.test.ts` (an "appears" trigger over an injected fetch delivers a notice to a contract-faithful sink; web-watch skipped cleanly when no MUSE_WEB_WATCH_CONFIG) — P22-1d bullet
- [Autonomy] The daemon launcher also re-evaluates standing objectives + notifies on met in the same process — `muse daemon --once` (now all five ticks: proactive + followup + ambient + web-watch + objectives) — `apps/cli/src/commands-daemon.test.ts` (a MET objective notifies the user via a contract-faithful sink; objectives skipped cleanly when no model resolves) — P22-1e bullet
- [Anticipation] The daemon foreground loop shuts down cleanly on a stop signal — `muse daemon` (ctrl-c) — `apps/cli/src/commands-daemon.test.ts` runDaemonLoop suite (stops on signal + returns tick count, a throwing tick doesn't stop the loop, the interruptible sleep resolves immediately on stop instead of waiting out the interval) — P22-1f bullet
- [Anticipation] The daemon's ambient tick reads the REAL macOS active window (osascript) when MUSE_AMBIENT_SOURCE=macos — `muse daemon` — `apps/cli/src/commands-daemon.test.ts` (an injected osascript "Slack\ngeneral" signal drives a matching ambient rule to a contract-faithful sink; file source otherwise) — P22-2 bullet
- [Anticipation] The daemon drives source:chrome web-watches through an injected Chrome DevTools MCP connection — `muse daemon` — `apps/cli/src/commands-daemon.test.ts` (a contract-faithful Chrome connection navigate_page+take_snapshot edge-fires a chrome-source watch; with no connection the chrome watch is skipped fail-soft and the daemon stays up) — P22-3a bullet
- [Anticipation] muse daemon --status reports which of the five ticks are enabled for the current config — `muse daemon --status` — `apps/cli/src/commands-daemon.test.ts` (bare config → only proactive enabled; ambient rules + web-watch config + resolved model → followup/ambient/web-watch/objectives all report enabled; no send) — P22-4a bullet
- [Anticipation] muse daemon --init persists provider+destination to a config file the launcher loads (no env/flags needed) — `muse daemon --init` — `apps/cli/src/commands-daemon.test.ts` (init writes the resolved provider/destination; a later run with NO flag/env reads them from the file and delivers an imminent task to the configured destination) — P22-4b bullet
- [Anticipation] Full daemon startup→delivery e2e + deny/timeout produces no send — `muse daemon --once` — `apps/cli/src/commands-daemon.test.ts` (all FIVE ticks deliver in one run = 5 sends to a contract-faithful sink; a separate smoke: a throwing/timed-out provider yields zero delivery, daemon stays up, nothing marked fired) — P22-5 bullet
- [Anticipation] muse daemon --install writes a macOS LaunchAgent plist so the daemon survives logout/reboot — `muse daemon --install` — `apps/cli/src/commands-daemon.test.ts` (buildLaunchAgentPlist passes plutil -lint with RunAtLoad/KeepAlive/`daemon`; --install writes a valid plist at MUSE_DAEMON_PLIST_FILE and prints the launchctl load line) — P22-6 bullet
- [Anticipation] The daemon builds a real Chrome connection from the MCP stack at startup (best-effort, fail-soft) for chrome-source web-watches — `muse daemon` (MUSE_CHROME_DEVTOOLS_ENABLED) — `apps/cli/src/commands-daemon.test.ts` (chromeSnapshotConnectionFromTools adapts toMuseTools→callTool, forwards/throws correctly, and drives a daemon chrome-source watch edge-fire e2e; disabled/connect-refused → undefined → watch skips, daemon up; real browser handshake is manual) — P22-3b bullet
- [Knowledge] knowledge_search recalls exact rare tokens via hybrid RRF retrieval, not just cosine (Reciprocal Rank Fusion, Cormack/Clarke/Büttcher SIGIR 2009: fuse rankings by Σ 1/(k+rank)) — `knowledge_search` tool / `rankKnowledgeChunks({hybrid:true})` — `packages/agent-core/test/knowledge-recall-agent.test.ts` (a chunk with zero cosine but an exact keyword is DROPPED by pure cosine, RECALLED by hybrid; existing cosine path + P20 citing flow stay green) — research-applied slice (arXiv-era open paper cited in code; no new dep, deterministic/local) — P23-1 bullet
- [Knowledge] The corpus-search surfaces (briefing enricher + the notes knowledge_search corpus tool) also recall exact rare tokens via hybrid RRF — `createNotesKnowledgeSearchTool` / `createKnowledgeEnricher` — `packages/autoconfigure/test/knowledge-recall-sources.test.ts` (a zero-cosine exact-keyword chunk is recalled through the corpus-search tool; stopword-filtered lexical so a decoy sharing only "my"/"is" is not falsely recalled) — P23-2 bullet
- [Knowledge] knowledge_search top-K is diversified with MMR so near-duplicate passages don't crowd out a distinct relevant one (Maximal Marginal Relevance, Carbonell & Goldstein SIGIR 1998: λ·relevance − (1−λ)·max-sim-to-picked) — `knowledge_search` / `rankKnowledgeChunks({diversify:true})` — `packages/agent-core/test/knowledge-recall-agent.test.ts` (plain top-2 returns two near-duplicates; MMR returns one duplicate + the distinct relevant passage) — research-applied slice (open paper cited in code; deterministic, no dep, local) — P24-1 bullet
- [Knowledge] MMR default lambda tuned 0.7→0.5 from live nomic-embed evidence (0.7 never drops a real paraphrase; 0.5 does in clear cases) — `rankKnowledgeChunks({diversify:true})` default — `packages/agent-core/test/knowledge-recall-agent.test.ts` (default 0.5, no explicit lambda, drops an exact duplicate for the distinct passage). Honest scope: real-paraphrase dedup is best-effort (embedding jitter on the thin margin), deterministic only on exact duplicates — P24-2 bullet
- [Perception] The daemon's ambient notice carries a "Related:" line from the user's own knowledge about what they're looking at — `muse daemon` ambient tick (knowledgeEnrich seam → createAmbientNoticeRunner enrich) — `apps/cli/src/commands-daemon.test.ts` (a fired ambient rule's notice is enriched with the injected enricher's line keyed on the active window; absent → plain notice) — P25-1 bullet
- [Perception] The daemon's ambient "Related:" enricher is the user's REAL notes — `muse daemon` (MUSE_BRIEFING_RELATED_KNOWLEDGE_ENABLED → createKnowledgeEnricher + Ollama embed + LocalDirNotesProvider, hybrid+MMR; fail-soft) — live: enrich("Q3 budget memo") over a temp notes dir returned the real notes/q3-budget.md line, not the parking decoy; the seam + default-off path are in `apps/cli/src/commands-daemon.test.ts` — P25-2 bullet
- [Reach] The daemon also watches Home Assistant entity states (read-only) in the same process — `muse daemon` home-watch tick (MUSE_HOME_WATCH_CONFIG + HA creds; homeWatchesFromConfig → createWebWatchRunner) — `apps/cli/src/commands-daemon.test.ts` (a HA /api/states snapshot of "unlocked" edge-fires a notice to a contract-faithful sink; skipped without config/creds; never acts on the home per outbound-safety; reported by --status) — P26-1 bullet
- [Anticipation] The daemon also fires due reminders in the same process — `muse daemon` reminders tick (runDueReminders, always-on like proactive) — `apps/cli/src/commands-daemon.test.ts` (a due pending reminder is delivered to a contract-faithful sink, a future one is not; --status reports it). The resident daemon now covers all 7 proactive/perception ticks — P26-2 bullet
- [Anticipation] The resident daemon also delivers the situational briefing (objective status + imminent tasks + a related note) — `muse daemon` (MUSE_BRIEFING_ENABLED; runDueSituationalBriefing + deriveBriefingImminent + the shared knowledge enricher, self-deduped by sidecar) — `apps/cli/src/commands-daemon.test.ts` (an imminent task ⇒ a brief delivered; skipped without the flag; reported by --status). No model needed — composes deterministically — P27-1 bullet
- [Anticipation] The daemon briefing names an upcoming birthday from the user's contacts — `muse daemon` (MUSE_BRIEFING_ENABLED; birthdayLine = queryContacts → resolveUpcomingBirthdays → formatBirthdayBriefLine) — `apps/cli/src/commands-daemon.test.ts` (a contact whose birthday is today appears in the delivered brief) — P27-2 bullet
- [Anticipation] The daemon briefing surfaces imminent calendar events too — `muse daemon` (MUSE_BRIEFING_ENABLED; deriveCalendarBriefingImminent over the calendar registry's listEvents merged into the brief's imminent) — `apps/cli/src/commands-daemon.test.ts` (a calendar event 5 min out appears in the delivered brief). P27 complete: brief = objectives + imminent tasks/calendar + birthdays + related note — P27-3 bullet
- [Knowledge] knowledge_search reorders the top-K so the most relevant passages sit at the context edges, not the middle (Lost in the Middle, Liu et al. 2023, arXiv 2307.03172: models attend best to start+end) — `knowledge_search` / `edgeLoadByRelevance` — `packages/agent-core/test/knowledge-recall-agent.test.ts` (best-first [a,b,c,d,e] → [a,c,e,d,b]; the top match stays first so citation is unaffected) — research-applied slice (open paper cited in code; deterministic, no dep, local) — P28-1 bullet
- [Presence] muse daemon --print echoes every delivered notice (from any tick) to stdout so the user watches the resident daemon work in the foreground, while still delivering to the channel — `muse daemon --print` — `apps/cli/src/commands-daemon.test.ts` (with --print the notice text appears in stdout; without it only the tick summary; channel delivery unaffected) — P29-1 bullet
- [Presence] muse daemon --status shows the resolved source paths (config/tasks/reminders/followups/objectives) so a non-firing tick is debuggable without reading source — `muse daemon --status` — `apps/cli/src/commands-daemon.test.ts` (status output contains the resolved tasks/reminders/objectives file paths) — P30-1 bullet
- [Presence] muse daemon --status reports launchd autostart state (installed vs not, with the plist path or an --install hint) so the user knows if the daemon survives reboot — `muse daemon --status` — `apps/cli/src/commands-daemon.test.ts` (no plist → "not installed"; plist present → "installed") — P30-2 bullet
- [Reach] Muse proposes a state-changing action draft-first and never sends until the user confirms — `muse propose list | approve <id> | decline <id>` — `packages/mcp/test/proposed-action.test.ts` + `apps/cli/src/commands-propose.test.ts` (propose persists pending + sends nothing; approve executes once + logs performed + is replay-guarded; decline → no send + refused; a send failure stays pending). Closes the perceive→propose→confirm→act loop per outbound-safety — P31-1 bullet
- [Reach] A standing objective can PROPOSE its message draft-first instead of auto-sending — `muse daemon` (MUSE_OBJECTIVES_PROPOSE) via createProposingObjectiveActuator — `apps/cli/src/commands-daemon.test.ts` (a met objective in propose-mode creates a pending proposed action and sends nothing; the user confirms it with `muse propose approve`). Completes the perceive→propose→confirm→act loop — P31-2 bullet
- [Reach] A proposed action expires (default 24h) and can no longer be confirmed — outbound-safety's "approval timeout → no send" applied to the propose flow — `muse propose` / isProposalActionable — `packages/mcp/test/proposed-action.test.ts` (an expired proposal is not actionable; confirm refuses "expired" without sending; a fresh proposal is actionable within its TTL) — P31-3 bullet
- [Presence] The learned-strategy playbook injects only the strategies RELEVANT to the current turn, not the whole bank (ReasoningBank, arXiv 2509.25140: a self-evolving agent retrieves relevant reasoning memory rather than dumping it) — `rankPlaybookStrategies` (CJK-aware token-overlap + tag boost, top-K with a recency floor so a non-empty bank never injects zero) wired into BOTH `applyPlaybook` (runtime `--with-tools`) and the chat-only `muse ask` path via `selectPlaybookSection` (env `MUSE_PLAYBOOK_INJECT_TOPK`, default 6); when the bank is ≤ topK the SET is unchanged (today's inject-all), only ordered most-relevant-first — `packages/agent-core/test/playbook.test.ts` (relevance ranks above irrelevant; ≤K returns all best-first; >K drops least-relevant; recency floor on zero-overlap; Korean CJK match; applyPlaybook orders by the latest user message) + `apps/cli/src/commands-ask-playbook.test.ts` (an email prompt injects the email strategy and drops a scheduling-only one at topK=1) — research-applied slice (arXiv id cited in code; deterministic, no new dep, no extra LLM call; foundation for slice-2 correction-driven auto-distillation)
- [Presence] Muse auto-LEARNS a strategy from a correction the user made — at session end it distills a generalised `[Learned Strategies]` entry from a corrected exchange into the SAME `~/.muse/playbook.json` (ReasoningBank, arXiv 2509.25140: a self-evolving agent distills reusable strategies from its own experience; the outcome signal is the RELIABLE explicit correction, NOT small-model self-judgement per arXiv 2404.17140) — auto at REPL exit (`MUSE_PLAYBOOK_DISTILL_ENABLED`, default off) + manual `muse playbook distill`; pipeline = `detectCorrections` (EN+KO, precision-first rule pass) → `distillStrategyFromCorrection` (one LLM call, secret-redacted, fail-soft) → `strategyTextSimilarity` dedup (≥0.6, makes re-runs idempotent) → `recordPlaybookStrategy` — `packages/agent-core/test/correction-distiller.test.ts` (detector EN+KO + false-positive guard + no-prior-assistant + cap; distiller parse/omit-tag/fail-soft) + `playbook.test.ts` (similarity) + `apps/cli/src/chat-distill-corrections.test.ts` (records from a corrected session, skips no-correction, dedups, skips no-userId) + `commands-playbook.test.ts` (distill subcommand); LIVE qwen3:8b: the Korean correction "그게 아니라 불릿 포인트로 정리해줘" distilled "when asked to summarise, use bullet points not prose" into the playbook end-to-end via `muse playbook distill` — research-applied slice (arXiv ids cited in code; deterministic detection+dedup, single offline LLM call, no new dep; completes ReasoningBank retrieval→distillation)
- [Knowledge] knowledge_search grades retrieval confidence and FRAMES weak grounding as low-confidence instead of telling the small local model to cite it (CRAG, arXiv 2401.15884: a lightweight retrieval evaluator scores evidence quality) — `classifyRetrievalConfidence` over the TOP match's ABSOLUTE cosine (surfaced as `KnowledgeMatch.cosine`, separate from the hybrid RRF `score`): confident→"cite the [source]", ambiguous→"Possibly-related (LOW confidence — verify, don't cite as fact)", none→no passages; `renderKnowledgeMatches` (the agent `knowledge_search` + notes corpus-search tools) and the ambient `createKnowledgeEnricher` (Related line only when confident) gate on it — `packages/agent-core/test/knowledge-recall-agent.test.ts` (verdict none/ambiguous/confident; cosine surfaced in both paths; render header by verdict) + `knowledge-render-litm.test.ts` + `packages/autoconfigure/test/knowledge-enricher*.test.ts` (weak match → no Related line) — research-applied slice. HONEST scope: live-calibrated on nomic-embed (relevant ~0.61 vs personal distractors ~0.44–0.51 → `confidentAt` 0.55), but nomic's cosine space is compressed (unrelated encyclopedic text can score ~0.54), so this is a BEST-EFFORT low-confidence flag for personal corpora, not a hard relevant/irrelevant separator (same honesty class as the P24-2 MMR nudge). No web fallback (deferred); deterministic, no new dep, no extra round
- [Autonomy] The plan-execute planner reuses a similar PAST plan as a few-shot exemplar so the small local model plans better in one shot (Agentic Plan Caching, arXiv 2506.14852: extract plan templates from completed runs and reuse them on similar tasks) — completed plans auto-captured to `~/.muse/plan-cache.json`, retrieved by Jaccard token overlap (`selectPlanExemplar`, reuses `strategyTextSimilarity`), rendered as a `[Similar Past Plan]` block injected into the planning prompt before `[User Request]`; threaded `PlanCacheProvider` → `PlanExecuteRunner` → `AgentRuntime`, built by `buildPlanCacheProvider` (default-on, opt out `MUSE_PLAN_CACHE=false`), conservative (no userId / no match ⇒ no exemplar + no record), fail-open — `packages/agent-core/test/plan-cache.test.ts` (retrieval picks most-similar / none below threshold; render shape) + `agent-runtime-plan-cache.test.ts` (a plan-execute run records its plan; a cached similar plan is injected into the planning request; no userId ⇒ neither) + `packages/mcp/test/personal-plan-cache-store.test.ts` (record/query/filter/upsert/cap/tolerant) + `packages/prompts/test/prompts.test.ts` (exemplar before [User Request]) — research-applied slice. HONEST scope: this is APC's faithful store/match adapted for a LOCAL free model as planning GUIDANCE (improves one-shot plan reliability), NOT APC's skip-the-planner efficiency play (cloud cost benefit is moot locally; exact repeats already covered by responseCache). Deterministic, no new dep, no extra model round
- [Autonomy] Muse AUTHORS a reusable SKILL.md from a procedural user correction at session end (fork-and-review, after Hermes Agent's background-review loop, MIT — see THIRD_PARTY_NOTICES.md) — deterministic `detectSkillCandidates` (reuses `detectCorrections`, EN+KO) → one local-Qwen `draftSkillFromSignal` (returns `NONE` for mere preferences so those stay playbook-only, secret-redacted, fail-soft) → `AuthoredSkillStore.writeOrPatch` (EXECUTE-GATED by type: a SkillDraft has no `requires` field so an authored skill can never declare runnable bins → `muse.skills.run` refuses it until a human promotes it; dedup→patch/skip, cap 30 with archive-never-delete, atomic fsync+rename 0600) into `~/.muse/skills/authored/`, loaded as the LOWEST-precedence registry root (a same-named user/workspace skill always wins) so it is picked up next session and surfaced by `selectRelevantSkills` on a similar request; auto at REPL exit (`MUSE_SKILL_AUTHOR_ENABLED`, default off) + manual `muse skills author` — `packages/skills/test/authored-skill-store.test.ts` (create + execute-gate + dedup patch/skip + cap-archive + collision suffix) + `packages/agent-core/test/skill-review.test.ts` (detect EN+KO + cap; draft parse / NONE→null / malformed→null / fail-soft) + `packages/autoconfigure/test/skills-registry-precedence.test.ts` (user overrides authored; authored-only still loads) + `apps/cli/src/chat-author-skills.test.ts` (authors from a corrected session, skips no-correction, fail-soft, AND end-to-end: authored → loaded next session via buildSkillRegistry → selected by selectRelevantSkills on a similar request, not on an unrelated one); `pnpm check` + `pnpm lint` 0/0 + `pnpm smoke:live` (request path unaffected) — self-improvement slice (the inputs already existed — ReasoningBank distiller, reflection, UserModel; this is the missing bridge: experience → durable reusable skill)
- [Autonomy] Authored skills record `lastUsedAt` when body-injected per turn, are now included in the REPL's per-turn skill selection (not only the agent-core catalog), and `muse skills authored` lists them with authored/last-used dates (curator lifecycle foundation, after Hermes Agent curator, MIT — see THIRD_PARTY_NOTICES.md) — `AuthoredSkillStore.recordUsage()` (60s throttle, fail-soft, atomic write) + `buildSkillsPrompt` `onSelected` callback + REPL authored-skill merge (user overrides authored on name collision) — `packages/skills/test/authored-skill-store.test.ts` (recordUsage stamps lastUsedAt; returns false for unknown skill; throttled within 60s; updates after >60s) + `apps/cli/src/chat-skills.test.ts` (onSelected fires for body-injected skill only, not for index-only skills); `pnpm check` + `pnpm lint` 0/0
- [Knowledge] Muse synthesises GROUNDED reflections ("dreaming") — higher-level insights about the user from recent episodes/notes — that CANNOT make things up (after OpenClaw's dreaming + Generative Agents reflection, arXiv 2304.03442; honest/local Muse key) — `synthesizeReflections` (agent-core) prompts the LOCAL model over `{id,text}` items; `parseReflections` deterministically strips any cited source id absent from the inputs and drops a reflection below `minSupport` distinct real sources, so an insight can only be grounded in episodes the user actually has — `packages/agent-core/src/reflection-synthesis.test.ts` (11: grounding strips invented ids, minSupport, dedupe, junk-tolerant JSON, fail-soft) + a LIVE qwen3:8b battery `apps/cli/scripts/verify-reflection-synthesis.mjs` in `eval:self-improving` (a recurring networking theme across 3 episodes synthesised + grounded in the right real ids; grounding invariant holds for every reflection); `pnpm lint` 0/0 — P32-1 differentiation slice (idle-daemon wiring + persistence is P32-2)
- [Knowledge] `muse reflections [refresh]` surfaces Muse's GROUNDED "dreaming" — higher-level insights about the user synthesised from past episodes, each citing the real episodes it came from (P32-2; OpenClaw dreaming + Generative Agents reflection in Muse's honest/local key) — `reflections-store` (atomic + mutation-queued, dedup on normalised insight, FIFO-trim) + `muse reflections refresh` (reads recent episodes → `synthesizeReflections` over the local model → `addReflections`, dedup) + `muse reflections` (lists newest-first WITH source episode ids). The honesty guard (`parseReflections` strips any cited id not in the inputs) carries through, so a stored insight can only be grounded in episodes the user actually has — `packages/mcp/src/reflections-store.test.ts` (add/read; dedup across passes; newest-first; tolerant + corrupt-row) + `apps/cli/src/commands-reflections.test.ts` (render shows insight+sources newest-first / empty hint; reflectionsToStore mapping); live: 5 seeded episodes → 2 grounded reflections (3 networking episodes grouped + cited ep-101/102/103; 2 admin ones ep-104/105). `pnpm lint` 0/0 — P32-2 differentiation slice (the reflection engine is now a real user surface; daemon-idle auto-run is P32-3)
- [Knowledge] The daemon AUTO-dreams while idle — a throttled background reflection pass synthesises GROUNDED insights about the user from recent episodes with NO user action, persisting only insights cited to real episodes (P32-3; off by default `MUSE_REFLECTION_ENABLED`, slow cadence `MUSE_REFLECTION_INTERVAL_MS` default 6h) — `muse daemon` `reflectionTick` (gated + throttled by `shouldRunReflection`; now writes via `resolveReflectionsFile(e)` — FIXED to honor the daemon's injected env instead of a global `process.env` path, so a configured daemon dreams to the right file) — `apps/cli/src/commands-daemon.test.ts` ("grounded dreaming tick (P32-3)": enabled + 3 seeded episodes → exactly one grounded reflection persisted citing only the real ids e1/e2/e3 and a `reflections: +1` line; flag unset → nothing written + no `reflections:` line, the gate is real). The `parseReflections` honesty guard carries through, so an auto-dreamed insight can only cite episodes the user actually has — `pnpm --filter @muse/cli build` (tsc) + 1546 cli tests pass + `pnpm lint` 0/0 — P32-3 differentiation slice (closes the P32 dreaming epic: synth → persist/surface → idle auto-run)
- [Reach] Muse-to-Muse SWARM shares KNOW-HOW only (skills/strategies/reasoning), NEVER personal data, with inbound INERT — `muse swarm share|serve|pending|promote`: a peer's shared skill crosses as a PII-redacted, HMAC-signed A2A `message/send`, lands QUARANTINED on the receiver (a peer can never trigger compute), and runs nothing until the user promotes it to an EXECUTE-GATED authored skill (no `requires:` → `muse.skills.run` refuses it). Off by default (`MUSE_A2A_ENABLED`, fail-closed), peer-allowlisted, draft-first share (`--yes`); the Agent Card advertises the contract (`acceptsExecution:false`, `inboundDisposition:[quarantine,reject]`, `piiRedacted:true`) — `packages/a2a` (68 tests/12 files: signing, transport, handler, quarantine, agent-card, council-wire) + `apps/cli/src/commands-swarm.test.ts` (11: full send→quarantine→promote e2e + draft-first redaction + unknown-peer reject). LIVE-AUDITED end-to-end (two configs on localhost, BUILT CLI): A `swarm share` → real signed HTTP 200 → B verified + quarantined → `pending` → `promote` → execute-gated skill; the `sk-proj-…` secret was `[redacted-openai-key]` on the wire; a WRONG-secret share deposited NOTHING — A2A swarm initiative (personal swarm + federation modes), safety per `outbound-safety.md`
- [Reach] The swarm can convene a COUNCIL — `muse swarm council <q>` asks allowlisted peers to reason (data-light: only PII-redacted reasoning crosses — no corpus, no execution), then synthesises ONE grounded answer that cites ONLY the real members who weighed in (Multiagent Debate, Du et al. 2023, arXiv 2305.14325; the honesty gate carries through so a non-participating peer is never cited) — `packages/a2a` council-wire tests (`signCouncilRequest`/`verifyCouncilRequest`) + `packages/agent-core` council synthesis (`produceCouncilReasoning`/`synthesizeCouncilAnswer`/`parseCouncilAnswer` drop non-real members). LIVE-AUDITED: A `swarm council "rent or buy within 3 years?"` → 2 members (phone-A + remote laptop-B over HTTP, real qwen3:8b both ends) → synthesised answer "drawn from: phone-A, laptop-B" (only real participants) — A2A swarm initiative (council mode)
- [Autonomy] Muse runs REINFORCEMENT LEARNING over its learned-strategy playbook (model is fixed → RL on the MEMORY, not the weights): each strategy carries a clamped `reward` that reward-weighted `rankPlaybookStrategies` blends into selection, so a proven strategy surfaces first and one that keeps getting corrected DECAYS out of the injected top-K; the signal is AUTOMATIC — at session end the strategy a correction implicates (most similar to the corrected request) is docked via `adjustPlaybookReward`, alongside ReasoningBank distillation of the new lesson. Reward flows through BOTH injection paths (agent-runtime `buildPlaybookProvider` + chat-only `muse ask` `selectPlaybookSection`); `muse playbook` shows each strategy's learned reward and `muse playbook distill` reports what it decayed — `packages/agent-core/test/playbook.test.ts` (reward breaks a relevance tie; a −5 strategy sinks out of top-K below a fresh peer; absent reward == 0; `clampReward` bounds) + `packages/mcp/test/playbook-store.test.ts` (`adjustPlaybookReward` accumulates/clamps/unknown-id/non-finite no-op; back-compat read of a reward-less entry; non-numeric reward quarantined) + `apps/cli/src/chat-distill-corrections.test.ts` (a corrected session docks the implicated strategy to −1, unrelated untouched). LIVE: `muse playbook list` renders ⟨reward +3⟩/⟨reward −4⟩ and a −4 prose strategy is deranked below an equally-relevant bullet strategy through the BUILT CLI; `pnpm check` green (agent-core 1064 / mcp 1108 / autoconfigure 450 / cli 1547 / api 747) + `pnpm lint` 0/0 — P33-1 RL slice (ACE arXiv 2510.04618 + the sibling veto store; reward-shaped, model-fixed)
- [Autonomy] The RL reward signal is now BIDIRECTIONAL — an explicit user APPROVAL reinforces the strategy that applied, the mirror of correction-driven decay, so the playbook learns from both "you got it right" and "you got it wrong" (not just absence-of-negative). `detectApprovals` (agent-core) is a precision-first EN+KO detector mirroring `detectCorrections` (fires on "perfect"/"exactly right"/"nailed it"/"완벽해"/"딱 좋아"/"바로 그거야" — NOT bare "ok"/"thanks"/"좋아"/"고마워", which acknowledge but don't endorse); at session end `distillSessionCorrections` credit-assigns each approval to the existing strategy most similar to its request and REINFORCES it (+1, `adjustPlaybookReward`), once per strategy (never both decayed and reinforced in one session), alongside correction-decay + distillation. `muse playbook distill` now reports both ↑ reinforced and ↓ decayed — `packages/agent-core/test/correction-distiller.test.ts` (detectApprovals: 13 EN+KO endorsements fire, 9 bare-acknowledgement phrases do NOT, assistant→user pairing + request backfill + maxExchanges cap) + `apps/cli/src/chat-distill-corrections.test.ts` (an approval session with no correction lifts the applied strategy to +1, unrelated untouched, status still "skipped"-for-distillation). agent-core 1068 / cli 1548 tests pass + `pnpm lint` 0/0 — P33-2 RL slice (bidirectional reward from explicit feedback)
- [Autonomy] LEARNED AVOIDANCE closes the RL extinction loop — a strategy corrected into the floor (reward ≤ `PLAYBOOK_AVOID_BELOW` = −4, ~4 implicating corrections) is EXCLUDED from injection ENTIRELY, not merely deranked: `rankPlaybookStrategies` filters it out before selection even when the bank is ≤ topK (where ranking otherwise returns everything, so a deeply-negative strategy would still slip into a small bank). Soft + reversible (the veto-store parallel): it stays in the bank, shows in `muse playbook` as "· avoided (not injected)", and a later approval can lift its reward back above the line. `isAvoidedStrategy` is the shared predicate — `packages/agent-core/test/playbook.test.ts` (an avoided strategy is dropped even at bank ≤ topK; reward −3 still injects ranked last; all-avoided → empty; `isAvoidedStrategy` boundary incl. absent=not-avoided). LIVE: `muse playbook list` renders "⟨reward −4 · avoided (not injected)⟩" and a −4 strategy is excluded from a 2-strategy bank's injection through the BUILT CLI. agent-core 1072 / cli 1548 tests pass + `pnpm lint` 0/0 — P33-3 RL slice (learned avoidance; the soft, reversible veto)
- [Autonomy] The RL reward loop now spans AUTHORED SKILLS too, not just the strategy playbook — a skill the user keeps correcting stops being applied; one they approve earns standing (the same decay/reinforce/avoid mechanics, extended to a second memory type). A sidecar `skill-rewards.json` (name→reward, kept OUT of each SKILL.md so a decay never rewrites the body) holds the reward; `adjustSkillReward` (clamped, mutation-queued) moves it; at session end `applySkillRewardsFromSession` credit-assigns each correction/approval to the authored skill the live prompt WOULD have applied — via the SAME `selectRelevantSkills` the per-turn prompt uses, so credit lands on the skill that actually applied — and decays (−1) / reinforces (+1) it; `buildSkillsPrompt` then DROPS an avoided skill (reward ≤ `SKILL_AVOID_BELOW` = −4) from the per-turn prompt entirely (no body, not even an index line). `muse skills authored` shows each skill's reward + "· avoided (not applied)" — `packages/mcp/test/skill-rewards-store.test.ts` (read/clamp/adjust/avoid-boundary) + `apps/cli/src/chat-skills.test.ts` (selectRelevantSkills + buildSkillsPrompt drop an avoided skill, keep the rest) + `apps/cli/src/chat-author-skills.test.ts` (a corrected request decays the applied skill to −1, an approval reinforces to +1, a bare ack → no change). LIVE: `muse skills authored` renders "reward: −4 · avoided (not applied)" and a −4 skill is excluded from a matching prompt through the BUILT CLI. mcp 1112 / cli 1553 tests pass + `pnpm lint` 0/0 — P33-4 RL slice (reward loop extended from the playbook to authored skills)
- [Autonomy] The user can STEER the RL reward directly — `muse playbook reward <id> [amount] [--down]` and `muse skills reward <name> [amount] [--down]` manually reinforce (or, with `--down`, penalise) a learned strategy/skill, clamped via the SAME `adjustPlaybookReward`/`adjustSkillReward` the auto-signal uses. So a strategy/skill the auto-loop wrongly penalised can be RESCUED back above the avoid line, and a known-good one PRE-TRUSTED — the reversibility + control that makes the (default-off) auto-RL safe to turn on. `amount` is a positive integer (default 1) so `--down` sidesteps commander parsing a leading `-` as a flag; an unknown id/name is refused and never written — `apps/cli/src/commands-playbook.test.ts` (reinforces +amount, --down penalises, clamps, prefix-matched id, unknown id refused) + `apps/cli/src/commands-skills.test.ts` (adjusts an authored skill, --down penalises, unknown skill refused + not written; `resolveSkillRewardsFile` env/default). LIVE: `muse playbook reward <id> 3` → +3, then `--down 8` clamps to −5 and `muse playbook list` shows it "· avoided (not injected)" through the BUILT CLI. cli 1556 tests pass + `pnpm lint` 0/0 — P33-5 RL slice (manual reward control: user steering + reversibility over the auto-loop)
- [Presence] `muse learned` is one honest view of what Muse has LEARNED about working with you — the strategies & skills it now TRUSTS (reinforced, reward ≥ +1, highest first), the ones it has learned to AVOID (corrected too often, reward ≤ −4, no longer applied), and the grounded reflections it has formed — composed from the playbook + authored-skill + skill-reward + reflection stores with NO model call. The "shows its work" edge turned on Muse's OWN self-improvement, so the (default-off) RL learning (P33) is legible enough to trust + enable: the empty state explains how to turn learning on, and tips point to `muse playbook/skills reward` to tune it — `apps/cli/src/commands-learned.test.ts` (`renderLearnedDigest`: empty → enable hint; trusted sorted desc + neutral reward-0 hidden; avoided under its own heading; reflections newest-first capped at 5). LIVE: a seeded mix → `muse learned` renders the trusted (+3 strategy, +2 skill), the avoided (−5 strategy), and a dated grounded reflection through the BUILT CLI. cli 1560 tests pass + `pnpm lint` 0/0 — P33-6 slice (makes the RL self-improvement visible + trustworthy; the precondition to enabling default-off learning)
- [Autonomy] Skill selection is now reward-WEIGHTED, not just avoidance-gated — among equally-relevant authored skills competing for the limited per-turn body slots, a reinforced (higher-reward) one is selected first, completing skill-RL to full parity with the playbook's reward-weighted ranking (decay · reinforce · avoid · RANK). `selectRelevantSkills` blends `SKILL_REWARD_RANK_WEIGHT × reward` into the rank AFTER the token-overlap relevance gate, so reward orders relevant skills but NEVER makes an irrelevant one relevant; `buildSkillsPrompt` + chat-ink thread each skill's reward from `skill-rewards.json` — `apps/cli/src/chat-skills.test.ts` (the higher-reward of two equally-relevant skills wins the single slot, overriding the name tie-break; a +5 but zero-overlap skill is still excluded). cli 1562 tests pass + `pnpm lint` 0/0 — P33-7 RL slice (reward-weighted skill ordering; skill-RL now mirrors the playbook end-to-end)
- [Knowledge/Edge] The recall WEDGE gained an OUTPUT-side grounding gate, not just the input-side confidence gate — `enforceAnswerCitations` deterministically STRIPS any `[from <source>]` a `muse ask` answer cites that ISN'T a note Muse actually showed the model, so a fabricated citation can never reach the user BY CODE (mirrors `parseReflections`/`parseCouncilAnswer` for the recall surface — the model never decides what counts as a valid source). Wired into the chat-only recall path: invented citations are removed from the answer, the user is warned ("Removed N citation(s) to source(s) you don't have"), and `--json` carries `strippedCitations`; the `--with-tools` agent (different source set) is gated separately — `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (`citedSourcesIn` extracts `[from X]`; `enforceAnswerCitations` keeps real / strips invented / case-insensitive / mixed / whitespace-tidy — 7 cases) + LIVE `apps/cli/scripts/verify-recall-citation-gate.mjs` in `eval:self-improving` (real qwen3:8b: every surviving citation resolves to a real source; an injected `[from secrets/ssn.md]` is deterministically stripped + reported). agent-core 1179 / cli 1562 tests pass + `pnpm lint` 0/0 — HARDENS the wedge to the deterministic output-gate standard of the other grounded surfaces (the "shows its work" edge focus — `docs/strategy/the-edge.md`)
- [Knowledge/Edge] The recall citation gate now covers EVERY citation type a `muse ask` answer makes, not just notes — `[from <note>]` + `[feed: <name>]` match EXACTLY (they're identifiers), while the free-text title forms `[task: …]` / `[event: …]` / `[reminder: …]` match by CONTENT-TOKEN OVERLAP so a paraphrased-but-real title survives but a wholly-invented one (`[event: lunch with Bob]` when no such event exists) is stripped — INCLUDING `[session: …]`, matched against the retrieved past-session summaries (the model rewrites the recap, so overlap not exact). So a fabricated citation of ANY of the six forms to something the user doesn't have is removed by code. `enforceAnswerCitations` takes an `AllowedCitations` set (notes/feeds/tasks/events/reminders/sessions) built from the real grounding at answer time and wired into BOTH `muse ask` recall paths — chat-only (allowed notes = the retrieved `scored`) AND `--with-tools` (the agent can pull MORE via knowledge_search, so its allowed notes are the whole LIVE corpus — any real note file is fair, only a citation to a non-existent file is stripped), feeds/tasks/events/reminders/sessions the same on both — `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (10: note exact incl. mixed/case; feed exact-strip; task/event + session token-overlap keep-paraphrase vs strip-fabricated) + the LIVE `verify-recall-citation-gate` battery still PASS on qwen3:8b. agent-core 1192 / cli 1562 tests pass + `pnpm lint` 0/0 — widens the wedge's output gate to every claim source incl. past sessions, on BOTH ask paths (shows-its-work edge)
- [Knowledge/Edge] The `--with-tools` answer is now STRIP-BEFORE-SHOW, not warn-after — its buffered answer prints AFTER the citation gate, so a fabricated citation is removed from the displayed text before the user ever sees it (the chat-only path streams live, so it can only warn post-hoc). Completes the "a fabricated citation can NEVER reach the user BY CODE" guarantee on the buffered ask path — `apps/cli/src/commands-ask.ts` (the tools-branch `io.stdout(answer)` moved below the gate) — cli 1562 tests pass + `pnpm lint` 0/0 (shows-its-work edge)
- [Knowledge/Edge] `muse ask` now prints a followable "📎 Sources" footer — the notes the answer actually CITED (post-gate, so only the real surviving citations), each as a full OPENABLE path under the notes dir, so the user can open the exact receipt to VERIFY rather than just trust the inline `[from …]` name. "Shows its work" made tangible: the grounding is one `open` away, not just shown — deepening the wedge from code-gated (can't fabricate) to user-verifiable (you can check). `formatSourcesFooter` is pure (post-gate `citedSourcesIn` → dedup → resolve each to its full/absolute path; undefined when nothing was cited so there's no empty footer) — `apps/cli/src/commands-ask-sources-footer.test.ts` (cited → openable paths; none → no footer; dedup a repeat; an already-absolute path kept as-is). cli 1566 tests pass + `pnpm lint` 0/0 (shows-its-work edge — followable receipts)
- [Front door] A first-time user with NO setup and NO ingested notes can SEE Muse's edge in ONE command — `muse demo` points the REAL `muse ask` recall path at a bundled SAMPLE corpus (shipped in the cli package, `sample-corpus/notes/`) inside a throwaway HOME, then runs two scripted questions: an ANSWERABLE one (returns the WireGuard **MTU 1380** WITH a `[from 2026-03-03-vpn-wireguard.md]` citation + an openable 📎 Sources path) AND a MUST-REFUSE one (an honest "I don't have access to … your sister's birthday" — no fabricated answer, no invented citation). Isolation is by construction: a HOME/USERPROFILE override redirects EVERY `~/.muse/*` default (notes index, tasks, reminders, episodes) into the throwaway dir — backed by the new env-overridable `resolveNotesIndexFile` (`MUSE_NOTES_INDEX_FILE`, wired into `muse ask`'s `notesIndexPath`) — so the demo NEVER reads or writes the user's real `~/.muse`; `--top 12` injects the whole tiny corpus so the answerable note is never ranked out of context (guards against a false refusal on a question the corpus DOES answer) — `apps/cli/src/commands-demo.test.ts` (corpus resolves to a real dir containing the vpn note + repo-fixtures fallback; one-answerable+one-refuse pairing answerable-first; `buildDemoEnv` redirects HOME/USERPROFILE + isolates the index off `~/.muse` + forces `MUSE_LOCAL_ONLY`; the command prints the local-only banner, asks both questions against the isolated env, and awaits an async runner) + `packages/autoconfigure` 464 tests (the new resolver). LIVE qwen3:8b via the BUILT CLI: `node apps/cli/dist/index.js demo` → "You set the MTU to **1380** … [from 2026-03-03-vpn-wireguard.md]" + a 📎 Sources footer to the real openable note, then an honest refusal on the sister's-birthday question, with NO real `~/.muse` task/reminder leak (the first run's leak was fixed by the HOME override). cli 1572 / autoconfigure 464 tests pass + `pnpm lint` 0/0 — front-door rung (a): the zero-setup cited-answer + honest-refusal demo (loop-v2 B0 §3a) — P34-1 bullet
- [Front door] `muse ask`'s first-run auto-reindex now STREAMS per-file ingest progress AND makes a partial failure VISIBLE — a beachhead user pointing `muse ask` at a real corpus for the first time (PDFs embed slowly on CPU) sees `+ <file> (n chunks embedded)` per note instead of a silent multi-second hang, and a corrupt/unreadable file shows `✗ <file> (could not read — skipped: <reason>)` and is SKIPPED, not fatal (the rest ingest). Two surface fixes closing rung (c)'s named "progress signal + partial-failure tolerance": (1) the ask auto-reindex now passes `onProgress` to stderr and the summary reports a `skipped` count; (2) `reindexNotes`' extract-failure catch — previously a SILENT `failed++; continue` — now emits the `✗ … could not read` progress line (the engine already walked `.pdf` via `extractDocumentText`/`parsePdfBuffer`, but a corrupt file was swallowed with zero feedback) — `apps/cli/src/commands-notes-rag.test.ts` ("a corrupt document is skipped VISIBLY, not silently": a `%PDF-` garbage file throws "Invalid PDF structure", emits the ✗ skip line, the good note still embeds, the run doesn't abort, the corrupt file isn't stored as a hollow entry). LIVE qwen3:8b on a `.muse-dev` mock corpus (seed notes + a corrupt `.pdf`, isolated HOME — never real `~/.muse`): the first `muse ask` streamed `+ <file>` lines + `✗ corrupt-download.pdf (could not read — skipped: Invalid PDF structure.)` + `(notes index refreshed: 6 embedded, 0 cached, 1 skipped)`, then returned a CITED answer ("MTU **1380** … [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources footer) AND an honest refusal on the sister's-birthday question. cli 1573 tests pass + `pnpm lint` 0/0 — front-door rung (c): corpus ingest shows progress + tolerates a bad file VISIBLY (loop-v2 B0 §3c) — P34-2 bullet
- [Knowledge/Edge] `muse ask` no longer FALSE-REFUSES a question its corpus answers when the answer note's embedding cosine ranks below near-misses — the headline chat-only recall path is now HYBRID (embedding-cosine + lexical keyword RRF), the same fusion the `knowledge_search` agent path already used (P23, RRF: Cormack et al. SIGIR 2009), but it was never applied to the headline `muse ask` path, which ranked notes by PURE cosine. On nomic's compressed embedding space a query with strong distinctive terms ("WireGuard", "MTU") ranked the one answer-bearing note ~5th, so the default top-3 excluded it and Muse honestly-but-WRONGLY said "I'm not sure" — the "GUARD THE EDGE" failure mode (a false refusal turns honest into useless). `diversifyAskChunks` gains an optional `query`: when present it fuses the cosine rank with a lexical-overlap rank (reusing agent-core's now-exported `lexicalTokens`/`lexicalOverlap`/`fuseByReciprocalRank`) via RRF, normalises the fused relevance to [0,1] so MMR's diversity term stays comparable, and selects the top-K from that — WITHOUT re-embedding (index embeddings reused) and KEEPING each chunk's absolute cosine `score` so the CRAG confidence framing is unchanged; a stopword-only query falls back to the prior cosine MMR — `apps/cli/src/commands-ask-mmr.test.ts` (hybrid: a keyword-matching answer note that pure-cosine top-3 EXCLUDES is rescued into the top-3; the rescued chunk keeps its absolute cosine 0.50; a stopword-only query is byte-identical to cosine selection). LIVE qwen3:8b on a `.muse-dev` mock corpus at DEFAULT top-3 (no `--top` override, isolated HOME — never real `~/.muse`): "What MTU did I set for the WireGuard VPN?" now grounds the vpn note FIRST and answers "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources (was a false refusal last fire), "When is rent due and how much?" answers "the 25th … $1,450 [from tasks/finances.md]", AND "What's my sister's birthday?" still honestly refuses ("None of the provided context contains information…", no fabrication). cli 1576 / agent-core 1228 tests pass + `pnpm lint` 0/0 — hardens the recall wedge: fewer false refusals at default settings on the headline command (shows-its-work edge / loop-v2 B0 "GUARD THE EDGE")
- [Knowledge/Edge] A correctly-grounded `muse ask` answer no longer carries a FALSE "⚠ LOW confidence — verify, may not be in your notes" caution — the CRAG confidence framing now considers LEXICAL strength, not absolute cosine alone. nomic's cosine space is compressed, so a genuinely-relevant note (e.g. the WireGuard note at cosine ~0.50) sat just below the confident threshold and got flagged LOW even when ranked first and cited correctly — a SOFT false-refusal that tells the user not to trust an answer that IS grounded, eroding the trust edge. `notesGroundingFraming` now takes the query: a STRONG lexical match (≥2 distinct query content tokens present in a grounded chunk, reusing agent-core's `lexicalTokens`/`lexicalOverlap`) upgrades an ambiguous-cosine verdict to `confident`; a must-refuse question shares no content tokens so it stays LOW/none, and the output-side citation gate remains the hard backstop — fabrication=0 is preserved by construction — `apps/cli/src/commands-ask-crag.test.ts` (ambiguous-cosine + strong-keyword query → confident, plain header, no weak-match guidance; a must-refuse query → stays LOW; a single shared token does NOT upgrade — needs ≥2). LIVE qwen3:8b at DEFAULT top-3 on a `.muse-dev` mock corpus (isolated HOME — never real `~/.muse`): "What MTU did I set for the WireGuard VPN?" grounds the vpn note first, shows a CLEAN grounding line (no LOW-confidence warning), and answers "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources; "What's my sister's birthday?" STILL shows ⚠ LOW confidence and honestly refuses with no fabrication. cli 1579 tests pass + `pnpm lint` 0/0 — hardens the trust edge: a correct cited answer is no longer undercut by a false caution, while the honest refusal is untouched (shows-its-work edge / loop-v2 B0 "GUARD THE EDGE")
- [Front door] A beachhead user can get a whole FOLDER of real documents into their searchable corpus in ONE command — `muse read <dir> --save-to-notes <prefix>` now bulk-ingests every supported doc (`.pdf/.txt/.md/.markdown/.log/.csv`) under a directory (recursively, subdirs preserved) into the notes corpus as `.md` notes under `<prefix>/`, so they're citable via `muse ask`. Previously `muse read` was one-file-at-a-time (a 50-PDF download pile meant 50 invocations); pointing it at a directory walks the tree, extracts each doc (reusing `extractDocumentText`/`parsePdfBuffer`), and saves each via `saveDocumentToNotes`, with per-file progress (`+ <file> → <id>.md`) and partial-failure tolerance (a corrupt/binary/empty file is SKIPPED with a `✗` line, never aborts the run — same contract as the reindex ingest). Bug found+fixed in the slice: notes were first written WITHOUT a `.md` extension, so the notes-index walker (`.md/.markdown/.txt/.pdf` only) skipped them and `muse ask` couldn't cite them — the live run proved "I am not sure" on an ingested fact; the save now appends `.md` so the note is actually indexed — `apps/cli/src/commands-read.test.ts` (`ingestDirectoryToNotes`: ingests every supported doc, skips a corrupt PDF VISIBLY + a binary-ext file isn't walked, writes `.md` notes under the prefix with subdirs preserved, run doesn't abort; `noteIdForDocument` strips ext / preserves subdirs / applies prefix). LIVE qwen3:8b on a `.muse-dev` docs folder (a `.txt`, a nested `.md`, a corrupt `.pdf`, isolated HOME — never real `~/.muse`): `muse read <dir> --save-to-notes downloads` → "ingested 2, skipped 1 of 3" with the ✗ skip line, then `muse ask` cited "warranty expires 2027-08-19 [from downloads/warranty.md]" + 📎 Sources AND "gate 24 [from downloads/manuals/trip.md]", and honestly refused the sister's-birthday question. cli 1581 tests pass + `pnpm lint` 0/0 — front-door rung (c): get a real folder of documents in, in one command (loop-v2 B0 §3c "get THEIR real corpus in") — P34-5 bullet
- [Front door] `muse read <file> --save-to-notes <id>` now actually makes the document citable by `muse ask` — it told the user "now searchable" but saved the note under a BARE extensionless id (e.g. `garage`), which the notes-index walker (`.md/.markdown/.txt/.pdf` only) skipped, so `muse ask` answered "I don't have access" on a just-ingested fact: a false-claim correctness bug on the corpus front door (the single-file sibling of the bug fixed for bulk ingest in P34-5). A shared `ensureNoteMarkdownExtension` appends `.md` unless the id already carries an indexable text extension (`.md/.markdown/.txt`), wired into BOTH the single-file save and the bulk-folder path; the success message now says "searchable via `muse ask` and knowledge_search" — `apps/cli/src/commands-read.test.ts` (`ensureNoteMarkdownExtension`: appends `.md` to a bare/subdir id, leaves `.md`/`.markdown`/`.txt` untouched — no double extension; a single-file `--save-to-notes <bareId>` save is read back as a `.md` note). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): `muse read garage.txt --save-to-notes garage` saved `garage.md` and `muse ask "What's the garage door opener code?"` cited "7731 [from garage.md]" + 📎 Sources (was "I don't have access" before the fix), while the sister's-birthday question still honestly refused. cli 1584 tests pass + `pnpm lint` 0/0 — closes the single-file half of the "ingested doc isn't actually searchable" bug (loop-v2 B0 §3c, "get THEIR real corpus in" — verified, not just claimed) — P34-6 bullet
- [Front door] A user can keep their corpus LIVE by dropping documents into a watched folder — `muse watch-folder --ingest` now folds each new file INTO the notes corpus as a citable `.md` note (searchable via `muse ask`) instead of firing a proactive notice, so the day-2 "stays live without re-running ingest" habit works with NO manual step (loop-v2 north star: "watch a folder / auto-ingest new notes — a static one-shot import is a party trick, not a habit"). Reuses the same extract/save contract as `muse read` (`extractDocumentText` + `ensureNoteMarkdownExtension` + `saveDocumentToNotes`), so it's partial-failure tolerant: a corrupt/binary/empty drop is SKIPPED with a `✗` line and never crashes the watcher, then the original is archived to `.processed/`. New `--ingest` + `--notes-prefix <p>` (default `inbox`) options; the note id is `<prefix>/<basename>.md` via the testable `watchIngestNoteId` — `apps/cli/src/commands-watch-folder.test.ts` (`watchIngestNoteId`: `<prefix>/<stem>.md`, keeps an already-indexable extension, tolerates a slashed/empty prefix). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): dropped `pool.txt` + a corrupt `broken.pdf` into a watched inbox → `muse watch-folder --ingest` ingested `pool.txt → inbox/pool.md` and skipped `broken.pdf` (✗, no crash), then `muse ask "What's the pool gate keypad code?"` cited "4417 [from inbox/pool.md]" + 📎 Sources, while the sister's-birthday question honestly refused. cli 1586 tests pass + `pnpm lint` 0/0 — front-door rung (d): continuous folder-watch CORPUS ingest, the day-2 retention piece (loop-v2 B0 §3d) — P34-7 bullet
- [Front door] A brand-new user who runs `muse ask` before adding any notes is now SHOWN how to get started instead of just hitting a dead-end refusal — when the notes corpus is EMPTY (zero indexed notes), `muse ask` prints a one-time on-ramp hint naming every real ingest path (`muse demo` for a sample, `muse read <file>/<dir> --save-to-notes`, `muse watch-folder --ingest`) and STILL answers honestly (the refusal is unchanged; the hint is additive). The hint fires ONLY on an empty corpus — once any note exists it returns undefined, so a normal no-match answer is never cluttered. `corpusOnboardingHint(liveNoteCount)` is pure + gated on the live note count (`filterLiveNoteIndexFiles`), wired into the chat-only `muse ask` path after the index load — `apps/cli/src/commands-ask-onboarding.test.ts` (empty → hint naming demo/`--save-to-notes`/`watch-folder --ingest`; ≥1 note → undefined). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): an EMPTY corpus → the on-ramp hint + an honest "I don't have access" refusal; a POPULATED corpus → NO hint plus a cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources on the answerable question AND an honest refusal ("None of the provided context contains…") on the sister's-birthday must-refuse. cli 1588 tests pass + `pnpm lint` 0/0 — front-door on-ramp: the empty-corpus first run now guides instead of dead-ending, without weakening the honesty floor (loop-v2 B0 §3, the demo/ingest surfaces the headline command refers to) — P34-8 bullet
- [Front door] The empty-corpus on-ramp hint (P34-8) no longer FALSELY fires when embedding is merely down — it was gated on the indexed/live chunk count, but when Ollama is unreachable the auto-reindex embeds 0 chunks (all skipped) so a user WITH notes was wrongly told "your notes corpus is empty"; the gate now counts note FILES on disk (`notesCorpusFileCount`, recursive `.md/.markdown/.txt/.pdf`, hidden/non-note ignored, missing dir ⇒ 0), independent of whether embedding succeeded. So the hint fires only for a genuinely empty corpus; a populated-but-unindexed corpus shows the existing "notes search unavailable — `ollama pull …`" guidance instead — `apps/cli/src/commands-ask-onboarding.test.ts` (`notesCorpusFileCount` counts 3 across subdirs, ignores a `.json` + a hidden `.md`; missing/empty dir ⇒ 0 → hint fires; a 3-file dir ⇒ hint undefined — the Ollama-down state). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): Ollama DOWN + a 6-note corpus → NO "corpus is empty" line, only the `ollama pull`/`ollama serve` guidance; Ollama UP + an EMPTY dir → the on-ramp hint fires; Ollama UP + the populated corpus → no hint, a cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources, and an honest refusal on the sister's-birthday must-refuse. cli 1590 tests pass + `pnpm lint` 0/0 — hardens P34-8: the on-ramp guides a truly-new user without lying to a user whose embedding is just offline (loop-v2 B0 "GUARD THE EDGE" — no false statements to the user) — P34-9 bullet
- [Front door] `muse demo` now shows the edge across TWO distinct notes before the refusal — the zero-setup demo asked ONE answerable + one refuse, which could read as a single lucky hit; it now asks TWO answerable questions citing DIFFERENT notes (WireGuard MTU → `2026-03-03-vpn-wireguard.md`, rent → `tasks/finances.md`) THEN the must-refuse, so a first-time user sees in <60s that cited recall is real across the corpus AND that Muse honestly refuses what it can't ground. `DEMO_QUESTIONS` gains the rent question; the answerable-first ordering + isolation (throwaway HOME, never real `~/.muse`) are unchanged — `apps/cli/src/commands-demo.test.ts` (≥2 answerable citing different notes, then ≥1 refuse; answerable-first; the async runner is awaited for every question). LIVE qwen3:8b via the BUILT CLI `muse demo`: "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources, then "rent … the 25th … $1,450 [from tasks/finances.md]" + 📎 Sources, then an honest refusal on the sister's-birthday question. Also this fire: a FULL live regression sweep of the EXPECTED.md oracle on the mock corpus at DEFAULT top-3 (isolated HOME) — all 6 answerable returned the right fact + citation (A1 MTU 1380, A2 rent 25th/$1,450, A3 next-business-day, A4 coffee, A5 Foundry 2026-05-22, A6 Backblaze 2026-09-14) and all 4 must-refuse honestly refused with no fabrication (R1 sister, R2 wifi, R3 car, R4 flight) — the recall edge is green end-to-end. cli 1590 tests pass + `pnpm lint` 0/0 — front-door rung (a): a more convincing zero-setup payoff + a clean full-oracle edge sweep (loop-v2 B0 §3a) — P34-10 bullet
- [Knowledge/Edge] A `muse ask` REFUSAL no longer presents a followable "📎 Sources (open to verify)" footer pointing at a note that doesn't support it — found via a KOREAN must-refuse the English-only oracle sweep missed: the local model honestly refused ("저는 …정보를 가지고 있지 않습니다") but tacked on a spurious `cite as: [from preferences.md]`, which the citation gate KEPT (preferences.md is a real retrieved source) and the Sources footer then surfaced as "open to verify" — a citation on an answer that asserts nothing, which breaks "shows its work" (the user opens preferences.md and finds nothing about the question). A refusal grounds no claim, so a precision-first `answerIsRefusal` (EN+KO markers: "i don't have"/"none of the provided context"/"i'm not sure"/"가지고 있지 않"/"없습니다"/"찾을 수 없"/… — kept tight so a real cited answer never matches) now drops ALL citations from a refusal: the Sources footer is suppressed on EVERY path, and the inline `[from …]` is also stripped on the buffered `--with-tools`/`--json` paths. (Chat-only refusals stream live, so the inline marker can still flash on screen — the known streaming limitation the invented-citation gate already has; the followable footer, the actively-misleading part, is gone on all paths.) — `apps/cli/src/commands-ask-refusal.test.ts` (`answerIsRefusal`: fires on clear EN + KO refusals incl. the exact live failure phrase, never on a real cited answer EN/KO). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): the Korean sister's-birthday refusal → NO Sources footer (was `📎 …/preferences.md`); the Korean WireGuard answerable → still cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 Sources (no regression); a `--with-tools` car-payment refusal → no citation, no footer. cli 1593 tests pass + `pnpm lint` 0/0 — closes a demonstrated fabrication=0 leak (a citation on a refusal) surfaced only cross-lingually; the honest refusal now cites nothing, by code (shows-its-work edge) — P34-11 bullet
- [Presence/Felt] `muse ask` now renders each cited note as a MEMORY, not a filename — the "📎 From your notes" receipt shows "• from your note of <date> — '<verbatim snippet>'" + the openable path, so the grounding reads like Muse recalling WHERE you said it (S1 "citation-as-voice", loop-v2 PART B2 felt-experience, BUILD-FIRST). Pure deterministic render — `formatSourceReceipts` composes the date parsed from the note filename (`provenanceDate`, `YYYY-MM-DD`) + a whitespace-collapsed verbatim excerpt of the grounded chunk (`provenanceSnippet`, ≤90 chars) + the resolved path — NO second model call, the grounding/citation gate untouched, and it's post-gate so a refusal (which cites nothing) renders NO receipt — `apps/cli/src/commands-ask-receipts.test.ts` (`provenanceDate` parses/!parses; `provenanceSnippet` collapses+truncates+ellipsis; `formatSourceReceipts` renders dated memory + verbatim snippet + openable path, falls back to the note name when undated, returns undefined on a no-citation refusal, omits the snippet when no grounded chunk matches e.g. `--with-tools`). LIVE qwen3:8b on the mock corpus (isolated HOME — never real `~/.muse`): "What MTU…" → answer + "📎 From your notes … • from your note of 2026-03-03 — '# WireGuard VPN setup … Set this up on 2026-03-03 …' /…/2026-03-03-vpn-wireguard.md"; the sister's-birthday must-refuse → no receipt. cli 1599 tests pass + `pnpm lint` 0/0 — first felt-experience slice (rung 5 / B2 S1): the receipt feels like a memory, honesty floor unchanged. HONEST scope: the snippet is the cited chunk's opening excerpt (small notes = one chunk), not yet the single most-relevant line — a later refinement — P35-1 bullet
- [Presence/Felt] The citation-as-voice receipt (P35-1) now quotes a real CONTENT line the user wrote, never a structural markdown heading — it was excerpting the cited chunk's OPENING, which on a `# Heading`-led note read robotically ("from your note of 2026-03-03 — '# WireGuard VPN setup'"). `relevantSnippet(text, query)` now drops markdown heading lines (`^#{1,6}`) and, among the remaining content lines, picks the highest query-content-token-overlap one (reusing `lexicalTokens`/`lexicalOverlap`), falling back to the first content line (or the whole text if a note is nothing but a heading) — still pure deterministic, verbatim, gate untouched. So the receipt reads like a sentence you actually said, not a file's title — `apps/cli/src/commands-ask-receipts.test.ts` (`relevantSnippet`: never returns a `#` heading; picks the VPN+MTU content line over an unrelated line for an MTU query; skips the heading with no query → first content line; heading-only note → returns it; + `formatSourceReceipts` quotes the query-relevant line of a multi-line note). LIVE qwen3:8b on the mock corpus (isolated HOME — never real `~/.muse`): "What MTU…" → "📎 From your notes … • from your note of 2026-03-03 — 'Set this up on 2026-03-03 after the office switched to the new firewall.' /…/2026-03-03-vpn-wireguard.md" (a content sentence, not the `# WireGuard VPN setup` heading); the sister's-birthday must-refuse → no receipt. cli 1603 tests pass + `pnpm lint` 0/0 — completes the P35-1 follow-up (felt receipt quotes content, not headings). HONEST scope: among many content lines it picks highest-overlap (ties → first); pinpointing the single exact answer line within one blob chunk remains imperfect — P35-2 bullet
- [Presence/Felt] `muse ask` now NARRATES the wait so the 10–40s local-model pause reads as thinking, not hanging (S3 "narrate the wait", loop-v2 PART B2). It emits two REAL pipeline-stage deltas to stderr — "🔎 searching your notes…" before the query embed, and "💭 generating your answer on the local model…" before the generation await — bracketing the existing "(grounded on N note chunk(s) …)" banner, so the sequence is searching → grounded-on-N → generating → [answer streams]. Latency-honest per the B2 guardrail: it names only stages that actually run (no invented step), and is suppressed under `--json` so machine consumers stay clean. The answer, grounding gate, citation receipt, and refusal path are all untouched. Verified by the cli suite staying green (145 files / 1603 tests — no regression; this is inline UI narration with no pure function to unit-test) + LIVE qwen3:8b on the mock corpus (isolated HOME — never real `~/.muse`): "What MTU…" → "🔎 searching your notes…" then "(grounded on 3 …)" then "💭 generating … local model…" then the cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 receipt; the sister's-birthday must-refuse → the same deltas then an honest "None of the provided context contains…" with no receipt. `pnpm lint` 0/0 — felt-experience slice (rung 5 / B2 S3): the wait feels like thinking, honesty + latency-honesty intact — P35-3 bullet
- [Presence/Felt] An honest `muse ask` refusal now FEELS cared-for, not blocked — when Muse refuses AND the user already has notes, it closes with one on-brand line "(I'd rather tell you that than guess — add a note on this and I'll have it next time.)" (S2 "warm honesty", loop-v2 PART B2). Deterministic fixed template (no second model call, B2-compliant), gated by `shouldWarmClose(answer, noteFileCount)` = honest refusal (`answerIsRefusal`, EN+KO) AND ≥1 note on disk — so an EMPTY corpus gets the first-run on-ramp hint instead (the two never double up) and a real CITED answer never gets it. No note pointer, so it can't reintroduce the spurious-citation-on-a-refusal confusion P34-11 closed; reuses the on-ramp's already-computed `notesCorpusFileCount` (no extra disk walk) — `apps/cli/src/commands-ask-refusal.test.ts` (`shouldWarmClose`: fires on an EN + a KO refusal with notes present; does NOT fire at 0 notes (on-ramp covers it) nor on a real cited answer). LIVE qwen3:8b on the mock corpus (isolated HOME — never real `~/.muse`): the sister's-birthday + car-payment must-refuses → honest refusal then the warm close on its own line; "What MTU…" → the cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + 📎 receipt with NO warm close. cli 1605 tests pass + `pnpm lint` 0/0 — felt-experience slice (rung 5 / B2 S2): the honesty edge now lands warmly + nudges the corpus-building habit, fabrication floor untouched — P35-4 bullet
- [Autonomy/Self-learning] The background self-learning daemon now brakes on REAL OS idle, not just Muse's API quiet — the first brake-first prerequisite for safe unattended learning (PART A2 / B1 Slice 0). `isIdleForConsolidate` gated only on `lastActivityMs` (Muse /api activity), which per A2 "reports idle exactly when the laptop is busiest in ANOTHER app" — so an enabled consolidation could fire while the user is editing/compiling. New `apps/api/src/os-idle.ts`: `parseHidIdleSeconds` (pure parse of `ioreg -c IOHIDSystem`'s `HIDIdleTime` nanoseconds → seconds), `osIdleMs(runIoreg?)` (system-wide idle in ms; FAIL-CLOSED → undefined on non-macOS / ioreg error / parse failure), `isOsIdleEnough(osIdle, thresholdMs)` (undefined ⇒ NOT idle). `consolidate-tick` gains an optional `osIdleMs` seam: when wired, the LLM merge ALSO requires the MACHINE idle ≥ threshold (fail-closed), and `tick-daemons` passes the real probe — so the daemon never strains the laptop while the user works elsewhere. Omitted ⇒ OS-gate skipped (back-compat). — `apps/api/src/os-idle.test.ts` (parse ns→s; undefined on absent/garbage; `osIdleMs` ms via injected ioreg + undefined when it throws; `isOsIdleEnough` fail-closed on undefined, threshold boundary) + `apps/api/src/consolidate-tick.test.ts` (OS busy → no consolidate though /api quiet; OS-idle unknown → no consolidate (fail-closed); BOTH /api+OS idle → consolidates). LIVE on this macOS box: `osIdleMs()` returned ~10,632,000 ms (~10632s) parsed from the REAL `ioreg` HIDIdleTime — the probe reads true system-wide idle, not a fixture. api 756 tests pass + `pnpm lint` 0/0. Verification follows the rung-4 proof shape (unit + live probe round-trip), NOT cited-answer+refusal (CURRENT PHASE: rung-4 verifies differently); the recall path is untouched (different package). HONEST scope: brake-first INFRA — the felt payoff lands with the idle-distillation WRITER slice (B1 Slice 1); this is the prerequisite that makes enabling it safe — P36-1 bullet
- [Autonomy/Self-learning] The self-learning daemon now also brakes on MODEL-RESIDENT — it will never COLD-load the multi-GB model in the background (the 2nd B1 Slice-0 prerequisite, after the OS-idle brake P36-1). New `apps/api/src/model-resident.ts`: `parseResidentModels` (pure parse of Ollama `/api/ps` → loaded model names, tolerant of shape drift), `isModelResident(model, names)` (provider-prefix + tag aware: `ollama/qwen3:8b` matches `qwen3:8b`), `isModelResidentLive(model, baseUrl?, fetchImpl?)` (GET `/api/ps`, FAIL-CLOSED → false on non-OK / throw / Ollama-down). `consolidate-tick` gains an optional async `isModelResident` seam: when wired, the LLM merge runs ONLY if the model is already loaded (never an unattended cold load); omitted ⇒ skipped (back-compat). `tick-daemons` wires `isModelResidentLive(defaultModel)` (only when a model is configured). So the daemon now defers learning unless (OS idle AND model already warm) — `apps/api/src/model-resident.test.ts` (parse name+model deduped / tolerant of missing-models; provider-prefix+tag match; live probe true when listed, false when absent, fail-closed on throw + non-OK) + `apps/api/src/consolidate-tick.test.ts` (model not resident → no merge; idle + resident → merge). LIVE on this box against the REAL Ollama `/api/ps` ({"models":[]} — nothing loaded): `isModelResidentLive('ollama/qwen3:8b')` → false (correctly DEFERS, no cold load), a bogus model → false, and a down port → false (fail-closed). api 774 tests pass + `pnpm lint` 0/0. Verification follows the rung-4 proof shape (unit + live probe round-trip), NOT cited-answer+refusal (recall path untouched). HONEST scope: brake-first INFRA (default-off learning) — the felt payoff lands with the writer slice (B1 Slice 1); the remaining Slice-0 brake is the cross-process Ollama lease — P36-2 bullet
- [Autonomy/Self-learning] The self-learning daemon now also brakes on AC POWER — a heavy unattended LLM merge runs on wall power only, never draining the battery (3rd B1 Slice-0 brake, after OS-idle P36-1 + model-resident P36-2; A2/B1 "battery ⇒ memory-only"). New `apps/api/src/power-state.ts`: `parseOnAcPower` (pure parse of `pmset -g batt` "Now drawing from 'AC|Battery Power'" → true/false/undefined), `isOnAcPower(runPmset?)` (FAIL-CLOSED → undefined on non-macOS / pmset error), `isPowerOkForLlm(onAc)` (only confirmed AC is OK; battery + unknown ⇒ skip). `consolidate-tick` gains an optional `isOnAcPower` seam: when wired, the LLM merge runs only on confirmed AC; omitted ⇒ skipped (back-compat). `tick-daemons` wires the real probe. Net daemon gate is now OS-idle AND model-warm AND on-AC before any unattended merge — `apps/api/src/power-state.test.ts` (parse AC/battery/undefined; `isOnAcPower` reads injected pmset on macOS + undefined off-mac/on-throw; `isPowerOkForLlm` AC-only) + `apps/api/src/consolidate-tick.test.ts` (battery → no merge though idle; unknown power → no merge (fail-closed); idle + AC → merge). LIVE on this box against REAL `pmset -g batt` ("Now drawing from 'AC Power'"): `parseOnAcPower` → true, `isOnAcPower()` → true (correctly reads wall power → merge allowed here). api 791 tests pass + `pnpm lint` 0/0. Verification follows the rung-4 proof shape (unit + live probe), NOT cited-answer+refusal (recall path untouched). HONEST scope: brake-first INFRA (default-off learning) — felt payoff lands with the writer slice (B1 Slice 1); the remaining Slice-0 brake is the cross-process Ollama lease (cross-package: chat + daemon) — P36-3 bullet
- [Autonomy/Self-learning] Foreground chat/ask and the background self-learning daemon no longer contend for the local model — a cross-process Ollama LEASE completes the B1 Slice-0 brake set (after OS-idle P36-1, model-resident P36-2, AC-power P36-3; atomic writes pre-existing). New `packages/mcp/src/ollama-lease.ts` (shared by cli + api): `resolveOllamaLeaseFile` (`~/.muse/ollama.lease`, env-overridable), `acquireOllamaLease`/`releaseOllamaLease` (atomic-write pid+heartbeat; release only by the owner), `isOllamaLeaseHeldByOther` (held iff a DIFFERENT, LIVE pid holds a FRESH lease — FAIL-SAFE to false on missing/corrupt, and auto-releases a dead-pid or stale-heartbeat lease so a crashed holder can't wedge the daemon). `muse ask` now acquires the lease around its model stream and releases after (best-effort; process-exit frees it via the dead-pid check); `consolidate-tick` gains an `isForegroundBusy` seam (defer the merge while a foreground lease is held), wired in `tick-daemons`. Net daemon gate is now OS-idle AND model-warm AND on-AC AND no-foreground-contention — `packages/mcp/src/ollama-lease.test.ts` (resolver; held-by-other true / self false / dead-pid false / stale false / no-lease false; release only by owner) + `apps/api/src/consolidate-tick.test.ts` (foreground busy → no merge; free → merge). LIVE (isolated HOME — never real `~/.muse`): a lease round-trip on qwen3:8b box — acquire → held-by-other true (daemon defers), held-by-self false, after-release false; AND `muse ask` under the lease still returned the cited "MTU **1380** [from 2026-03-03-vpn-wireguard.md]" + receipt and an honest refusal ("None of the provided context contains…" + warm close), with the lease acquired during generation and released after. mcp 1211 / api 801 / cli 1605 tests pass + `pnpm lint` 0/0. Cited-answer+refusal mandate applies + PASSES here (this slice touches the ask recall path); the brake gates verify by the rung-4 proof shape. HONEST scope: foreground acquisition wired for `muse ask`; the long-running `muse chat` REPL acquirer is the pairing follow-up. This COMPLETES B1 Slice 0 — the idle-distillation WRITER (Slice 1, the felt payoff) is now unblocked — P36-4 bullet
- [Autonomy/Self-learning] The learn-queue signal substrate for idle distillation is in place — the first B1 Slice-1 piece, now that the brakes (Slice 0) are done. Stored episodes keep only SUMMARIES, so the raw correction exchange (user asked → assistant answered → user corrected) can't be recovered later; it must be captured WHEN it happens and consumed on idle. New `packages/mcp/src/learn-queue.ts` (shared): `resolveLearnQueueFile` (`~/.muse/learn-queue.jsonl`, env-overridable), `enqueueLearnEvent` (append-only — one real signal = one job), `readPendingLearnEvents` (oldest-first; corrupt lines skipped; missing file ⇒ []), `markLearnEventsDone` (atomic rewrite removing the distilled ids + trim to `MAX_LEARN_QUEUE_EVENTS`=200 so a runaway producer can't grow the file). Append-only + fail-safe, mirroring the atomic-store/lease pattern — `packages/mcp/src/learn-queue.test.ts` (resolver; missing→empty; enqueue oldest-first + readback; markDone removes only distilled; corrupt line skipped keeping valid ones). LIVE round-trip on this box (isolated temp — never real `~/.muse`): enqueue a correction → readPending (1, correction text preserved) → markDone → 0 pending. mcp 1216 tests pass + `pnpm lint` 0/0. Verification follows the rung-4 proof shape (unit + live round-trip), NOT cited-answer+refusal (queue substrate, not the recall path). HONEST scope: signal SUBSTRATE only — the felt payoff needs the idle distill-consumer tick (reads pending → `distillStrategyFromCorrection` → records a probation strategy behind the brakes) + the chat producer (enqueue on correction) + `muse learned` visibility; those are the remaining B1 Slice-1 parts — P36-5 bullet
- [Autonomy/Self-learning] The idle distill-consumer turns a QUEUED correction into a LEARNED STRATEGY on its own — the felt-core mechanism of B1 Slice 1, behind the complete brake set. New `apps/api/src/distill-queue.ts` `distillQueuedCorrections(deps)`: reads pending learn-queue events, distills ONE per tick (the LLM call is the cost) via the existing `distillStrategyFromCorrection`, records it to the playbook (`recordPlaybookStrategy`), and marks every drained event done. GROUNDING FENCE: an event with no real correction text, or a distiller that returns nothing, writes ZERO strategies (a non-corrective signal never fabricates a lesson) — yet is still drained so a dud can't jam the queue. Wired as an idle REM phase in `consolidate-tick` AFTER all the brakes (OS-idle + model-warm + on-AC + no-foreground-contention), and `tick-daemons` passes the real deps (queue + playbook + local model). Also fixed `enqueueLearnEvent` to create `~/.muse` if absent — `apps/api/src/distill-queue.test.ts` (distills one + records + drains; ≤1 per tick (2nd stays pending); grounding fence: empty correction → no write but consumed; distiller-returns-nothing → no write; empty queue → no-op) + `apps/api/src/consolidate-tick.test.ts` (distillQueued runs when the gates pass, NOT when a brake blocks). LIVE qwen3:8b (isolated HOME — never real `~/.muse`): enqueued "no — give me bullet points, not prose" → `distillQueuedCorrections` distilled 1 → playbook now holds "when asked for a summary, present information in bullet points rather than prose" → queue drained to 0. mcp 1216 / api 809 tests pass + `pnpm lint` 0/0. Verification follows the rung-4 proof shape (unit + LIVE distill round-trip on the local model), NOT cited-answer+refusal (self-learning path). HONEST scope: the felt MECHANISM is proven (queued correction → idle distill → learned strategy, no manual step); remaining Slice-1 parts = the chat PRODUCER (auto-enqueue on a detected correction, migrating exit-distill→idle to avoid double-distilling), PROBATION (record-but-don't-inject for unattended writes), and `muse learned` visibility, then the full 2-session live proof — P36-6 bullet
- [Autonomy/Self-learning] A user can now leave a correction in chat, walk away, and have Muse LEARN it on its own — the chat PRODUCER closes the idle self-learning loop end-to-end (B1 Slice 1). At REPL exit, `enqueueSessionCorrections` detects this session's corrections (the existing precision-first `detectCorrections`, EN+KO) and ENQUEUES each onto the learn-queue for the idle daemon to distill later behind the brakes — instead of the synchronous exit-time distill. Gated by `MUSE_IDLE_LEARNING_ENABLED`, MUTUALLY EXCLUSIVE with `MUSE_PLAYBOOK_DISTILL_ENABLED` (a correction is never both distilled at exit AND enqueued — no double-distill); fail-soft (a history-read error enqueues nothing, never blocks exit). Reuses the same boundary→turns resolution as the exit-distill — `apps/cli/src/chat-enqueue-corrections.test.ts` (a detected correction → 1 queued event carrying correction+priorAnswer+userId+id; a no-correction session → 0; a history-read error → 0 + reason). LIVE qwen3:8b, the FULL CHAIN end-to-end (isolated HOME — never real `~/.muse`): SESSION 1 a corrected session ("no, that's not what I meant — give me bullet points, not prose") → producer enqueued 1 → IDLE `distillQueuedCorrections` distilled 1 on the local model → SESSION 2 the playbook holds "when asked for a summary, present key points in bullet form rather than detailed prose" with NO manual step between, queue drained to 0. cli 1608 / api 809 tests pass + `pnpm lint` 0/0. This is effectively the B1 2-session proof, live (the rung-4 proof shape; not cited-answer+refusal). HONEST scope: the felt LOOP works (correction → idle distill → learned next session); remaining polish = PROBATION (record-but-don't-inject for the unattended write — the self-confirmation safety gate) and surfacing idle-distilled strategies in `muse learned` — P36-7 bullet
- [Autonomy/Self-learning] A strategy Muse learns UNATTENDED (idle distillation) is now held on PROBATION — recorded + visible but NEVER applied to answers until a real reinforce graduates it, so Muse can't start acting on a guess it made about the user without evidence (the self-confirmation safety gate, B1 §5 / ExpeL evidence-gated promotion). `PlaybookStrategy`/`PlaybookEntry` gain a `probation?: boolean`; `rankPlaybookStrategies` EXCLUDES probation entries from injection (alongside the avoided filter), so a probation strategy is in the bank + shows in `muse playbook`/`muse learned` but is never injected into a `muse ask`/runtime turn; `adjustPlaybookReward` GRADUATES it (clears probation) once its reward goes net-positive — a real reinforce. The idle distill-consumer (P36-6) now records every idle-distilled strategy with `probation: true`, while the WATCHED exit-distill path is unchanged (a user-present correction graduates immediately) — `packages/agent-core/test/playbook.test.ts` (a relevant probation strategy is NOT injected; a graduated probation:false one IS) + `packages/mcp/test/playbook-store.test.ts` (probation persists; a +1 reinforce graduates probation:false; a decay keeps it) + `apps/api/src/distill-queue.test.ts` (idle-distilled strategy recorded probation:true). LIVE qwen3:8b, the full gate (isolated HOME — never real `~/.muse`): a queued correction → idle distill → "when asked for a summary, present it in bullet points…" recorded probation:true → `rankPlaybookStrategies` injected 0 (held back) → after a +1 reinforce, probation cleared → injected 1 (graduated). cited-answer+refusal UNAFFECTED (a live `muse ask` still returns the cited "MTU 1380" + 📎 receipt and the honest sister's-birthday refusal — no recall regression from the shared-core change). agent-core 1234 / mcp 1218 / api 815 tests pass + `pnpm lint` 0/0. This makes idle self-learning SAFE to enable (the unattended write can't silently steer the agent). HONEST scope: remaining polish = surfacing the probation flag in `muse learned`'s rendered digest — P36-8 bullet
- [Presence/Felt] `muse learned` now SHOWS what Muse learned while idle — idle-distilled strategies appear under "Learning while idle (on probation — recorded, NOT yet applied until you reinforce it)", so the user can SEE the unattended learning + that it's held back, and decide to reinforce it (the legibility precondition to trusting idle self-learning; B1 §5 "Visible" + Hermes's visible what-I-learned). `renderLearnedDigest` gains a probation section and EXCLUDES probation strategies from the trusted/avoided lists (never double-listed); the probation section alone is enough to render (not the empty hint). A graduated strategy (probation cleared by a reinforce, P36-8) moves to "Trusted" — `apps/cli/src/commands-learned.test.ts` (a probation strategy renders under "on probation" with ⟨probation⟩, NOT as trusted; a graduated/trusted one still shows under Trusted; the probation section alone avoids the empty hint). LIVE qwen3:8b, the full visible chain (isolated HOME — never real `~/.muse`): a queued correction → idle `distillQueuedCorrections` (1) → `muse learned --user u1` (built CLI) prints "Learning while idle (on probation …): • when handling date rescheduling, assume ambiguous dates refer to the next business day ⟨probation⟩". cited-answer+refusal UNAFFECTED (a live `muse ask` still returns the cited "MTU 1380" + 📎 receipt and the honest sister's-birthday refusal — the change is cli-digest-only, no recall path touched). cli 1610 tests pass + `pnpm lint` 0/0. With this, the idle self-learning loop is now FELT + VISIBLE end-to-end: correction → idle distill (behind the brakes) → probation → seen in `muse learned` → reinforce to graduate — P36-9 bullet
- [Presence/Felt] A user now FEELS Muse grew between sessions — on starting a continuing chat, the opener leads with "💡 I learned N thing(s) while you were away (on probation) — review with `muse learned`." whenever the idle daemon distilled corrections since you were last here (the locked headline's explicit "you FEEL it next session" beat; B2 "felt framing is deterministic code, no second model call"). `formatIdleLearnedNotice(count)` (pure: count>0 → the beat, singular/plural; 0 → undefined so the opener stays silent) + `idleLearnedNoticeForUser(userId, env)` (reads the playbook fail-soft, counts probation strategies); `runChatInk` PREPENDS it to the launch recap on a continuing session, fail-soft (a read error → silent, never blocks the REPL). Honest by construction: it counts REAL probation entries, never fabricates — `apps/cli/src/commands-learned.test.ts` (notice undefined at 0; singular "1 thing"/plural "3 things" + points at `muse learned`; `idleLearnedNoticeForUser` → undefined on an empty playbook, "1 thing" with one probation + one graduated entry — only probation counts). LIVE qwen3:8b, the full felt chain (isolated HOME — never real `~/.muse`): a queued correction → idle `distillQueuedCorrections` → `idleLearnedNoticeForUser("u1")` returns "💡 I learned 1 thing while you were away (on probation) — review with `muse learned`." — exactly what the chat opener prepends. cited-answer+refusal UNAFFECTED (a live `muse ask` still returns the cited "MTU 1380" + the honest refusal — chat-startup/digest-only change, no recall path touched). cli 1613 tests pass + `pnpm lint` 0/0. With this the grows-with-you loop is FELT at the moment of return: correction → idle distill (behind the brakes) → probation → greeted next session → reviewed in `muse learned` → reinforce to graduate. HONEST scope: shown whenever probation strategies exist (no once-per-day dedup yet — a small polish); the chat-REPL render is a verified one-line recap prepend (the notice string itself is unit+live proven) — P36-10 bullet
- [Perception] A user can now point Muse at their EXPORTED calendar — a local `.ics` file — and `muse ask` grounds on and CITES its events, with zero cloud (B3 ② calendar read, local-ICS-first; diversifies off the P36 self-learning run). New read-only `LocalIcsCalendarProvider` (`packages/calendar`) reads a local iCalendar file and exposes its VEVENTs as CalendarEvents; `parseIcsCalendar` splits a VCALENDAR into VEVENT blocks and reuses the proven CalDAV `parseVEvent` (UID/SUMMARY/DTSTART/DTEND, all-day, TZID), tolerant (a malformed VEVENT skipped). It's a pure READ connector — `describe().local === true` (so the local-only registry filter keeps it IN and egressing providers OUT) and every mutator REJECTS ("read-only — edit the source file"); a missing/unreadable file ⇒ no events (fail-soft). Wired into `buildCalendarRegistry` as the `ics` provider (`MUSE_CALENDAR_PROVIDERS=ics`, file via `MUSE_CALENDAR_ICS_FILE`, default `~/.muse/calendar.ics`), so the agent's existing event-grounding (`muse ask` upcomingEvents + the `[event: …]` citation gate) picks it up unchanged — `packages/calendar/test/local-ics-provider.test.ts` (parseIcsCalendar: timed + all-day VEVENTs sorted with fields, malformed skipped, junk → []; provider: local:true, listEvents range-filters, missing file → [], all three mutators reject). LIVE qwen3:8b on a generated mock `.ics` (isolated HOME, empty notes — never real `~/.muse`): `MUSE_CALENDAR_PROVIDERS=ics muse ask "What's my investor sync with Foundry?"` → "grounded on 1 upcoming event" → "investor sync with Foundry … June 2nd … via Zoom [event: Investor sync with Foundry @ Zoom]" (cited from the .ics); "What time is my flight to Tokyo?" → "I don't have any information about your flight" (honest refusal, no fabrication despite the calendar holding a different event). calendar 122 / autoconfigure 464 tests pass + `pnpm lint` 0/0 — first perception-axis slice: read a real exported calendar locally, cite it, refuse what's not there — P37-1 bullet
- [Perception/Edge] The ambient reader no longer pumps clipboard/selection SECRETS into the model context — the B3 GATE-FIRST secret-skip on the creepiest live perception surface. `AmbientSnapshotProvider` injects frontmost app/window + selected text + clipboard + notifications when enabled, and the CONTENT fields routinely carry secrets (a copied `.env` line, an API key, a DB URI), which were injected verbatim (truncated + prompt-injection-sanitized, but NOT secret-redacted). `renderAmbientContextSection` now runs `redactSecretsInText` over the secret-bearing fields (selected/clipboard/notifications) BEFORE injection, so a key never reaches the model context; app/window (UI titles, low-risk) pass through unredacted. Local-by-construction means this stayed on-box even before — but a confidant must not even surface your clipboard secrets to its own model — `packages/agent-core/test/ambient-context.test.ts` (a clipboard `OPENAI_API_KEY=sk-proj-…` + a `postgres://user:hunter2@…` selection are redacted to `[redacted-openai-key]` / creds stripped; the `app: Terminal` title passes through; ordinary content untouched). LIVE: `renderAmbientContextSection({clipboard:"OPENAI_API_KEY=sk-proj-…"})` → "clipboard: OPENAI_API_KEY=[redacted-openai-key]". cited-answer+refusal UNAFFECTED (a live `muse ask` still returns the cited "MTU 1380" + the honest refusal — shared-core change, ambient is default-OFF + doesn't touch recall). agent-core 1236 tests pass + `pnpm lint` 0/0. Verification follows the safety-proof shape (secret never injected), NOT cited-answer+refusal (ambient-injection path, not recall). HONEST scope: the secret-skip half of B3 gate-first; the per-source consent (default-OFF `ambient_clipboard`/`ambient_selection` flags, vs today's single `enabled`) is the remaining half — P37-2 bullet
- [Perception] A user can now ask about RECURRING calendar events from their local `.ics` — "when's my weekly standup?" — and `muse ask` cites the next instance, even though the series' base DTSTART is in the past (extends P37-1). Real calendars are dominated by recurring meetings; the base VEVENT (DTSTART weeks ago) is filtered out of `muse ask`'s now→+7d window, so without expansion a recurring event NEVER surfaced. `parseVEvent` now captures the RRULE (`recurrence` on CalendarEvent); `expandRecurringEvent(event, from, to)` expands the dominant FREQ=DAILY/WEEKLY forms (+INTERVAL/COUNT/UNTIL) into the instances inside the range (capped at 200; an unsupported RRULE — e.g. MONTHLY — surfaces the base event, never fabricated instances), and `LocalIcsCalendarProvider.listEvents` flat-maps it before range-filtering — `packages/calendar/test/local-ics-provider.test.ts` (WEEKLY anchored in the past surfaces its in-window Monday; DAILY fills the window; COUNT=2 / UNTIL-in-past → 0 in a future window; non-recurring passthrough; unsupported MONTHLY → base only; provider expands a recurring .ics). LIVE qwen3:8b on a mock `.ics` with `RRULE:FREQ=DAILY` (DTSTART 2026-05-01, isolated HOME — never real `~/.muse`): "When is my engineering daily standup?" → "grounded on 7 upcoming event(s)" → "Your next engineering daily standup is scheduled for 2026-06-01 at 09:00 AM [event: Engineering daily standup]" (cited from the expanded RRULE); "When is my dentist appointment?" → honest "You don't have any scheduled dentist appointments" (no fabrication). calendar 127 tests pass + `pnpm lint` 0/0 — recurring events now surface + cite on the headline path. HONEST scope: FREQ=DAILY/WEEKLY (+INTERVAL/COUNT/UNTIL) only — MONTHLY/YEARLY and BYDAY-list expansion are unsupported (base event surfaced, never wrong instances) — P37-3 bullet
- [Perception] The local `.ics` calendar now works ZERO-CONFIG — a user just drops their exported calendar at `~/.muse/calendar.ics` and `muse ask` grounds + cites it, with no env to set (makes P37-1's connector actually reachable, like the notes corpus "just works"). `buildCalendarRegistry` auto-adds the `ics` provider when `resolveCalendarIcsFile(env)` exists on disk (and isn't already requested), so `MUSE_CALENDAR_PROVIDERS=ics` is no longer required; safe to auto-enable because the provider is read-only + local — `packages/autoconfigure/test/build-calendar-registry.test.ts` (auto-registers `ics` when calendar.ics exists; does NOT when absent; no duplicate when explicitly requested too). LIVE qwen3:8b (isolated HOME, empty notes, NO `MUSE_CALENDAR_PROVIDERS` — never real `~/.muse`): with only `~/.muse/calendar.ics` present, "When is my quarterly board review?" → "grounded on 1 upcoming event(s)" → "Your quarterly board review is scheduled for June 3rd at 14:00 [event: Quarterly board review]" (cited zero-config); "When is my doctor appointment?" → honest "You don't have any scheduled doctor appointments" (no fabrication). autoconfigure 467 tests pass + `pnpm lint` 0/0 — the exported-calendar reader is now discoverable by drop-the-file, the beachhead's lowest-friction path — P37-4 bullet
- [Self-learning] Learned strategies now FADE when you stop reinforcing them (B1 Slice 2, continuous RL disuse-decay) — a one-off thumbs-up no longer steers Muse forever. A positive-reward strategy left unreinforced past 30 days loses reward toward NEUTRAL 0 on the idle daemon (clamped at 0 — disuse fades trust, never punishes; only a real correction drives a strategy negative), so it sinks out of the injected `[Learned Strategies]` block on its own; `muse learned` shows "↓ fading (last reinforced Nd ago)" a few days BEFORE the reward drops so the user SEES trust ebbing. `adjustPlaybookReward` stamps `lastReinforcedAt` on a positive reinforce only (a penalty/decay never refreshes it); new `decayStalePlaybookRewards` (mcp) runs as an idle RL phase in `consolidate-tick` behind ALL the brakes (cheap + local, no LLM), wired in `tick-daemons`. Proof: unit tests in `packages/mcp/test/playbook-store.test.ts` (decays one step toward 0; clamps at neutral; fresh/neutral/negative/probation untouched; createdAt fallback; lastReinforcedAt stamped on reinforce not penalty), `apps/api/src/consolidate-tick.test.ts` (decay phase fires only when idle/unbraked), `apps/cli/src/commands-learned.test.ts` (↓ fading render past the window) + a LIVE end-to-end run on this box (HOME-isolated mock playbook, never real ~/.muse): a stale +2 strategy decayed +2→+1→0 across idle ticks while a fresh +3 stayed, a 2nd tick at 0 decayed nothing (clamp proven), and the faded one LEFT `muse learned`'s Trusted list. mcp 1222 / api 816 / cli 1615 tests + `pnpm lint` 0/0 — the grows-with-you loop now self-corrects in BOTH directions (learns AND forgets stale trust) — P36-11 bullet
- [Self-learning] A reinforced strategy is no longer forgotten just for being old (B1 Slice 3 — reward-/recency-weighted eviction). When the learned-strategy playbook overflows its 100-entry cap, eviction was blind FIFO (drop the oldest regardless of value), so a strategy you reinforced repeatedly could be evicted while a never-used newer one survived. New `retainPlaybookEntries` (mcp) keeps the highest-value entries instead — value = reward, then recency: a high-reward OLD strategy beats a low-reward NEW one, reward ties break toward the newer, negative/avoided entries are evicted first, and survivors keep their original insertion order (the recency proxy `rankPlaybookStrategies` relies on). `recordPlaybookStrategy` now caps via this helper instead of `.slice(-MAX)`. Proof: unit tests in `packages/mcp/test/playbook-store.test.ts` (at/under cap returned unchanged; high-reward-old kept over low-reward-new with order preserved; reward-tie→recency; avoided evicted before neutral/positive; record-path overflow of 120 neutral fillers keeps a +5 champion) + a LIVE end-to-end run on this box (HOME-isolated mock playbook, never real ~/.muse): a `+5` champion recorded FIRST then buried under 120 newer neutral records survived the cap and `muse playbook list` shows `[champion] ⟨reward +5⟩` still present, total capped at 100 (blind FIFO would have dropped it as oldest). mcp 1227 tests + `pnpm lint` 0/0 — the bank now keeps what you've proven matters, not merely what's newest — P36-12 bullet
- [Self-learning] `muse learned` now shows the WHY behind each learned strategy (B1 Slice 4 — provenance + legibility). A strategy used to be just a sentence; now each carries its ORIGIN (`grounded` = distilled from a real correction, `reflected` = synthetic, `manual` = you typed it) and the `source` correction that taught it, so `muse learned` prints "↳ learned from your correction: '<the exact thing you said>'" under both trusted and probation strategies — the legibility precondition to trusting unattended learning (you can judge whether to keep what Muse learned). PlaybookEntry/PlaybookStrategy gain `origin`/`source`; both correction-distill writers (idle daemon `distill-queue.ts` + chat-exit `chat-distill-corrections.ts`) stamp `origin: "grounded"` + the correction; `renderLearnedDigest` renders the why line; and a small ranking tie-break (`REFLECTED_RANK_PENALTY`, far below a reward step or relevance point) makes a `reflected` strategy never outrank an otherwise-equal `grounded` one — evidence beats synthesis at a dead heat. Proof: unit tests in `packages/mcp/test/playbook-store.test.ts` (origin/source round-trip + non-string-drops), `packages/agent-core/test/playbook.test.ts` (grounded outranks equal reflected; penalty is tie-break-only, a more-relevant reflected still wins), `apps/cli/src/commands-learned.test.ts` (why line for grounded incl. probation; reflected flagged synthetic; long source truncated; legacy/manual gets no line) + a LIVE full chain on qwen3:8b (HOME-isolated, never real ~/.muse): enqueue "no, that is not what I meant — give me bullet points, not prose" → idle `distillQueuedCorrections` → `muse learned` printed the probation strategy WITH "↳ learned from your correction: '<that correction>'". agent-core 1238 / mcp 1229 / api 817 / cli 1618 tests + `pnpm lint` 0/0 — the user can now SEE why Muse believes each learned thing — P36-13 bullet
- [Self-learning] `muse playbook undo <id>` now makes Muse forget a learned strategy AND not re-learn it (B1 Slice 5 — undo that TEACHES; `--pause` deferred). Plain `remove` only deletes a strategy, so the idle distiller re-learns the same lesson the next time the user gives a similar correction. `undo` removes the strategy AND records a suppressed-lesson veto keyed on its SOURCE correction (the provenance captured by P36-13), so the idle daemon distiller skips that signal BEFORE the LLM call and bumps the veto's blocked counter. Keying on the stable correction signal (not the LLM's run-to-run paraphrase of the strategy) is what makes the suppression actually stick — a flaw the live test caught and drove the redesign. New `suppressed-lessons.json` store (`packages/mcp/src/suppressed-lessons-store.ts`: record/read/query/incrementBlocked, atomic, capped) + `resolveSuppressedLessonsFile` (autoconfigure); `distill-queue.ts` (api) consults it via `strategyTextSimilarity` on the source; `muse playbook undo` (cli) wires it. Proof: unit tests in `packages/mcp/test/suppressed-lessons-store.test.ts` (source round-trip, cap, blocked-counter), `apps/api/src/distill-queue.test.ts` (skips a matching correction before distilling + bumps counter; a different correction still distills; no-source can't block; back-compat without the file), `apps/cli/src/commands-playbook.test.ts` (undo removes + records the veto with source) + a LIVE full chain on qwen3:8b (HOME-isolated, never real ~/.muse): correction → idle distill → `muse playbook undo` → the SAME correction re-enqueued distilled 0 (veto blockedCount 1) while a DIFFERENT correction still learned. mcp 1234 / api 823 / cli 1619 tests + `pnpm lint` 0/0 — the user is now in control of what Muse keeps learning — P36-14 bullet
- [Self-learning] `muse playbook pause` now stops ALL background self-learning with one command (B1 Slice 5 complete — the kill-switch half). A persisted pause flag (not an env var, so a daemon already running honors it without a restart): when paused, the idle distiller writes ZERO strategies AND the session producer enqueues ZERO corrections — a TRUE pause that doesn't accumulate-to-learn-later; any queue already present is left intact so a later `muse playbook resume` catches up. `muse learned` shows a "⏸ Background learning is PAUSED" banner so the state is legible. New `packages/mcp/src/learning-pause-store.ts` (isLearningPaused/readLearningPauseState/setLearningPaused; fail-OPEN on a corrupt file so a bad pause file can't silently wedge learning off forever) + `resolveLearningPauseFile` (autoconfigure); the consumer (`apps/api/src/distill-queue.ts`, also wired through `tick-daemons.ts`) and the producer (`apps/cli/src/chat-enqueue-corrections.ts`) both honor it. Proof: unit tests in `packages/mcp/test/learning-pause-store.test.ts` (round-trip paused+since / resume / fail-open on corrupt / non-true value), `apps/api/src/distill-queue.test.ts` (PAUSED ⇒ 0 writes + queue intact + resume catches up), `apps/cli/src/commands-playbook.test.ts` (pause persists / resume clears), `apps/cli/src/commands-learned.test.ts` (PAUSED banner incl. empty corpus) + a LIVE chain on qwen3:8b (HOME-isolated, never real ~/.muse): `muse playbook pause` → enqueue + idle distill = 0 distilled with queue pending 1, `muse learned` shows the ⏸ banner → `muse playbook resume` → distill = 1, queue drained. mcp 1238 / api 824 / cli 1621 tests + `pnpm lint` 0/0 — Slice 5 is now complete (undo + pause); the user can fully stop and steer what Muse learns — P36-15 bullet
- [Self-learning] `muse doctor` now reports whether background self-learning is actually running (B1 Slice 7 — verifiable autonomy). Background learning used to be invisible — a user couldn't tell if it was on, paused, or surviving reboots. `muse doctor` now includes a `self-learning` check resolving the real state: OFF (default, with how to enable) / ON-but-daemon-not-installed (warn → `muse daemon --install`) / ON+installed ("will run while idle") / PAUSED (warn → `muse playbook resume`). The pure `selfLearningCheck({enabled,paused,installed})` helper computes it from `MUSE_IDLE_LEARNING_ENABLED`, the persisted pause flag (`isLearningPaused`), and whether the LaunchAgent plist exists (`resolveLaunchAgentFile`, now exported). The LaunchAgent plist also gains `ProcessType=Background` so macOS throttles the resident daemon's CPU/IO under contention — the OS-level complement to the brake-first idle gates (StartInterval intentionally omitted: it conflicts with the KeepAlive-resident model). Proof: unit tests in `apps/cli/src/commands-doctor.test.ts` (selfLearningCheck all four states) + `apps/cli/src/commands-daemon.test.ts` (plist contains ProcessType/Background, still plutil-valid) + a LIVE `muse doctor --local` (HOME-isolated, never real ~/.muse) showing each state's exact line: OFF default ✓, ON-not-installed → `muse daemon --install`, ON+installed "will run while idle", paused → `muse playbook resume`. cli 1625 tests + `pnpm lint` 0/0 — the user can now VERIFY whether Muse is set up to learn while idle — P36-16 bullet
- [Perception] `muse ask` now grounds on and cites your CONTACTS (B3 perception — your address book). "What's Sarah's email?" / "how do I reach the plumber?" were questions the local model could only answer from the user's own contacts, which `muse ask` didn't read. It now pulls MATCHING contacts (query-token overlap on name/aliases/email/handle via `contactMatchScore` — never dumps the whole address book at the small model), injects them as a grounding block alongside notes/tasks/events/reminders, and cites each as `[contact: name]` under the same code-not-model citation gate: a new `contacts` class in `enforceAnswerCitations` (agent-core) strips any `[contact: …]` not in the matched set, so a fabricated person can't be cited. `--no-contacts` opts out. Proof: unit tests in `apps/cli/src/commands-ask-contacts.test.ts` (contactMatchScore matches first-name/alias/handle, scores 0 for unrelated/empty queries so an unknown person is NOT injected, full-name > partial) + `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (gate keeps a real contact, strips an unknown one by overlap) + a LIVE `muse ask` on qwen3:8b against a MOCK contacts.json (HOME-isolated, empty notes, never real ~/.muse): "What is Sarah's email address?" → "Sarah's email address is sarah.chen@foundry.io [contact: Sarah Chen]" (cited, zero fabrication); "What is my dentist Dr. Patel's phone number?" → honest "I don't have access to ... specific details about your dentist, Dr. Patel" (no fabricated number, no spurious citation). agent-core 1239 / cli 1630 tests + `pnpm lint` 0/0 — a user can now ask Muse about their PEOPLE and get a cited answer or an honest refusal — P37-5 bullet
- [Felt] `muse ask` now surfaces a grounded "I learned this about you" beat when it applies a preference you taught it (B2 S6). When the chat-only path injects a learned playbook strategy that is genuinely RELEVANT to the question (token overlap with the query, so a recency-floor pick never triggers a false claim) and the answer is NOT a refusal, it closes with a deterministic beat: `💡 Applied a preference you taught me: "<strategy>". (Not right? muse playbook undo.)` — so the user FEELS Muse growing with them, wired straight to the P36-14 undo reversal and converging with `muse learned`. Honesty-safe per the B2 guardrails: no second model call (a fixed template over the strategy ALREADY injected into the prompt), grounded in the user's own taught preference (the cited source), suppressed on a refusal (which applied nothing) and whenever the top strategy doesn't overlap the question. New `topAppliedStrategy` helper returns the head of the injected `[Learned Strategies]` block (same ranking + avoided/probation exclusions as `selectPlaybookSection`). Proof: unit tests in `apps/cli/src/commands-ask-playbook.test.ts` (topAppliedStrategy returns the top injectable for a relevant Q, matches the head of the rendered block, undefined for empty/probation/avoided banks) + a LIVE `muse ask` on qwen3:8b (mock corpus + a seeded WireGuard style preference, HOME-isolated, never real ~/.muse): "What MTU for the WireGuard VPN?" → cited "Set the MTU to 1380 ... [from 2026-03-03-vpn-wireguard.md]" AND the beat naming the preference; "When is my sister's birthday?" → honest refusal with NO beat. cli 1633 tests + `pnpm lint` 0/0 — a user can now SEE when Muse applied something they taught it (and undo it in one command) — P35-5 bullet
- [Felt] `muse today` now surfaces "what you've been focused on" — a grounded "Muse noticed" beat (B2 S7, pull surface). It names the note FAMILY the user has been editing most this week — "🔭 You've been focused on <family> lately — N notes edited in the last week." The ONLY signal is note *mtime* (writes the user actually made), never opens/reads, so the wording is honest ("edited", never "looked at"); a quiet week yields NO line (honest silence, no fabricated "you've been busy"). Built as a PULL surface (in `muse today`, which the user explicitly invokes) so it needs no proactive-interrupt budget — the clean tracer of the heavier push-notice S7. New pure helpers `selectNoteFocus`/`formatNoteFocusSection` in `apps/cli/src/note-focus.ts`; `muse today` gathers note mtimes via the existing recursive notes walk (fail-soft, `--json`-skipped) and composes the line alongside the existing revisit/stale-task/connection sections. Proof: unit tests in `apps/cli/src/note-focus.test.ts` (most-edited family wins ≥minEdits; quiet week → undefined; out-of-window/future/NaN mtimes ignored; count-tie breaks to most-recent; root-level notes group as "your notes"; honest "edited" wording with no "looked at"/"opened"/"read") + a LIVE `muse today --local` (HOME-isolated, GENERATED cluster under a temp dir, never real ~/.muse): 4 recent edits in projects/ → "🔭 You've been focused on projects lately — 4 notes edited in the last week"; a lone note → silent. cli 1641 tests + `pnpm lint` 0/0 — a user opening `muse today` now sees what they've been working on, grounded in real edits — P35-6 bullet
- [Perception] `muse ask --shell` now grounds on and cites your SHELL HISTORY (B3 — "what was that command?"; OPT-IN + secret-redacted). A question only the user's own history can answer. OPT-IN (default OFF, because history is sensitive — only with `--shell`), LOCAL + read-only, and every injected command is `redactSecretsInText`-scrubbed before it reaches the model (history holds `export TOKEN=…` lines). Commands are matched by query-token overlap (newest-first on ties, de-duplicated) via `selectShellCommands`, parsed by `parseShellHistory` (zsh-extended `: ts:dur;cmd` + plain + backslash-continuation), and cited as `[command: …]` under a new `commands` class in `enforceAnswerCitations` that strips any invented command. Source resolves from `$MUSE_SHELL_HISTORY_FILE` / `$HISTFILE` / `~/.zsh_history`. Proof: unit tests in `apps/cli/src/shell-history.test.ts` (parse extended/plain/continuation; overlap match, empty-query→[], dedup, cap) + `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (gate keeps a real command, strips an invented one by overlap) + a LIVE `muse ask --shell` on qwen3:8b (mock history, HOME-isolated, empty notes, never real ~/.muse): "What was my docker command to run nginx?" → "grounded on 2 shell command(s)" → cited "`docker run -p 8080:80 --name web nginx:latest` [command: docker run -p 8080:80 --name web nginx:latest]"; "kubectl scale command?" → honest "I don't have access to your shell command history ... for kubectl"; the OPENAI_API_KEY export line → grounding REDACTED to `export OPENAI_API_KEY=[redacted-openai-key]` (the real `sk-proj-…` secret NEVER appeared in output); without `--shell` → 0 shell-grounded lines (opt-in respected). agent-core 1240 / cli 1647 tests + `pnpm lint` 0/0 — a user can now opt in to ask Muse "what was that command?" and get a cited answer with secrets stripped, or an honest refusal — P37-6 bullet
- [Perception] `muse ask --file <path>` now grounds an answer on a specific file WITHOUT ingesting it into the notes corpus (B3 — ad-hoc file grounding). "What's the monthly rent in this lease?" / "what does this config do?" — read once, never indexed. Reuses the NOTES citation class so no new gate class: the file's passages are split (`chunkText`) and lexically ranked against the question (strongest kept up to a ~6000-char budget so a large file can't blow qwen3:8b's context), injected as note-class context, and cited `[from <path>]` under the same code gate — the cite-as token and the gate's `allowedNotes` normalise the path identically, so a real file citation survives and an invented one is stripped. Works even with an empty / unavailable notes index (sets notesUnavailable=false once file passages are injected). New exported `selectFilePassages` helper. Proof: unit tests in `apps/cli/src/commands-ask-file.test.ts` (small file → every passage in original order; big file → the query-relevant passage makes the cut + char budget respected; empty/whitespace file → no passages) + a LIVE `muse ask --file` on qwen3:8b (mock lease file, HOME-isolated, empty notes, never real ~/.muse): "How much is the monthly rent and when is it due?" → "The monthly rent is $4,200, and it is due on the 1st of each month [from ../lease-agreement.md]" (cited); "What is the landlord's phone number?" → honest "I don't have access to the landlord's phone number" (no fabrication). cli 1650 tests + `pnpm lint` 0/0. (The refusal's trailing `cite as:` parrot is the pre-documented chat-only streaming limitation — the gate strips it on buffered paths.) A user can now ask Muse about ANY file on the fly, cited, without growing their corpus — P37-7 bullet
- [Felt] `muse ask` now shows a followable "shows its work" receipt for the NON-note sources it cites (B2 S1 completion). The felt source receipt (P35-1: "📎 From your notes … from your note of <date> — '<quote>'") covered only NOTE citations; a cited calendar event / task / reminder / contact / shell command got the inline `[event: …]` marker but no followable receipt. New `formatNonNoteReceipts` (cli) parses the POST-gate answer's `[event|task|reminder|contact|command: …]` markers and renders a grouped "📎 Also grounded on:" block — 📅 calendar / ✅ tasks / ⏰ reminders / 👤 contacts / ⌨️ shell — so EVERY cited source is followable, not just notes. Deterministic (no second model call — fixed templates over the already-grounded structured data, per the B2 guardrail), skips a source type that wasn't grounded this turn, and renders nothing on a refusal (citations already stripped). Proof: unit tests in `apps/cli/src/commands-ask-receipts-nonnote.test.ts` (one grouped line per cited non-note source; shell-command receipt; skip-when-source-unconfigured; none on a refusal; de-dup a doubly-cited source) + a LIVE `muse ask` on qwen3:8b (mock contacts.json, HOME-isolated, empty notes, never real ~/.muse): "What is Sarah's email address?" → cited "[contact: Sarah Chen]" AND "📎 Also grounded on: 👤 from your contacts: Sarah Chen"; an unknown-person must-refuse → 0 "Also grounded on" lines. cli 1655 tests + `pnpm lint` 0/0 — "shows its work" is now FELT uniformly across every grounding source, not just notes — P35-7 bullet
- [Perception] `muse ask` can now answer "when is X's birthday?" — cited from your contacts (B3 coverage-gap fix). A contact's stored `birthday` (already used to drive birthday reminders) was NOT in the contacts grounding block (P37-5 injected only email/phone/handle), so a birthday question failed even though the data was present. The block now injects a readable birthday via the new `formatContactBirthday` helper (`MM-DD` / `YYYY-MM-DD` → "March 14" with the year appended when present; absent / malformed / out-of-range → omitted, never a fabricated date), cited as `[contact: name]` under the same code citation gate; the contact is matched into context by its name token as before. Proof: unit tests in `apps/cli/src/commands-ask-contacts.test.ts` (formatContactBirthday: MM-DD → "Month Day"; year-present → "Month Day, YYYY"; absent/malformed/out-of-range → undefined) + a LIVE `muse ask` on qwen3:8b (mock contacts.json with a birthday, HOME-isolated, empty notes, never real ~/.muse): "When is Sarah's birthday?" → "Sarah's birthday is March 14 [contact: Sarah Chen]" (cited, plus the P35-7 "📎 Also grounded on: 👤 from your contacts" receipt); "When is Daniel's birthday?" → honest "I don't have any information about Daniel's birthday" (and the citation gate stripped a spurious `[feed: …]` the model tried to append — defense confirmed). cli 1658 tests + `pnpm lint` 0/0 — a user can now ask Muse when someone's birthday is and get it cited from their own contacts — P37-8 bullet
- [Perception/Transparency] `muse ask` now grounds on Muse's OWN action log — "did you send that? / what have you done on my behalf?" answered (and cited) from the real audit trail (B3, gate a new surface). The action log records every outbound act Muse takes (email/message sends, refusals — written by the email/messaging actuators); it was never a `muse ask` grounding source, so the user couldn't ask what Muse had done. Now matching entries are pulled (query-token overlap on each entry's `what` via `selectGroundingActions`, newest-first, capped), injected with their result + detail, and cited as `[action: …]` under a new `actions` class in `enforceAnswerCitations` (which strips any invented action), with a "🤖 from your action log" P35-7 receipt. Default-on (`--no-actions` opts out) — it's the user's own local record, the READ side of the ACT actuators. Proof: unit tests in `apps/cli/src/commands-ask-actions.test.ts` (selectGroundingActions: overlap match newest-first, empty-query→[], cap) + `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (gate keeps a real logged action, strips an invented one by overlap) + a LIVE `muse ask` on qwen3:8b (mock action-log.json, HOME-isolated, empty notes, never real ~/.muse): "Did you email Sarah about the Q3 budget?" → "Yes, I emailed Sarah about the Q3 budget review — performed (sent) [action: email to sarah@foundry.io: Q3 budget review — performed (sent)]" (cited, + the action-log receipt); "Did you call the bank for me?" → honest "I did not call the bank for you" (and the citation gate stripped the model's spurious [reminder: none]/[task: none]). agent-core 1241 / cli 1661 tests + `pnpm lint` 0/0 — a user can now ask Muse what it has done on their behalf and get a cited answer from the real audit log, or an honest "no" — P37-9 bullet
- [Recall/Edge] `muse ask` now omits empty grounding sections from the prompt — tighter context for the small model + fewer spurious citations (edge hardening). With ~10 grounding sources injected, every turn previously carried an empty "(no pending reminders)" / "(no matching contacts)" block for each source the user had nothing in, which both bloated qwen3:8b's context (worsening lost-in-the-middle) and invited it to parrot a spurious "[reminder: none]"-style citation (the gate strips it, but it still flashes on the streaming path). New pure `groundingSectionLines` helper includes each OPTIONAL source section only when it has content this turn; the NOTES section stays always-present (the primary surface), and the 3 citation instruction lines are unchanged. Proof: unit tests in `apps/cli/src/commands-ask-grounding-sections.test.ts` (a present section emits header/body/footer/blank; an empty one is omitted entirely; all-empty → []; order preserved across multiple present sections) + a LIVE `muse ask` on qwen3:8b (mock corpus, HOME-isolated, never real ~/.muse): "What MTU should I set for the WireGuard VPN?" → still cited "[from …2026-03-03-vpn-wireguard.md]" with the 📎 receipt (no recall regression); "When is my sister's birthday?" → honest refusal with ZERO spurious `[reminder|task|feed|event|contact|action: none]` citation (the omitted empty sections no longer trigger the parrot). cli 1664 tests + `pnpm lint` 0/0 — the grounding prompt is now tighter for the small model and the spurious-citation surface is cut at the source, not just stripped after — P37-10 bullet
- [Edge/Measurement] `muse doctor --grounding` now scores the "shows its work" edge into two numbers a user reads on their own box (P38-1). A bundled held-out corpus (12 answerable / 8 must-refuse / 7 drift) is run through the REAL recall + RGV stack (`scoreGroundingEval`, agent-core, rank/verify injected) and prints faithfulness (fabrication caught) + false-refusal (in-corpus answer wrongly refused — loop-v2's GUARD-THE-EDGE metric, previously unmeasured). The same scorer is the `verify-faithfulness-rate` battery in `eval:self-improving` as the regression gate; shipped floor 0.84 / 0.25 sits one miss below the deterministic live baseline 0.93 / 0.08 (nomic-embed-text + qwen3:8b) so a retrieval/calibration regression trips it. Proof: `packages/agent-core/src/grounding-eval.test.ts` (rate arithmetic, NaN-guarded, 6 cases) + `apps/cli/src/grounding-eval-runner.test.ts` (renderer pass/fail/flagged, 5 cases) + a LIVE `node apps/cli/scripts/verify-faithfulness-rate.mjs` and `muse doctor --grounding` (deterministic 0.93 / 0.08 across runs). agent-core 1336 / cli 1683 + `pnpm lint` 0/0 — RAGAS arXiv:2309.15217 / CRAG arXiv:2401.15884 — a user can now SEE the edge's two rates and the loop GATES on them — P38-1 bullet
- [Edge/Grounding] `muse ask` now catches the WRONG-VALUE answer the lexical rubric was blind to (P38-2). A confident, high-coverage, fully-cited answer asserting a wrong number ("MTU 9000" where the note says 1380) passed `verifyGrounding` as `grounded` (one wrong token barely dents whole-answer coverage) and the weak-band judge never fired — the deepest documented hole. `verifyGroundingWithReverify` now escalates a `grounded` answer that asserts a numeric value absent from the evidence to ONE judge pass, FAIL-OPEN (the base already cleared every deterministic criterion, so only an explicit unsupported verdict demotes it); the recall wedge (`groundingVerdictNotice`) inherits it with no caller change. The `--grounding` corpus gained 2 wrong-value cases that WITHOUT this fix drop faithfulness to 0.80 < the 0.84 floor, so `muse doctor --grounding` / `verify-faithfulness-rate` now GUARD it. Proof: `packages/agent-core/test/knowledge-recall-reverify.test.ts` (5 escalation cases: demote-on-unsupported, uphold, fail-OPEN-on-error, no-escalation-when-supported, no-numbers) + a LIVE `node apps/cli/scripts/verify-claim-grounding.mjs` (the real qwen judge rejects "MTU 9000" at the DEFAULT threshold — the grounded path, not the forced-weak band; "MTU 1380" stays grounded). agent-core 1336 + `pnpm lint` 0/0 — Self-RAG ISSUP arXiv:2310.11511 / Chain-of-Note arXiv:2311.09210 — P38-2 bullet
- [Edge/Constructive] `muse ask --repair` makes the edge CONSTRUCTIVE — an ungrounded answer is corrected, not just warned (P38-3). When the recall wedge returns ungrounded, `--repair` runs ONE local pass that rewrites the answer constrained to the retrieved evidence and shows it as "🔧 Corrected from your notes" ONLY if the rewrite re-verifies GROUNDED through the SAME gate (citation check + rubric + the P38-2 claim-level value escalation — so a wrong value can't survive into the fix). Fail-CLOSED: a refusing / gate-failing / still-ungrounded / no-evidence rewrite leaves the honest refusal standing — a fix is never fabricated. Opt-in (one extra inference). Pure `repairToEvidence` (agent-core, rewrite/verify/gate/isRefusal injected). Proof: `packages/agent-core/test/attributed-repair.test.ts` (8 cases: grounded-rewrite-shown, still-ungrounded/weak-dropped, refusing-rewrite-not-shown, no-evidence-skip, gate-applied-before-verify, fail-on-error) + a LIVE `node apps/cli/scripts/verify-attributed-repair.mjs` on qwen3:8b ("MTU 9000" → repaired to the evidence's "MTU 1380", re-verifies grounded; an off-corpus draft over unrelated evidence is NOT repaired — the rewrite refuses, no fabricated fix). agent-core 1336 / cli 1683 + `pnpm lint` 0/0 — RARR arXiv:2210.08726 — the edge went from "warns" to "measures + catches + corrects" — P38-3 bullet
- [Edge/Recall] `muse ask` now refuses a question whose only retrieval is a flat off-corpus near-miss, instead of presenting it as confident (P38-4, adaptive confidence calibration). Near nomic's compressed-cosine floor a single absolute bar is fragile — an out-of-corpus query ("how much did I spend on groceries last month?") clipped a near-miss note at 0.563 > the 0.55 bar, so `classifyRetrievalConfidence` said `confident` and the model was invited to cite it. The gate now demotes a `confident` top that is BOTH borderline (within 0.05 of the floor) AND flat (top−runner-up < 0.08 — the query matches several notes weakly, not one strongly) to `ambiguous`, while a clearly-high top or a clear lead stays confident so genuine single-note matches are untouched. Calibrated from the live per-case margins (the lowest confident answerable sits at 0.627 with a 0.18 gap, far outside the band; only the flat near-miss flips). Drives both the recall framing and the proactivity gate. Proof: 4 margin unit tests in `packages/agent-core/test/knowledge-recall-agent.test.ts` (borderline+flat → ambiguous; borderline+clear-lead → confident; clearly-high+flat → confident; single match → confident) + the LIVE `node apps/cli/scripts/verify-faithfulness-rate.mjs`, where the groceries case is now caught and faithfulness rose 0.93 → 1.00 (15/15 unfaithful caught) with false-refusal UNCHANGED at 0.08, and `verify-cited-recall` (6) / `verify-rubric-gate` (4) / `verify-proactive-recall-gate` (4) all still green (no genuine match demoted). agent-core 1340 / cli 1683 + `pnpm lint` 0/0 — CRAG arXiv:2401.15884 — a user asking about something genuinely absent now gets an honest "I'm not sure" instead of a falsely-confident near-miss — P38-4 bullet
- [Edge/Grounding] `muse ask` now catches a WRONG-NAME answer, not just a wrong number (P38-5, claim-level value grounding extended to named entities). P38-2 escalated a `grounded` answer asserting an unsupported NUMBER ("MTU 9000" vs 1380) to the judge; a wrong NAME ("your landlord is Mr. Lee" where the note says "Mr. Park") slipped through — the same whole-answer-coverage hole, with no digit to flag. The value escalation now also flags a capitalized named entity (≥3 letters; month/day names and stopwords excluded; citations stripped first) absent from the evidence and escalates that confident, high-coverage answer to ONE judge pass — FAIL-OPEN exactly like P38-2, so a false flag only spends a judge pass that upholds a correct answer, never a false refusal. Proof: 3 new unit tests in `packages/agent-core/test/knowledge-recall-reverify.test.ts` (wrong name → demoted on an unsupported judge verdict; correct name in evidence → no escalation; a month name in a correct date answer → not escalated) + the LIVE `node apps/cli/scripts/verify-claim-grounding.mjs` (the real qwen judge rejects "Mr. Lee", upholds "Mr. Park", alongside the number cases) + `node apps/cli/scripts/verify-faithfulness-rate.mjs`, where a new wrong-named-entity drift case is now caught (faithfulness 1.00, 16/16 unfaithful caught) with false-refusal UNCHANGED at 0.08 (no answerable falsely escalated — fail-open confirmed on the real corpus). agent-core 1343 / cli 1683 + `pnpm lint` 0/0 — Self-RAG ISSUP arXiv:2310.11511 / Chain-of-Note arXiv:2311.09210 — a user asking "who is my landlord / dentist / contact?" now gets the ⚠️ unverified warning if Muse names the wrong person — P38-5 bullet
- [Felt] `muse ask "hi"` (or "thanks" / "bye") now gets ONE clean conversational line instead of the grounding machinery misfiring on a greeting (P39-1). Before: a bare social prompt produced the empty-corpus on-ramp (4 lines about adding notes), a model-fabricated `[action: greeted user]` citation the gate stripped (flashing "Removed 1 citation … treat as unverified"), AND a "⚠️ Grounding check: … treat as unverified" warning — on the word "Hello!". A new precision-first `classifyCasualPrompt` (agent-core, EN+KO, anchored + 30-char content guard so "hi, what's my rent?" or "thank you note template" never match) short-circuits a PURE social prompt to a fixed on-brand line — skipping retrieval, the on-ramp, the citation gate, the grounding-verdict warning, and even the model call (the fastest path in the CLI; tool-calling.md: don't run the retrieval machinery on a greeting). Proof: 6 classifier unit tests in `packages/agent-core/test/casual-prompt.test.ts` (greetings/thanks/farewells EN+KO; a real question opening with a social word → null; empty → null; the long-prompt guard) + 2 cli response-map guards in `apps/cli/src/commands-ask-casual.test.ts` (every kind has a reply; no `[from|action|…]` citation token can re-enter) + a LIVE `muse ask "hi"` on an isolated HOME (one clean line, zero grounding noise) while `muse ask "hi, what is my MTU?"` still flows through the grounded path (precision confirmed). agent-core 1349 / cli 1685 + `pnpm lint` 0/0 — a user greeting Muse now gets a warm instant reply, not a fabricated-citation-plus-two-warnings mess — P39-1 bullet
- [Felt/Honesty] `muse ask "what can you do?"` (or "넌 뭐야?", "how do you work?") now answers HONESTLY about Muse instead of letting the local model hallucinate an over-claimed capability list (P39-2). Before: a meta/capability question ran retrieval and the model free-composed "I can manage your schedule, set reminders, handle tasks…" — capabilities Muse does NOT autonomously have — which then got a "⚠️ treat as unverified" grounding warning: Muse lying about its OWN abilities, the same honesty failure the grounding edge forbids about recall. A new anchored `classifyMetaPrompt` (agent-core, EN+KO; precision-first so "what can you do about my taxes?" / "who are the attendees in the Q3 meeting" / "how do you make sourdough" never match) short-circuits a self-referential question to a fixed, ACCURATE `META_RESPONSE` — cited recall from your notes, honest "I'm not sure", everything local, and how to add notes / try `muse demo` — with no model freelancing, no retrieval, no grounding warning. Proof: 2 classifier unit tests in `packages/agent-core/test/casual-prompt.test.ts` (capability/identity/usage EN+KO → true; a notes question merely containing a meta word → false) + a cli guard in `apps/cli/src/commands-ask-casual.test.ts` (META_RESPONSE names the real value prop — notes / local / "not sure" — and never says "manage your schedule") + a LIVE `muse ask "what can you do?"` and `"넌 뭐야?"` (accurate line, zero grounding noise) while `"what can you do about my taxes?"` still flows through the grounded path (precision confirmed). agent-core 1351 / cli 1686 + `pnpm lint` 0/0 — a user asking what Muse can do now gets the honest truth, not an over-claim it would then have to walk back — P39-2 bullet
- [Edge/Recall fix] `muse ask` no longer slaps a false "⚠️ treat as unverified" warning on a CORRECT, well-cited answer when notes live on disk (P38-6, GUARD-THE-EDGE). A real note resolves to an ABSOLUTE path (`~/.muse/notes/q3.md`), but the model is shown — and cites — the relative name (`[from q3.md]`). The citation gate relativized its allow-list so the citation survived; but the grounding VERDICT (`groundingVerdictNotice`) validated the answer against the RAW absolute path, so `citationValidity` failed and a perfectly correct multi-fact answer ("Jin owns the deck, Mina owns pricing [from q3.md]") got "Grounding check: answer cites a source that was not retrieved — treat as unverified". A false refusal makes "honest" into "useless" — the exact regression loop-v2's GUARD-THE-EDGE clause warns about. The test corpora all pass short relative source names, so every battery missed it; it only bit REAL users with on-disk notes. Fixed with one source of truth `relativizeNoteSource(file, notesDir)` now used by the citation gate, the verdict, and the receipt so all three cite the SAME form. Proof: 3 unit tests in `apps/cli/src/commands-ask-verdict-source.test.ts` (absolute path under notesDir → relative basename; nested → relative subpath; already-relative left untouched; never returns an absolute path) + a LIVE before/after `muse ask "who owns what for the Q3 launch?"` over a real on-disk `~/.muse/notes` corpus (the spurious warning is gone, the cited answer + 📎 receipt remain) + `verify-cited-recall` (6) still green. cli 1689 + `pnpm lint` 0/0 — a user whose notes live on disk (i.e. everyone) stops seeing correct answers wrongly labelled unverified — P38-6 bullet
- [Edge/Agent fix] `muse ask --with-tools` works again on grounded queries — Muse's own prompt no longer self-trips the injection guard (P38-7). The agent path runs the injection-input-guard over the WHOLE composed prompt (the system role included), and Muse's own citation instruction — "copy an existing `cite as:` token, or a name shown in a marker" — matched the `credential_extraction` injection pattern ("token … shown" = a credential word within 15 chars of an extraction verb), so EVERY grounded `--with-tools` query died with "(error: Input guard detected injection patterns: credential_extraction)". A benign "what MTU for the office VPN?" was blocked by Muse guarding against Muse. Fixed by extracting the citation lines into `CITATION_INSTRUCTION_LINES` and wording them with "tag", never "token" — no credential word remains in the prompt, and the security pattern itself is untouched (no detection weakened). Proof: 2 dependency-free unit tests in `apps/cli/src/commands-ask-injection-guard.test.ts` (the lines carry no credential word token/secret/password/api-key EN+KO; they still instruct verbatim `cite as:` citation + "NEVER invent") + a LIVE before/after `muse ask --with-tools "what MTU for the office VPN?"` over an on-disk note (was the injection error; now "The office VPN requires an MTU of 1380 on wg0…" with a [from vpn.md] citation + 📎 receipt). cli 1691 + `pnpm lint` 0/0 — a user can use the agent (`--with-tools`) path on their own notes again. (FOLLOW-UP, deferred + security-reviewed: the guard scanning the user's OWN trusted notes for injection still false-positives on a note that legitimately mentions credentials — needs a trusted/untrusted-content split.) — P38-7 bullet
- [Felt/Front-door] `muse ask` / `muse demo` no longer leak a "cite as:" label into the answer (P39-3). The note grounding marker handed qwen3:8b a copy-ready `cite as: [from FILE]` token, and the small model often copied the WHOLE line — so a cited answer read "You set the MTU to **1380** for the WireGuard VPN. **cite as:** [from 2026-03-03-vpn-wireguard.md]", with the raw "cite as:" label showing right on `muse demo`, the very first thing a new user sees (the front door). Root fix: the marker now prints just `[from FILE]` (no "cite as:" prefix for the model to copy) and the `CITATION_INSTRUCTION_LINES` reference the `[from …]` tag instead; plus a deterministic `stripEchoedCiteAs` safety net strips any residual echoed label before the gate/receipt on the buffered paths (the chat path streams live, so a label can still flash there — the known streaming limitation). Proof: 4 unit tests in `apps/cli/src/commands-ask-cite-as.test.ts` (strips an echoed "cite as:" before a real `[from/task/contact/…]` citation; leaves a clean citation and ordinary "cite as established fact" prose untouched) + a LIVE `muse demo` (the MTU answer now reads "…the WireGuard VPN [from 2026-03-03-vpn-wireguard.md]" — label gone, the RIGHT source still cited so citation reliability held) + a LIVE `muse ask --with-tools` ("The MTU for the office VPN is 1380 [from vpn.md]" — clean). cli 1695 + `pnpm lint` 0/0 — the cited answer a new user judges Muse by now reads clean, not littered with an internal marker label — P39-3 bullet
- [Felt] `muse today` no longer warns "muse: API not reachable" at the default local-first user (P39-4). The morning briefing tries the API daemon first and, on the EXPECTED ECONNREFUSED (the default CLI user runs no daemon — local-first is Muse's identity), printed "muse: API not reachable — falling back to local briefing." on EVERY run — an error-shaped line on the working happy path that reads as broken. Now it warns ONLY when the user EXPLICITLY pointed Muse at an API endpoint (`--api-url` flag or `MUSE_API_URL` env — i.e. they expect a remote and would want to know it's down); the default CLI user silently gets the on-disk briefing, which IS the intended path. Proof: 4 unit tests in `apps/cli/src/commands-today-api-warn.test.ts` (`apiWasExplicitlyConfigured`: false for unset / empty / whitespace, true when the flag or env var carries a value, flag-precedence) + a LIVE `muse today` on an isolated HOME (0 "API not reachable" lines by default; exactly 1 when `MUSE_API_URL` is set to an unreachable host). cli 1699 + `pnpm lint` 0/0 — the daily briefing the local-first user opens every morning no longer looks broken — P39-4 bullet
- [Edge/Felt] `muse ask` now surfaces the constructive `--repair` at the moment it can help (P38-8). P38-3 shipped `muse ask --repair` (one local rewrite of an ungrounded answer constrained to the retrieved evidence, shown only if it then re-verifies grounded), but it is opt-in and a user never discovers it exists. Now, when an answer trips the grounding check (`⚠️ treat as unverified`) AND there is retrieved evidence to rewrite from — so a repair could actually succeed rather than just refuse — `muse ask` prints exactly one tip: "(Re-run with --repair and I'll rewrite this using only your notes — shown only if it then checks out.)". Pure `shouldSuggestRepair` gates it: suppressed when the verdict didn't fire (a clean cited answer), when `--repair` was already requested (it ran), under `--json` (no prose in structured output), and when there is NO retrieved evidence (repair would just refuse, so the tip would mislead). Proof: 5 unit tests in `apps/cli/src/commands-ask-repair-nudge.test.ts` + a LIVE `muse ask "what encryption cipher and key size does the office VPN use?"` over a note that states only the MTU (the answer "the provided context does not specify… [from vpn.md]" trips the verdict — answer makes claims the evidence does not support — and the --repair tip appears beneath it). cli 1704 + `pnpm lint` 0/0 — a user who hits an unverified answer now learns Muse can rewrite it cleanly from their notes — P38-8 bullet
- [Edge/Felt] The `muse ask` 📎 "open to verify" receipt now shows the SAME relative path the answer cited, not just the basename (P38-9). After P38-6 made citations relative (`[from projects/vpn.md]`), a non-dated note's receipt still labelled it by basename ("from vpn.md"), so a user with `work/a/notes.md` AND `work/b/notes.md` could not tell which "from notes.md" receipt was which (the openable full path was shown, but the human-readable label was ambiguous). `formatSourceReceipts` now prints the cited relative path as the label, matching the `[from …]` citation exactly. Proof: the `apps/cli/src/commands-ask-receipts.test.ts` "no date" case updated to assert "from tasks/finances.md" (was "from finances.md") + a LIVE `muse ask "what MTU for the office VPN?"` over a nested note (`~/.muse/notes/projects/vpn.md`): the answer cites `[from projects/vpn.md]` AND the receipt reads "from projects/vpn.md". cli 1704 + `pnpm lint` 0/0 — a user with notes in subfolders gets an unambiguous, citation-matching receipt. (This iteration also confirmed `muse chat` lacks the recall citation gate, but recorded in the README Rejected ledger that this is BY DESIGN — chat is the conversational companion; the edge's grounded surfaces are Recall / Proactivity / Reflection / Council, not chat.) — P38-9 bullet
- [Recall/Felt] `muse ask "what's in my notes?"` now lists the corpus instead of refusing (P39-5). A whole-corpus OVERVIEW request ("what's in my notes?", "summarize my notes", "list my notes", "what notes do I have") is NOT a top-K recall — every note ranks weakly for such an aggregate query, so the confidence gate refused and the warm-close told a user WHO HAS NOTES to "add a note on this and I'll have it next time" (nonsensical — they were asking about the notes they already have). A new precision-first `classifyCorpusOverview` (agent-core, EN+KO; each pattern anchors the overview verb DIRECTLY on "(my) notes" and a "notes about/on <topic>" exclusion, so "what's in my notes about the VPN?" and "summarize my VPN notes" — specific questions — do NOT match) short-circuits the request to a deterministic inventory: "You have N notes: <relative paths> — ask me anything in them and I'll quote the source." No model call, no retrieval, no fabrication. Proof: classifier unit tests in `packages/agent-core/test/casual-prompt.test.ts` (overview EN+KO → true; a specific question ending in its own topic → false) + `listNoteFiles` (recursive, sorted, ignores hidden/non-notes, capped) and `formatCorpusOverview` (count, inventory, '… and N more', singular) unit tests in `apps/cli/src/commands-ask-overview.test.ts` + a LIVE `muse ask "what's in my notes?"` over a nested corpus (lists `lease.md` + `projects/vpn.md`) while `muse ask "what is my rent?"` still recalls + cites "1,250,000 KRW [from lease.md]" (precision confirmed). agent-core 1353 / cli 1709 + `pnpm lint` 0/0 — a user can now ask what's in their corpus and SEE the inventory, not a confusing refusal — P39-5 bullet
- [Honesty/Agent] `muse ask "remind me to…"` no longer makes a FALSE PROMISE of action on the chat-only path (P39-6). Without `--with-tools` the no-tools path cannot act, yet the model happily replied "I'll remind you to call the dentist tomorrow" — claiming an action it never performed (and fabricating a `[reminder: …]` citation the gate then stripped, plus a grounding warning). Lying about acting is the same honesty failure the edge forbids about facts. A new precision-first `classifyActionRequest` (agent-core, EN; anchored on the imperative action verb at the start after an optional polite lead — "remind me", "set a reminder", "add a task", "create an event", "email Sarah", "can you remind me" — so a QUESTION about actions like "what reminders do I have?" / "when is my dentist reminder?" / "how do I set a reminder" does NOT match) now answers honestly on the chat-only path: "That's something to DO, not a question … re-run with `--with-tools` and I'll actually do it (I show the exact action and ask before any outbound send); reads stay silent, writes/sends always ask first." The `--with-tools` path is untouched — it really sets the reminder (`muse.reminders.add`, scheduled, with an ID). Proof: classifier unit tests in `packages/agent-core/test/casual-prompt.test.ts` (imperatives incl. polite leads → true; questions about reminders/actions → false) + an `ACTION_GUIDE` guard in `apps/cli/src/commands-ask-casual.test.ts` (names --with-tools + the ask-first safety, never claims it already acted) + a LIVE before/after: `muse ask "remind me to call the dentist tomorrow"` → the honest guide (no false promise); the same with `--with-tools` → "I've set a reminder…"; `muse ask "what reminders do I have?"` → still recalls (not short-circuited). agent-core 1355 / cli 1710 + `pnpm lint` 0/0 — Muse never claims to have done something it didn't — P39-6 bullet
- [Felt/Agent] A `muse ask --with-tools` ACTION confirmation now reads clean — no recall-grounding noise (P39-7). `muse ask "set a reminder to submit taxes friday" --with-tools` set the reminder but led with "(grounded on 1 note chunk(s) — lease.md ⚠ LOW confidence — verify, may not be in your notes)" — a recall banner about a totally unrelated note — and, when the model cited the tool name as a source, warned "Removed 1 citation(s) to source(s) you don't have (muse.reminders.add) — treat those claims as unverified". Both are noise on a successful ACTION: the user asked Muse to DO something, not to recall. Now, when `classifyActionRequest(query)` matches (the same precision-first detector P39-6 added), the recall grounding banner is suppressed AND the stripped-citation warning is silenced — while the citation gate still runs (the spurious `muse.reminders.add` token is still removed from the text, it just doesn't earn a scary warning). RECALL is completely untouched: a real question still shows its grounding banner and cites. Proof: a LIVE before/after `muse ask "add a reminder to renew my passport next month" --with-tools` (output is now just "(tools used: muse.reminders.add)" + the confirmation — no "grounded on …" banner, no "Removed citation" warning) while `muse ask "what is my rent?"` still prints "(grounded on 1 note chunk(s) — lease.md …)" and cites "1,250,000 KRW [from lease.md]" (recall regression-checked). cli 1710 + `pnpm lint` 0/0 — the agent-action path (P39-6's other half) now confirms cleanly instead of burying the action under irrelevant recall diagnostics — P39-7 bullet
- [Actuation] `muse ask "remind me next month …" --with-tools` now works — Muse understands "next week / next month / next year" (P40-1). The shared relative-time resolver (`loopback-relative-time.ts`, used by `muse.reminders.add` / `muse.tasks.add` / `muse.calendar.add`) handled "in 1 month" and "next monday" but NOT the equally-natural "next week" / "next month" / "next year": the weekday `next <day>` branch read "month"/"week"/"year" as a weekday name, found none in WEEKDAY_INDEX, and returned UNRESOLVED — so `muse ask "remind me next month to renew my passport" --with-tools` died with "next month is not a supported relative phrase". Added a period-offset branch before the weekday parse (week → reference + 7 days at 09:00; month → calendar +1 month; year → calendar +12 months; with optional time-of-day "next month at 2pm") plus Korean parity (다음 주 / 다음 달 / 내년 / 담주 / 다음 해), reusing the same `addCalendarMonths` clamp the "in N months" path uses (so Jan 31 → Feb 28, not Mar 3). Precision preserved: "next mango" / "next thing" still UNRESOLVED, "next monday" still lands on a Monday, "다음 주 월요일" still routes to the weekday resolver. Proof: 7 timezone-robust unit tests in `packages/mcp/test/relative-time-period.test.ts` (next week/month/year resolve to future dates; ~7-day and ~1-year offsets; KO toISOString() === EN; "next month at 2pm" → local hour 14; "next monday".getDay() === 1; non-period "next <noun>" → undefined) + a LIVE `muse ask "remind me next month to renew my passport" --with-tools` → "(tools used: muse.reminders.add)" then "I've set a reminder … for July 3, 2026" (previously an error). mcp 167 files / 1310 tests + `pnpm lint` 0/0 — a user can now set a reminder/task/event with the date words they actually use — P40-1 bullet
- [Honesty/Agent] `muse ask` never falsely claims to act — even in a MIXED question+action request (P39-8). P39-6 short-circuits a PURE imperative ("remind me to …"), but a MIXED request that OPENS with a question — "what is my rent and remind me to pay it tomorrow" — flowed through the normal path: Muse answered the rent (correctly cited) and then tacked on "I will remind you to pay it tomorrow", a false promise on the no-tools path (and fabricated a `[reminder: …]` citation the gate stripped). A new `answerPromisesAction` (agent-core) detects the false promise in the ANSWER itself (not the query, so it catches mixed/compound requests the start-anchored classifyActionRequest misses): it matches an action-TOOL claim — "I'll remind you / set a reminder / add a task / event / schedule / book / email", "I've set/added/scheduled/booked/emailed" — but NOT a conversational "I'll explain", a plain cited answer, or a recall "you have a reminder to pay rent". On the chat-only path, when it fires, Muse appends an honest correction: "(Heads up: I can't actually set reminders, tasks, or events on this path — re-run with --with-tools to do that.)". The --with-tools path is untouched — there the claim is TRUE (it really acted). Proof: detector unit tests in `packages/agent-core/test/casual-prompt.test.ts` (six action-claim forms incl. mixed → true; cited answer / "I'll explain" / "I'm not sure" / "you have a reminder" → false) + a LIVE `muse ask "what is my rent and remind me to pay it tomorrow"` → "Your rent is 1,250,000 KRW [from lease.md]" then "(Heads up: I can't actually set reminders … re-run with --with-tools)". agent-core 1357 / cli 1710 + `pnpm lint` 0/0 — Muse's "I'll do X" is now always true or always corrected, never a bare false promise — P39-8 bullet
- [Actuation] `muse ask "remind me this weekend / by end of the month …" --with-tools` now works — more everyday date phrases land (P40-2). Probing the shared relative-time resolver after P40-1 found "this weekend", "next weekend", "end of the month" / "end of month" / "end of this month" all UNRESOLVED, so a reminder/task/event with those words failed. Added them: "this weekend" / "next weekend" → the Saturday of this / next ISO week at 09:00 (with time-of-day, "this weekend at 8am"); "end of (the/this) month" → the last calendar day at 09:00 (Date(y, m+1, 0)); plus Korean parity (이번 주말 / 다음 주말 / 월말 / 이달 말). The deliberately-vague "in a couple of days" / "in a few days" are intentionally left OUT, honoring the resolver's existing design note (a reminder needs a precise time). Precision preserved — "next mango" / "next thing" still UNRESOLVED, "next monday" still a Monday. Proof: 4 new timezone-robust unit tests in `packages/mcp/test/relative-time-period.test.ts` ("this weekend"/"next weekend".getDay() === 6 and 7 days apart; "end of month".getDate() === 30 && getMonth() === 5 for June; "this weekend at 8am".getHours() === 8; KO 이번 주말/다음 주말/월말/이달 말 toISOString() === their EN counterparts) + a LIVE `muse ask "remind me this weekend to call home" --with-tools` → "(tools used: muse.reminders.add)" then "I've set a reminder for this Saturday (June 6, 2026) to call home". mcp 167 files / 1314 tests + `pnpm lint` 0/0 — reminders/tasks/events now accept the weekend + month-end phrasings users actually type — P40-2 bullet
- [Perception/Edge] `muse ask "what is <contact>'s email/phone?"` answers from your address book WITHOUT a false "treat as unverified" scare (P38-10). Probing the contacts perception surface found a correctly-grounded contact answer (e.g. "Mina Park's email is mina@foundry.io", "phone +1 415 555 0148") fired TWO spurious grounding warnings: (a) the local model cites a contact with the note verb / by slot or id (`[from contact 1]`, `[from contact: mina]`) because the `<<contact N — id>>` wrapper mirrors the `<<note N — file>>` → `[from file]` pattern, so the exact-match note gate false-stripped it ("Removed 1 citation… treat as unverified"); (b) the rubric verdict (`verifyGrounding`) scored coverage against note chunks ONLY, so a contact-sourced fact looked "not backed by your notes" (a second "treat as unverified"). Fixed BOTH deterministically: a new `normalizeContactCitations` (agent-core) rewrites the model's `[from contact N]` / `[contact: id]` mis-forms to the canonical `[contact: <name>]` the gate accepts (resolve-or-leave: an in-range slot, or an id/name that token-overlaps a real matched contact, resolves; an unresolvable `[from contact 9]` is left for the gate to strip; a real `[from contacts.md]` note citation is never rewritten), run before `enforceAnswerCitations`; AND the verdict's evidence set now includes the matched contacts (a high-precision structured exact match, cosine 1) so an address-book answer verifies as grounded rather than unverified. Precision preserved — asking for someone NOT in contacts ("what is Bob Quagmire's email?") still refuses honestly, no fabrication, no contact receipt. Proof: 9 new `normalizeContactCitations` unit tests in `packages/agent-core/test/knowledge-recall-citation-gate.test.ts` (slot-number `[from contact 1]`→`[contact: Mina Park]`; note-verb+id `[from contact: mina]`; bare-slot `[contact 2]` + id `[contact: jin]`; gate flows clean with no false strip; first-name partial resolves by overlap; unresolvable out-of-range/unknown left untouched; `[from contacts.md]` & `[from contact-list.md]` never rewritten; empty-book no-op; canonical idempotent) + LIVE `muse ask "what is Mina Park's email address?"` → "Mina Park's email address is mina@foundry.io" with "👤 from your contacts: Mina Park" receipt and ZERO "unverified"/"Removed citation" warnings; LIVE phone variant identical; LIVE negative `muse ask "what is Bob Quagmire's email address?"` → "I don't have access to Bob Quagmire's email address…". agent-core 1366 + cli 1710 tests + `pnpm lint` 0/0 — the user can now ask for a contact's email/phone and get a cleanly-cited answer instead of a self-contradicting "here it is, but treat it as unverified" — P38-10 bullet
- [Perception/Edge] `muse ask "what tasks / reminders / events do I have?"` answers from your actuator stores WITHOUT a false "treat as unverified" scare — generalizes P38-10 from contacts to EVERY non-note grounded source (P38-11). Probing the deferred P38-10 follow-up confirmed the same self-contradiction across the actuator-recall surfaces: "what tasks do I have?" listed the real open tasks yet fired BOTH a citation strip ("Removed 2 citations (t1, t2) — treat as unverified") AND the rubric verdict ("not backed by your notes"). Two root causes, fixed by code in apps/cli/src/commands-ask.ts: (a) the task/event/reminder context wrappers exposed an internal id/provider in the marker (`<<task N — t1>>`) with NO citation hint — unlike the note wrapper which embeds `[from src]` on its own line — so the local model cited the id (`[task: t1]`), which the title-matching gate then false-stripped; each wrapper now embeds the canonical `[task|event|reminder: <title/text>]` hint so the model cites the human title the gate accepts. (b) the rubric verdict (`verifyGrounding`) scored answer-coverage against note chunks ONLY, so any fact from a non-note source looked "not backed by your notes"; `scoredMatches` now includes EVERY grounded source the model was shown (open tasks, upcoming events, pending reminders, past-session summaries, logged actions, shell commands, feed headlines, contacts — each a high-precision structured/retrieved exact match), and the verdict-answer expands content-citations inline (`[task: X]` → ` X `) BEFORE scoring so a list-style answer whose titles live only inside the citation markers isn't zeroed out by the marker-strip. Fabrication still caught — evidence is ONLY the real retrieved sources and the claim-level value check still rejects a wrong number/name. Proof: LIVE `muse ask "what tasks do I have?"` → cites `[task: Review the Q3 pricing deck]` / `[task: Renew the office insurance]`, "✅ from your tasks" receipts, ZERO "unverified"/"Removed citation" warnings; LIVE `"what reminders do I have?"` clean; LIVE negative `"what is my bank account number?"` → "I don't have access to your bank account number…" (refuses, no fabrication); `node apps/cli/scripts/verify-claim-grounding.mjs` → ALL PASS (4) (wrong number 9000 + wrong name Lee still rejected as ungrounded; correct 1380 + Park still grounded); `node apps/cli/scripts/verify-cited-recall.mjs` → ALL PASS (6) (ingested-chat/email citable, out-of-corpus refuses). cli 163 files / 1710 tests + `pnpm lint` 0/0 — the user can now ask "what tasks/reminders do I have?" and get a cleanly-cited list instead of a self-contradicting "here they are, but treat as unverified" — P38-11 bullet
- [Perception/Edge] `muse ask "what is on my calendar this week?"` answers cleanly (no false "unverified") AND tells you the CORRECT day — repairs the events piece P38-11 claimed but never live-proved (P38-12). Falsifying P38-11 with a real `.muse/calendar.json` event showed events still RED on two counts: the rubric verdict still fired "not backed by your notes" (coverage 0.43 < 0.5 floor), and the local model told the user the WRONG weekday ("Saturday" for a Thursday-dated event). Root cause: the event context wrapper fed the model only ISO timestamps (`2026-06-05T...Z`), so it (a) mis-derived the human weekday and (b) its reformatted-date prose ("Saturday, June 4th, 8 PM") plus true framing ("no other events this week") shared almost no tokens with the title-only evidence → coverage below floor. Fixed both in apps/cli/src/commands-ask.ts with NO agent-core change: (1) the event wrapper now hands the model a HUMAN-readable local date via `toLocaleString` (weekday/month/day/year/time, ISO kept in parens for precision) so the model echoes the correct day instead of guessing; (2) the verdict evidence for every date-bearing source (events startsAt/endsAt, tasks dueAt, reminders dueAt) now carries the same human-date rendering (a `humanDate` helper), so the model's derived date tokens are covered. Fabrication still caught — evidence is ONLY the real retrieved sources and the claim-level wrong-value check is intact. Proof: LIVE `muse ask "what is on my calendar schedule this week?"` run 3× → ALL clean (zero "unverified"/"Removed citation"), and the answer now reads "Friday, June 5, 2026" (the real day, was wrongly "Saturday"); LIVE tasks + reminders stay clean; LIVE negative "what is my bank account number?" still refuses; `node apps/cli/scripts/verify-claim-grounding.mjs` → ALL PASS (4) (wrong number 9000 / name Lee still rejected). cli 163 files / 1710 tests + `pnpm lint` 0/0 — the user can now ask "what's on my schedule?" and get a cleanly-cited answer with the right day of the week — P38-12 bullet (repairs P38-11 events)
- [Perception/Edge] The proactive `📎 Related in your notes` nudge now quotes the RELEVANT line of the matched note, not the chunk's opening — "shows its work" precisely on the one grounded surface still unhardened (P38-13). Probing `decideProactiveRecall`: a chunk matches the triggering item (a due task / imminent event) as a whole via embedding, but the gate quoted the chunk's first 160 chars — so when the actually-relevant sentence sits later in a multi-sentence chunk, the unasked nudge surfaced a non-sequitur AND truncated the real reason away (proven: a 308-char journal chunk for item "Mom birthday" rendered "Project kickoff went well… budget… timeline…" and CUT OFF "Mom's birthday is on June 12th"). Fix (packages/agent-core/src/proactive-recall-gate.ts): `decideProactiveRecall` now accepts the triggering item's title as `query`, and a new `selectRelevantExcerpt` centres the quoted snippet on the sentence with the most query-token overlap (`lexicalOverlap`); a chunk that fits in maxChars is quoted whole, and when there's NO lexical overlap (a purely semantic cosine match) or only one sentence it falls back to the opening — so the change is never worse than before. The confidence DECISION (whether to surface at all) is untouched — only the precision of the quoted evidence improves, so there's zero regression risk to the "stay silent on weak recall" guarantee. Both proactive investigators (`createConfidenceGatedInvestigator` in agent-core + `createIndexedProactiveInvestigator` in apps/cli/src/proactive-notes-recall.ts) thread `item.title` through as the query. Proof: 4 new unit tests in `packages/agent-core/test/proactive-recall-gate.test.ts` (centres on the query-matching line not the opening; no-overlap query falls back to the opening; a short chunk is quoted whole; an over-long chosen sentence is truncated with an ellipsis) + the existing 21 still green + LIVE `node apps/cli/scripts/verify-proactive-recall-gate.mjs` → ALL PASS (4) (trigger in-corpus surfaces a cited relevant finding, off-topic + absent-personal triggers stay SILENT — the earn-proactivity guarantee intact). agent-core 112 files / 1370 tests + cli proactive 30 tests + `pnpm lint` 0/0 — a proactive nudge now shows the user the sentence that actually relates to what's coming up, not a confusing chunk opening — P38-13 bullet
- [Perception] `muse ask --git "what did I work on?"` grounds an answer on your RECENT GIT COMMITS in the current repo — a NEW read-only perception source (the one loop-v2 A3 names but Muse lacked) (P37-11). A user can now ask "what have I been working on?", "what was that payments commit?" and get it answered from their real commit history, cited like any other source. Implemented FILE-side: a new `apps/cli/src/git-reflog.ts` reads `.git/logs/HEAD` (the HEAD reflog) as a file — NO `git` process spawn — so it stays the same low-risk perception class as the shell-history source, never the runner's execution path. `parseGitReflog` extracts commit subjects (keeps commit / commit (initial) / commit (amend) entries, drops checkout/merge/rebase/reset reflog noise, never throws on malformed lines); `selectGitCommits` ranks by query-token overlap with recency tie-break AND recency-fills, so the archetypal generic question ("what did I work on?" — zero token overlap with `feat:`/`fix:` subjects) still surfaces the most recent commits rather than nothing. OPT-IN via a new `--git` flag (mirrors `--shell`: default OFF, `$MUSE_GIT_REFLOG_FILE` overrides the source), so git is never injected unless explicitly requested. Wired through the SAME deterministic grounding gate as every other source: a new `commits` citation class in agent-core `enforceAnswerCitations` (a real commit subject cited `[commit: <subject>]` survives by content-overlap, an invented one is stripped), a "🔧 from your git commits" receipt in `formatNonNoteReceipts`, the wrapper embeds the canonical `[commit: <subject>]` hint (the proven pattern so the local model cites the subject the gate accepts), and the matched commits join the rubric-verdict evidence (so a git-grounded answer isn't false-flagged "not backed by your notes"). Proof: 7 new unit tests in `apps/cli/test/git-reflog.test.ts` (parse keeps the 3 commit kinds + drops checkout/merge/rebase/reset, never throws, returns [] for empty; select ranks an overlapping commit first, recency-fills when nothing overlaps, caps + dedups) + a new `commits`-gate test in agent-core (real subject kept, invented "chore: delete production database" stripped) + LIVE on qwen3:8b with a seeded reflog (HOME + `$MUSE_GIT_REFLOG_FILE` isolated): `muse ask --git "what have I been working on?"` → "you've been working on adding OAuth login… handling Stripe webhook retries… scaffolding the repo" each cited `[commit: …]` with the 🔧 receipt and the banner "3 git commit(s)", ZERO false "unverified"/"Removed citation" warning, the `checkout:` reflog line correctly excluded; specific `muse ask --git "what was the payments commit about?"` → cites exactly "fix(payments): handle the Stripe webhook retry"; the SAME question WITHOUT `--git` injects no git (opt-in respected); negative `muse ask --git "what is my bank account number?"` → refuses, no fabrication from the commit noise; `node apps/cli/scripts/verify-claim-grounding.mjs` → ALL PASS (4) (the new citation class + evidence didn't weaken the wrong-value gate). cli 164 files / 1717 tests + agent-core citation-gate 26 + `pnpm lint` 0/0 — Muse can now read your git history to answer "what did I work on?", cited and honest — P37-11 bullet
- [Perception] `muse ask` now makes its OPT-IN perception sources discoverable — a refusal to a git/shell question points the user at `--git` / `--shell` (P37-12). The `--git` (P37-11) and `--shell` sources are opt-in (default off, for safety), which made them effectively invisible: a user who asks "what did I commit last week?" or "what was that docker command I ran?" just gets "I don't have that in your notes" and never learns Muse can read their git/shell history. New precision-first `suggestOptInSource(query, enabled)` helper (apps/cli/src/commands-ask.ts): on a REFUSAL whose query matches an unmistakable git intent (commit/git/branch/rebase/repo/repository/codebase/pull request) or shell intent (command/terminal/shell/bash/zsh/docker/kubectl), and ONLY when the matching flag isn't already enabled, append a one-line tip — "(tip: add --git to also ground on your recent git commits in this repo)" / "(tip: add --shell …)". Mirrors the sanctioned --repair discoverability nudge (P38-8). Precision-first by design: the ambiguous base verbs push/merge/work-on are deliberately EXCLUDED so a non-git refusal ("what's my rent?", "when is my dentist appointment?") is never cluttered with a spurious tip; and the tip is gated on answerIsRefusal AND !flag-already-on (asking "what did I commit?" WITH --git produces no re-tip). Proof: 9 unit tests in apps/cli/src/commands-ask-repair-nudge.test.ts (suggests --git for 5 git phrasings incl. "what's on my current branch" / "what did I do in the git repo"; --shell for 3 command phrasings incl. "which kubectl command did I use"; SILENT on 4 non-matching refusals incl. car insurance / dentist / rent / landlord; no re-suggest when git or shell already enabled) + LIVE on qwen3:8b (HOME-isolated, rent-only corpus): "what did I commit last week?" → refusal then "(tip: add --git …)"; "what was that docker command?" → "(tip: add --shell …)"; "what is my car insurance number?" → NO tip; "what did I commit?" --git → NO re-tip. cli 164 files / 1721 tests + pnpm lint 0/0 — a user who would otherwise never find --git/--shell is now shown the exact flag that answers their question — P37-12 bullet
- [Actuation] `muse remind` is now daily-reliable server-less — every subcommand (add/snooze/fire/clear/history) falls back to the local store when the Muse API isn't running, matching `list` (P40-3). Probing the actuator found a real local-first reliability defect: `muse remind add "tomorrow at 9am" "call the dentist"` HARD-ERRORED with "muse: Muse API not reachable at http://127.0.0.1:3030. Re-run with --local…" on the default (no server running) setup — while `muse remind list` quietly degraded to the local store with a friendly note. So the single most common write ("add a reminder") failed on exactly the local-first machine Muse is built for (MUSE_LOCAL_ONLY default-on), unless the user knew to pass --local. Fixed in apps/cli/src/commands-remind.ts by extracting a DRY `withLocalFallback(io, useLocal, local, api)` helper and applying it to add, snooze, fire, clear, and history (mirroring the fallback `list` already had): when the API is unreachable it transparently uses ~/.muse/reminders.json with a one-line "muse: API not reachable — using the local reminders store." note; `--local` still skips the API entirely; and crucially a REAL API error (4xx/5xx) STILL throws — the fallback only catches connection-refused ("Muse API not reachable"), so it never silently masks a genuine server failure. Proof: 3 new unit tests in apps/cli/src/commands-remind.test.ts (an unreachable API on `add` persists the reminder to the local file instead of erroring; a 500 on `add` STILL throws AND writes nothing locally — the fallback is unreachable-specific; an unreachable `clear` removes from the local store) + the existing 13 still green + a LIVE server-less sequence on the built CLI (no --local, no server): `remind add "tomorrow at 9am" "call the dentist"` → "API not reachable — using the local store" then "Added [rem_…] call the dentist — due 2026-06-04 09:00", then list/snooze/fire/history/clear all succeed against the same local store. cli 164 files / 1724 tests + pnpm lint 0/0 — a user on a local-first box can now run every `muse remind` subcommand without a server or remembering --local — P40-3 bullet
- [Actuation] `muse tasks` is now daily-reliable server-less too — add/complete/edit/delete fall back to the local store when the API isn't running, via a shared helper (P40-4). Probing after P40-3 (which fixed `muse remind`) found the IDENTICAL local-first defect on the other write actuator: `muse tasks add "review the deck"` HARD-ERRORED "muse: Muse API not reachable…" on the default (no-server) setup, while `muse tasks list` already degraded gracefully to the local store. Rather than duplicate the fix, promoted the local-fallback into a shared `withApiLocalFallback(io, useLocal, local, api, storeLabel)` helper in apps/cli/src/program-helpers.ts (next to isApiUnreachable) and applied it to tasks add/complete/edit/delete — AND refactored `muse remind` (add/snooze/fire/clear/history) to use the same helper, so the two actuators share one DRY local-first seam. Same safety contract: `--local` skips the API entirely; a REAL 4xx/5xx still throws (only connection-refused "Muse API not reachable" degrades), so the fallback never silently masks a genuine server failure. tasks edit's defensive index===-1 not-found branch (dead, since resolveLocalTaskId throws first on a bad id) became a clean throw inside the closure. Proof: 3 new unit tests in apps/cli/src/commands-tasks.test.ts (an unreachable API on `add` persists the task to the local file; a 500 on `add` STILL throws AND writes nothing — fallback is unreachable-specific; an unreachable `complete` marks the task done locally) + the existing 15 tasks + 16 remind tests still green (the remind refactor is behavior-preserving) + a LIVE server-less sequence on the built CLI (no --local, no server): `tasks add "review the Q3 deck"` → "API not reachable — using the local tasks store" then "Added [task_…]", then list/edit/complete/delete all succeed against the same local store, and `remind add` still works. cli 164 files / 1727 tests + pnpm lint 0/0 — every common `muse remind` AND `muse tasks` write now works on a server-less local-first box without --local — P40-4 bullet
- [Perception] `muse ask --file <path>` now cites a clean basename instead of an ugly `../../` escape path (P37-13). Probing the ad-hoc --file perception source found a real citation wart: `muse ask --file ~/work/RUNBOOK.md "…"` grounded and answered correctly but cited `[from ../../../work/RUNBOOK.md]` — because the file sits OUTSIDE the notes dir, `relativizeNoteSource` produced a notesDir-relative escape path. Fixed in apps/cli/src/commands-ask.ts: `relativizeNoteSource` now returns the BASENAME for an absolute path that escapes the notes dir (citation `[from RUNBOOK.md]`), while an in-corpus nested note KEEPS its disambiguating relative path (`projects/vpn.md`, preserving P38-9's "a/notes.md vs b/notes.md" intent — only escaping paths change). Crucially, openability is preserved: `formatSourceReceipts` now derives the 📎 "open to verify" path from the MATCHED chunk's real absolute file rather than from the (now-basename) cited token, so `[from RUNBOOK.md]` still opens the real `~/work/RUNBOOK.md` instead of a non-existent `notesDir/RUNBOOK.md` join. The change is consistent across the gate (allowedNotes), the rubric-verdict evidence source, and the receipt — all use the same `relativizeNoteSource`, so the basename citation matches everywhere. Proof: 2 new unit tests (escaping abs path → basename + in-corpus nested path unchanged in commands-ask-verdict-source.test.ts; receipt opens the real abs path, not the notesDir join, in commands-ask-receipts.test.ts) + the existing 13 receipt/verdict-source tests green + a LIVE `muse ask --file <abs>/RUNBOOK.md "what database is production on?"` → "PostgreSQL 16 [from RUNBOOK.md]" with the receipt pointing at the real /var/folders/.../RUNBOOK.md. Also ran the ~10th-feat-iter regression sweep — verify-claim-grounding 4/4 + verify-cited-recall 6/6 + verify-proactive-recall-gate 4/4 (no grounding regression across the session's 9-feat-iter accumulation). cli 164 files / 1729 tests + pnpm lint 0/0 — a developer grounding on an ad-hoc file now gets a clean, openable citation — P37-13 bullet
- [Perception/Edge] `muse recall` and `muse today --connect` now preview the query-RELEVANT line of a matched note, not the chunk's opening (P38-14). P38-13 fixed this for the proactive-recall GATE (decideProactiveRecall), but the recall RANKER `rankRecallCandidates` (in apps/cli/src/commands-recall.ts — shared by `muse recall` semantic search AND `muse today --connect`'s "💡 Related in your brain" proactive block) still built its preview as `chunk.text.slice(0, 200)` — the chunk opening, query-blind. So a multi-line note whose matching content sits further down surfaced a misleading non-sequitur: e.g. a note starting "# Meeting log / General standup chatter…" whose relevant line is "The Q3 board deck must cover the new pricing tiers" previewed the heading + standup chatter instead of the line that actually matched the query. Fixed with a new `relevantExcerpt(text, queryTokens, max)` helper (local to commands-recall, reusing its existing lexicalOverlap — no circular import with commands-ask): it picks the chunk LINE with the most query-token overlap, skips markdown headings (structure, not content), and falls back to the opening when there's no query overlap or a single-line chunk (so it's never worse than the old slice). The ranker already computed `queryTokens` for its hybrid lexical+vector scoring, so no new work; and `findTodayConnections` (commands-today.ts) now passes `queryText: query` so the today-connections snippet is relevant too. Proof: 2 new unit tests in apps/cli/src/commands-recall.test.ts (a multi-line chunk previews the query-matching line with the "# Meeting log" heading AND the "standup chatter" opening both excluded; a no-queryText call falls back to the first line — back-compat) + the existing 19 recall + all today tests green + LIVE on qwen3:8b/nomic with a 4-line note: `muse today --connect` (task "Prepare the Q3 board deck") → "💡 Related in your brain: [notes] log.md — The Q3 board deck must cover revenue up 22% and the new pricing tiers" (the match, not the opening), and `muse recall "Q3 board deck pricing"` → the same relevant line previewed under the hit. cli 164 files / 1731 tests + pnpm lint 0/0 — a user's recall search and proactive "related notes" now show WHY each note matched, not a confusing opening — P38-14 bullet
- [Actuation] `muse calendar` reads (events / tomorrow / free / conflicts / providers / export) now work server-less — a user can LIST what they added without --local (P40-5). Probing the calendar surface found the inverse asymmetry of P40-3/4: `calendar add` is LOCAL-by-design (it writes ~/.muse/calendar.json directly, works server-less, and has no --local flag at all), but EVERY read subcommand defaulted to the API and HARD-ERRORED "muse: Muse API not reachable at http://127.0.0.1:3030. Re-run with --local…" on the default (no-server) setup. So a user could run `muse calendar add "Dentist cleaning" --at "tomorrow 3pm"` (succeeds, local) and then be UNABLE to see it with `muse calendar events` unless they discovered --local — you could write but not read your own calendar. Fixed in apps/cli/src/commands-calendar.ts by wrapping all six read subcommands (events, the day-shortcuts like `tomorrow`, free, conflicts, providers, export) in the shared `withApiLocalFallback` helper introduced in P40-4 (so remind/tasks/calendar now share ONE server-less seam): when the API is unreachable the read transparently uses the local calendar provider with a "muse: API not reachable — using the local calendar store." note; `--local` still skips the API; and a REAL 4xx/5xx still throws (only connection-refused degrades, never masking a server error). Proof: 2 new unit tests in apps/cli/src/commands-calendar.test.ts (an unreachable API on `calendar events` lists the locally-added "Dentist" event via the LocalCalendarProvider read-back; a 500 on `events` STILL throws — the fallback is unreachable-specific) + the existing 33 calendar tests green (the closure-extraction refactor is behavior-preserving) + a LIVE server-less sequence on the built CLI (no --local, no server): `calendar add "Dentist cleaning" --at "tomorrow 3pm"` then `calendar events` → "API not reachable — using the local calendar store" then the event listed ("[cal_74c8] 15:00–16:00 Dentist cleaning (local)"), and tomorrow/free/conflicts/providers all fall back too. cli 164 files / 1733 tests + pnpm lint 0/0 — a user on a local-first box can now both add AND read their calendar without a server or --local — P40-5 bullet
- [Front-door/Privacy] `muse doctor` now reports the TRUE local-only model instead of a misleading "inferred from GEMINI_API_KEY" (P34-12). Under MUSE_LOCAL_ONLY (default-ON, the core privacy posture), the model router IGNORES any ambient cloud key and runs the local ollama/qwen3:8b (per resolveDefaultModel) — but the doctor's model-env check did NOT account for local-only: on any box that merely carried a GEMINI_API_KEY in the environment, it printed "[warn] model env: inferred from GEMINI_API_KEY". So a privacy-bound user running `muse doctor` precisely to CONFIRM that nothing leaves their machine was told their model is Gemini — the diagnostic undercut the exact guarantee local-only exists to provide. Fixed in apps/cli/src/commands-doctor.ts by extracting a pure, exported `modelEnvCheck(env)` that mirrors the runtime's resolveDefaultModel exactly: an explicit MUSE_MODEL is reported verbatim; under local-only (parseBoolean(MUSE_LOCAL_ONLY, true)) it reports "ollama/qwen3:8b (local-only default — ambient cloud keys ignored)" as OK; and the cloud-credential inference WARN ("inferred from GEMINI_API_KEY (MUSE_LOCAL_ONLY=false)") appears ONLY under an explicit opt-out. Bonus fix: the doctor's downstream "ollama model pulled" check now uses the RESOLVED model (resolveDefaultModel(env)) instead of the raw MUSE_MODEL env, so under local-only — where no MUSE_MODEL is set — it now actually verifies qwen3:8b is pulled (5.2 GB), a check that was silently skipped before. Also exported LOCAL_FIRST_DEFAULT_MODEL from @muse/autoconfigure's index for the mirror. Proof: 5 new modelEnvCheck unit tests in apps/cli/src/commands-doctor.test.ts (local-only + GEMINI key → reports the local model NOT "inferred from GEMINI"; default env unset → local model; explicit MUSE_LOCAL_ONLY=false + key → warn inferred; explicit MUSE_MODEL reported verbatim; opt-out + no model + no key → fail) + 2 existing program.test.ts doctor tests corrected to set MUSE_LOCAL_ONLY=false where they intentionally exercise the cloud-inference fail/warn path + a LIVE `muse doctor` in all three scenarios (local-only+GEMINI → "✓ model env: ollama/qwen3:8b (local-only default — ambient cloud keys ignored)" + "✓ ollama model: qwen3:8b pulled (5.2 GB)"; opt-out+GEMINI → warn inferred; explicit MUSE_MODEL → verbatim). cli 164 files / 1738 tests + autoconfigure 484 + pnpm lint 0/0 — a privacy-conscious user can now trust `muse doctor` to truthfully confirm their model is local — P34-12 bullet
- [Grounding edge/Perception] `muse ask` now treats facts the user told `muse remember` as a first-class CITED grounding source — no more misattribution to a random note (P38-15). Probing the user-memory surface found a real fabrication bug: `muse remember "I am allergic to penicillin"` then `muse ask "what am I allergic to?"` answered correctly ("You are allergic to penicillin") but cited it `[from n.md]` — a note that never mentioned penicillin — and then fired a "treat as unverified" warning on a TRUE answer. Root cause: remembered facts are injected into the persona (buildMusePersona) so the model KNOWS them, but they were not a citable grounding source, so the model attributed the fact to the only note available and the rubric verdict false-flagged it. Fixed in apps/cli/src/commands-ask.ts + packages/agent-core/src/knowledge-recall.ts by making remembered facts a first-class source via the proven P38-10 contacts pattern: a new `memories` citation class + `[memory: <topic>]` strip branch in enforceAnswerCitations; a query-matched memory grounding block with the [memory: <topic>] hint; a "🧠 from what you told me" receipt in formatNonNoteReceipts; the facts in the rubric-verdict scoredMatches evidence and the content-citation expansion; and a `renderMemoryFact` helper that turns a machine-keyed auto-extracted fact (allergy_penicillin: "yes") into a natural phrase ("allergy penicillin") so the model + re-verify judge can read it. The citation gate validates against ALL the user's facts (allUserMemoryFacts — the persona exposes all of them, so any is citable), so a correctly-recalled fact is never wrongly stripped, while the internal veto:/goal: preference slots are excluded. Proof: 6 new helper unit tests in apps/cli/src/commands-ask-memory.test.ts (allUserMemoryFacts includes facts + plain prefs but not veto:/goal:; renderMemoryFact drops a bare yes/true value and keeps a real one; selectMemoryFacts matches the query) + a `memories`-gate test in agent-core (a real remembered fact kept, an invented "bank_pin" the user never stated stripped) + LIVE on qwen3:8b: `muse ask "what is my favorite color?"` → "Your favorite color is blue [memory: favorite_color]" with the 🧠 receipt and ZERO unverified/Removed-citation warnings; `"what is my apartment number?"` → "4B [memory: apartment_number]" clean; an unremembered fact ("blood type") still refuses; `node apps/cli/scripts/verify-claim-grounding.mjs` → ALL PASS (4) (the new citation class didn't weaken the fabrication gate). cli 165 files / 1744 tests + pnpm lint 0/0 — a fact the user explicitly told Muse is now recalled cited to Muse's MEMORY, not a note that never mentioned it (known edge: an adjective/noun query mismatch like "allergic" vs an "allergy_penicillin" fact still trips the answerability→judge verdict, but the fact is correctly cited regardless — see Rejected ledger) — P38-15 bullet
- [Felt/Grounding edge] `muse brief` greets you by your REAL name or none — never an invented one (P35-8). Probing the JARVIS-style morning briefing found a fabrication on the felt surface: with no name on file, `muse brief` opened "Good morning, Alex." — the small qwen model filled the "Good morning, ___" greeting slot with an INVENTED name, consistently across runs and even when an explicit `--user bob` was passed (so it was a hallucinated name, not the user id). On a "tell it everything, it knows you" assistant, being greeted by a name that isn't yours is both a fabricated fact (asserting your name is something it isn't) and a trust-puncturing felt miss — the opposite of "knows you". The briefing CONTENT was already faithful (a separate probe with a real task + reminder accurately stated "submit the tax forms by 10:06 AM" and "call the plumber" with no embellishment); only the greeting slot fabricated. Fixed in apps/cli/src/commands-brief.ts: a new exported `resolveUserName(facts)` helper reads the user's actual name from a `name` / `first_name` / `firstname` / `full_name` / `preferred_name` / `nickname` fact (case + separator-insensitive), and the brief's greeting system-prompt instruction is now CONDITIONAL — `Address the user as "<name>".` when a name is known, else "No name is on file for the user — open with a plain time-of-day greeting (e.g. 'Good morning.') and do NOT address them by any name or invent/guess one." Proof: 3 new resolveUserName unit tests in apps/cli/src/commands-brief.test.ts (resolves `name`/`first_name`/`Preferred Name`/`nickname` variants; returns undefined for a facts set with no name fact so the greeting stays generic; ignores a blank value) + the existing 11 brief tests green + LIVE on qwen3:8b: no name on file → "Good morning" ×3 (the invented "Alex" is gone); after `muse remember "my name is Jinan"` (stored as `name: Jinan`) → "Good morning, Jinan" (the real name). cli 165 files / 1747 tests + pnpm lint 0/0 — Muse's morning briefing now addresses you correctly instead of fabricating a stranger's name — P35-8 bullet
- [Actuation] `muse remind add` warns when the due time is in the PAST — catching the date typo instead of silently creating a useless overdue reminder (P40-6). Probing the actuator found that `muse remind add "2020-01-01T09:00:00Z" "old"` (or any past-resolving time) SILENTLY created a reminder dated in the past, with no feedback — but a reminder fires AT its dueAt, so a past time is almost always a typo (a wrong year, or "at 8am" when it is already 9am): the reminder is immediately overdue and fires on the very next `muse remind run`, not when the user intended. Fixed in apps/cli/src/commands-remind.ts: after parseReminderDueAt resolves the time, if it's before now the add prints a one-line heads-up to stderr ("muse: heads up — <time> is in the PAST; this reminder is already overdue and will fire on the next `muse remind run`. If that's a typo, `muse remind clear <id>` and re-add a future time.") and STILL creates the reminder (warn, don't block — the user may genuinely want a past-dated entry, and can clear it otherwise). Suppressed under --json so structured output stays clean. Proof: 3 new unit tests in apps/cli/src/commands-remind.test.ts (a past ISO dueAt warns on stderr AND is still Added; a future time does NOT warn; --json emits no prose warning) — the runRemind test harness now captures stderr — + the existing 16 remind tests green + a LIVE check: `muse remind add "2020-01-01T09:00:00Z" "old"` → the PAST heads-up + "Added"; `muse remind add "tomorrow at 9am" "future"` → just "Added" (no warning). cli 165 files / 1750 tests + pnpm lint 0/0 — a user who fat-fingers a date now learns their reminder is overdue instead of wondering why it never fired — P40-6 bullet
- [Actuation] `muse tasks complete` is now idempotent — re-completing an already-done task keeps its ORIGINAL completion time instead of silently rewriting it (P40-7). Probing the actuator found that `muse tasks complete <id>` on a task whose status is already "done" silently set `completedAt: new Date().toISOString()`, OVERWRITING the original "when it was actually done" (verified: an original completedAt of 2026-01-15 vanished, replaced by now), and reported a misleading "Completed [id] title" as if it had just done it. Fixed in apps/cli/src/commands-tasks.ts: the local complete path now checks the task's status first — an already-done task is returned UNCHANGED (no write, original completedAt preserved) and the command reports "Task [id] title was already done (completed <date>) — no change." The open→done path is untouched (still writes completedAt=now and reports "Completed"). Proof: 1 new unit test in apps/cli/src/commands-tasks.test.ts (re-completing a task with completedAt=2026-01-15 leaves that timestamp intact, NOT rewritten to now) + the existing 18 tasks tests green + a LIVE check (already-done task → "was already done (completed 2026-01-15 …) — no change" with the original timestamp preserved in the store; an open task → normal "Completed [id]"). cli 165 files / 1751 tests + pnpm lint 0/0. (This iteration also investigated #1 --with-tools RGV — confirmed it genuinely needs an agent-runtime/autoconfigure assembly change to surface knowledge_search evidence, ApiServerAssemblyOptions has no hooks seam, so it stays a multi-commit job — and CONFIRMED the P38-15 memory-verdict "edge" is the gate being correctly conservative: relaxing answerabilityFloor to clear the warn LEAKED a wrong-drug answer, so it was reverted and recorded in the Rejected ledger.) — P40-7 bullet
- [Grounding edge] Cross-lingual recall no longer false-flags a CORRECT answer — a Korean question against English notes (or vice versa) (P38-16). Probing 진안's real usage: `muse ask "내 와이파이 비밀번호가 뭐야?"` against an English note ("The office WiFi password is hunter2-blue") grounded and answered correctly ("당신의 와이파이 비밀번호는 hunter2-blue입니다 [from net.md]") but the rubric verdict fired "treat as unverified" — because the LEXICAL coverage/answerability scores ~0 when the answer/query are Korean and the evidence is English (the Korean tokens never match the English ones), pushing it to the weak band where the small grounding judge — instructed to answer NO when "unsure" — defaults to NO on the language gap. Fixed by hardening the SHARED REVERIFY_SYSTEM_PROMPT in packages/agent-core/src/knowledge-recall.ts: the judge is now told the QUESTION/ANSWER/EVIDENCE may be in DIFFERENT languages and to judge whether the underlying FACTS match (a value/number/name/term that appears in the EVIDENCE supports the same fact in the ANSWER even when the surrounding words are translated), with an explicit guard that a value the EVIDENCE does NOT contain is still unsupported in any language. Crucially this does NOT relax or bypass the gate — unlike the P38-15 answerability-relaxation attempt that was reverted last iteration for leaking a wrong drug, this keeps the judge in the loop and only makes it better at the cross-language comparison, so a WRONG cross-lingual value is still rejected. Proof: 2 new permanent regression cases added to apps/cli/scripts/verify-claim-grounding.mjs (CROSS-LINGUAL correct: a Korean answer drawn from an English note whose value "hunter2-blue" matches → the judge upholds → GROUNDED; CROSS-LINGUAL wrong value: a Korean answer asserting "dragon99-red" that the evidence never states → UNGROUNDED) — the full battery passes 6/6 on two consecutive runs (stable), and the original 4 same-language wrong-value/correct-value cases still pass 4/4 (no regression to same-language judging) — plus a new agent-core unit guard asserting the prompt instructs cross-language fact comparison, + LIVE on qwen3:8b: `muse ask "내 와이파이 비밀번호가 뭐야?"` 5/5 runs clean (no unverified warning, cited [from net.md]); a Korean must-refuse ("내 신용카드 번호?") still refuses with no fabrication. agent-core 112 files / 1373 tests + pnpm lint 0/0 — a Korean-speaking user (or anyone whose notes and questions span languages) now gets cited, un-warned answers instead of a spurious "treat as unverified" on every correct cross-lingual recall — P38-16 bullet
- [Felt/Grounding edge] A `muse ask` refusal now reads clean — no self-contradicting "Removed citation … treat those claims as unverified" warning on an answer that asserts nothing (P34-13). When the small local model tacks a spurious citation onto a refusal (e.g. the Korean "저는 당신의 혈액형에 대한 정보를 가지고 있지 않습니다 [from n.md]" or the English "I don't have that [memory: …]"), the citation gate correctly strips it — but the code then printed "⚠️ Removed 1 citation(s) to source(s) you don't have (…) — treat those claims as unverified," which is nonsensical on a refusal (it makes no claims to "treat as unverified") and especially jarring for a Korean user, where the small model fabricates a stray citation on a refusal noticeably more often. The bug: that warning (commands-ask.ts) fired before the refusal was even classified and didn't gate on it. Fixed by extracting a pure, exported `shouldWarnStrippedCitations({strippedCount, json, isActionRequest, isRefusal})` (mirroring shouldSuggestRepair) and moving the refusal classification up so the notice is gated on !isRefusal (it already skipped action requests, where citing a tool name is a harmless quirk). The spurious citation is still stripped from the answer text — only the user-facing warning is suppressed on a refusal, so fabrication=0 is unchanged (the citation never reaches the user either way). Proof: 4 new unit tests in apps/cli/src/commands-ask-repair-nudge.test.ts (fires on a normal answer that had a fabricated citation stripped; SILENT on a refusal, on an action request, when nothing was stripped, and under --json) + the existing refusal tests green + LIVE on qwen3:8b: a Korean must-refuse "내 혈액형이 뭐야?" → "정보를 가지고 있지 않습니다" + the warm "add a note and I'll have it next time" nudge with ZERO "Removed citation" warning; an English must-refuse likewise clean; a non-refusal answer with a fabricated citation still warns (the warning only adds the !isRefusal term). cli 165 files / 1755 tests + pnpm lint 0/0 — a user (especially Korean-speaking) whose question Muse can't answer now gets an honest, uncluttered "I don't have that" instead of a contradictory unverified-claims warning on a no-claim answer — P34-13 bullet
- [Grounding edge/Perception] A Korean memory recall now cites `[memory: <key>]` cleanly instead of a false `[from <key>]` that gets stripped (P38-17). Probing 진안's core "knows you" flow: `muse remember "내 차 번호판은 12가 3456이야"` stored fact.car_license_plate = "12가 3456" fine, but `muse ask "내 차 번호판 뭐야?"` answered correctly ("당신의 차 번호판은 12가 3456이에요") yet cited it `[from car_license_plate]` — the NOTE citation verb with the memory KEY — which the exact-match note gate then false-stripped with a "Removed 1 citation … treat those claims as unverified" warning on a TRUE recalled fact. Root cause: the Korean query tokens ("차", "번호판") don't lexically match the English fact key ("car", "license", "plate"), so selectMemoryFacts returns nothing and the `[memory: <topic>]`-hint grounding block isn't injected; the persona still lists the fact (so the model knows it) but, without the hint, the small model falls back to the note verb. This is the same class as the P38-10 contact-citation normalization, now for memory: a new normalizeMemoryCitations (packages/agent-core/src/knowledge-recall.ts) rewrites a `[from <X>]` whose <X> EXACTLY matches a known memory fact key (separator- and case-insensitive) into `[memory: <X>]` BEFORE enforceAnswerCitations runs, while a real `[from note.md]` whose name is not a memory key is left untouched — so a note citation is never mistaken for a memory, and the gate (validating against all the user's facts from P38-15) then keeps the corrected memory citation. Applied in commands-ask alongside normalizeContactCitations. Proof: 4 new unit tests in agent-core (rewrites `[from car_license_plate]`→`[memory: car_license_plate]` incl. a "Car License Plate" spacing variant; leaves `[from home.md]` / `[from projects/vpn.md]` untouched; the rewritten form flows through enforceAnswerCitations with zero strips; a no-op when there are no memory keys) + LIVE on qwen3:8b: `muse ask "내 차 번호판 뭐야?"` run 3× → ZERO "Removed citation" warning and the "🧠 from what you told me: car_license_plate" receipt now shows; a Korean WiFi-note query still cites `[from home.md]` (a real note is not rewritten to memory). agent-core 112 files / 1377 tests + cli 165 files / 1755 tests + pnpm lint 0/0 — a Korean user can now recall a fact they told Muse and see it cited to memory, not stripped as if it were a fabricated note citation — P38-17 bullet
