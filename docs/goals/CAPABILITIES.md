# Capabilities — the loop's only success metric

Append-only inventory of **real, user-exercisable** Muse
capabilities. One line per capability: what the user can do, the
exact command/surface, and the executable check that proves it.

Rules (enforced by `.claude/rules/iteration-loop.md`):

- Every shipped outward goal MUST append exactly one new line here.
  The `<check>` MUST be a concrete automated test or smoke id that
  actually runs green under `pnpm check` / `pnpm smoke:broad` /
  `pnpm smoke:live` and asserts the *capability* end-to-end (not
  that code compiles). Prose with no runnable id is not a check.
- **Cross-time falsification:** every iteration's first action is
  to run the newest line's check and prove it still passes. A
  faked/broken line = the next iteration's whole job is to fix it.
- **Regression sweep:** every 10th iteration re-runs ALL checks;
  any regression = next iteration must restore it.
- A request/response-path capability whose `smoke:live` did NOT
  actually run is tagged `[UNVERIFIED-LIVE]`; it does not count
  until a later iteration runs the live check and drops the tag.
- Append-only. Never delete or weaken a line.
- **The success metric is NOT this line count.** It is
  `OUTWARD-TARGETS.md` bullets flipped `[ ]`→`[x]`. A line here is
  the *evidence* for a flip (cite the bullet it delivers); a line
  that adds no bullet flip is thin and does not satisfy the metric.
  No bullet flipped in the last 5 iterations ⇒ next iteration's
  sole mandate is to flip one end-to-end. Flat bullets =
  degeneration; act on it — never stop, never ask a human.

Format: `- [<axis>] <capability> — <command/surface> — <runnable check id> — P<n> bullet`
axis ∈ Reach | Anticipation | Autonomy | Presence

## Inventory

- [Reach] Ask any LLM provider behind one runtime — `muse ask "…"` (OpenAI/Anthropic/Gemini/OpenRouter/Ollama/LM Studio/compat) — `pnpm smoke:broad` chat endpoint
- [Reach] Real local-LLM round-trip on the loop PC's Qwen — `pnpm smoke:live` — `scripts/smoke-live-llm.mjs` (local Ollama Qwen, executes end-to-end)
- [Reach] Notes / tasks / calendar personal stores queried by the agent — `muse notes|tasks|calendar …` — `@muse/mcp` store tests + smoke:broad
- [Reach] Calendar across Local / Google / CalDAV / macOS — `muse calendar events --from --to` — calendar provider contract tests
- [Anticipation] Proactive notice loop (upcoming events / reminders) — proactive daemon tick — `apps/api` proactive tick tests
- [Anticipation] Self-queued follow-up promises fire later — followups daemon — `runDueFollowups` tests
- [Autonomy] Multi-step plan-execute over tools — `muse ask --with-tools` / plan-execute endpoint — smoke:broad plan-execute
- [Autonomy] Multi-agent sequential/parallel orchestration — orchestration endpoint — smoke:broad multi-agent
- [Presence] Voice in/out (Whisper / Piper / whisper.cpp) — `muse listen` / `--speak` — voice unit tests
- [Presence] Cross-session episodic recall surfaced into context — REPL across sessions — episodic-summariser tests
- [Presence] Proactive notices route to the active terminal and fall back to messaging when it goes stale (no black-hole) — `muse proactive watch` (TTY) then idle past the active-session window — `@muse/mcp` mcp.test.ts "falls back to messaging when terminal presence is stale"
- [Presence] An inbound consumer drains the messaging inbox and runs the FULL agent runtime per message, replying on the originating channel (not append-to-soft-context) — wired bot inbox with `MUSE_INBOUND_REPLY_ENABLED=1` — `@muse/api` inbound-reply-tick.test.ts "answers each new inbox message via the agent, replies to its source, and is idempotent" (integration inbound→run→reply) — P1-b1
- [Presence] The agent reply is POSTed back over the real channel provider's HTTP send (chat_id + text), verified through a contract-faithful HTTP fake of a real provider, never a fake registry — inbound→reply over `TelegramProvider` — `@muse/api` inbound-reply-tick.test.ts "delivers the agent reply over a real provider's HTTP send — contract-faithful, not a fake registry" — P1-b2
- [Presence] A channel chat is a continuous session — the user's next message on the same channel carries prior turns (user + Muse) into the agent, isolated per {provider,source} — multi-turn inbound on a wired channel — `@muse/messaging` inbound-threaded-runner.test.ts "prepends prior turns on the next message of the same channel, isolated per channel" — P1-b3
- [Autonomy] A channel-triggered risky tool (write/execute) is NOT executed without approval — it posts an in-chat approval prompt back over the real provider's HTTP and fail-closed denies; read tools pass — risky tool during inbound→agent run on a wired channel — `@muse/messaging` channel-approval-gate.test.ts "blocks a risky tool and posts an in-chat approval prompt over the real provider's HTTP" — P1-b4
- [Anticipation] Muse grows its user-model from real use on a tool-using turn (channel chats included — the run now carries a channel-derived userId; the wired afterComplete auto-extract hook stores the fact) — talk to Muse on a wired channel / tools-enabled API turn — `@muse/agent-core` auto-extract-tool-turn.test.ts "stores an extracted fact under the run's userId after a tool-using turn" — P0-b1
- [Anticipation] Cross-session recall is embedding-similarity in production — a paraphrase that shares no tokens with a past session recalls it (Jaccard misses); zero-cost local-Ollama embedder, fail-open to Jaccard if down — REPL/API across sessions — `@muse/agent-core` episodic-recall-embedding.test.ts "uses embedding cosine when an embedder is wired — paraphrase recalls" + "fail-open: a throwing embedder degrades to Jaccard" — P0-b2
- [Anticipation] A proactive notice surfaces the user's related notes for the imminent item's topic, unasked (Muse investigated an unstated need) — `muse proactive` daemon with notes configured — `@muse/mcp` notes-investigator.test.ts "surfaces a finding citing the real note for a matching imminent item" + proactive-loop "autonomously investigates the unstated need and surfaces the finding in the unasked notice" — P0-b3
- [Autonomy] On an under-specified imperative ("do it", no object/referent) the agent is steered to ask ONE clarifying question instead of hallucinating an action — wired live into the agent-runtime context pipeline — any `muse ask`/chat/API turn — `@muse/agent-core` clarify-directive.test.ts "prepends a clarify directive when the lone user message is under-specified" + "does NOT fire when a prior assistant turn makes it a confirmation" — P0-b4
- [Anticipation] The proactive daemon delivers an imminent-item notice to a REAL channel API (not a fake registry) — `runDueProactiveNotices` over a real `TelegramProvider`'s HTTP send — proactive daemon with a messaging provider + tasks/calendar configured — `@muse/api` proactive-notice-delivery.test.ts "POSTs an imminent-task notice over the real provider's HTTP send and dedupes on the next tick" (asserts Bot API URL + chat_id + text, real dedupe sidecar) — P2-b1
- [Anticipation] Anticipatory prep — the imminent-item notice carries the related doc ("here's the doc") on the SAME real-channel POST — real `LocalDirNotesProvider` + `createNotesInvestigator` wired into `runDueProactiveNotices` over a real `TelegramProvider`'s HTTP send — proactive daemon with notes + a messaging provider configured — `@muse/api` proactive-notice-delivery.test.ts "delivers anticipatory prep (the related doc) in the POST when an investigator is wired" (POST body carries the announcement + "Related notes: …", decoy excluded) — P2-b2
- [Presence] Muse perceives the user's environment unasked — an ambient change (e.g. the active window) measurably alters a subsequent agent answer — `applyAmbientContext` wired into the live agent-runtime pipeline behind an opt-in `ambientSnapshotProvider` (off by default, fail-open, untrusted-field sanitised) — any `runtime.run` with an ambient provider configured — `@muse/agent-core` ambient-context-runtime.test.ts "an ambient change measurably alters a subsequent agent answer" + "is off by default" — P3-b1
- [Autonomy] Calendar WRITE (create / move / cancel) is contract-faithfully exercised — not read-only — across the real Google, CalDAV and macOS providers with only the transport (fetch / osascript spawn) faked — `muse calendar` write paths / agent calendar tool — `@muse/calendar` calendar-write-contract.test.ts (Google POST/PATCH/DELETE w/ Bearer+JSON; CalDAV PUT/REPORT→PUT/DELETE w/ Basic+ICS; macOS make-new-event/delete AppleScript over the real osascript spawn) — P4-b1
- [Presence] Voice round-trip works end-to-end — captured mic audio is transcribed, the transcript reaches the agent, the reply is synthesised and played — `muse listen` (push-to-talk) — `@muse/cli` commands-listen.test.ts "captured audio is transcribed, sent to the agent, the reply is synthesised and played" drives the real `registerListenCommand` action with only mic spawn / STT / TTS / `/api/chat` / playback faked, asserting each stage's data flowed — P4-b2
- [Autonomy] A standing objective ("watch X / keep trying until Z / tell me when W") registered by the user survives a process restart and the ~20-min loop boundary as durable on-disk state — `~/.muse/objectives.json` via `addObjective`/`readObjectives` — `@muse/mcp` personal-objectives-store.test.ts "register → restart → still tracked: a registered objective survives a fresh read" (+ accumulation across independent registrations, idempotent register, corrupt-quarantine) — P5-b1
- [Autonomy] A standing objective is autonomously re-evaluated on a tick: condition met → its action fires once and it is durably marked done; unmet → exponential-backoff retry; unmeetable or attempts-exhausted → durably escalated (never silently dropped) — objective re-evaluation daemon (`runDueObjectives`) — `@muse/mcp` objective-evaluation-loop.test.ts "condition flips → action fires once and the objective is durably marked done" + "unmet → exponential backoff" + "unmeetable → durably escalated" + "unmet too many times → escalates" — P5-b2
- [Autonomy] A standing objective acts as the user only under recorded consent — a met objective performs a real (HTTP-faked) external action carrying the user's scoped service credential ONLY when a matching consent record exists; absent/scope-mismatched consent is fail-closed (no credential use, no HTTP) — `runDueObjectives` act via `performConsentedAction` over `~/.muse/consents.json` — `@muse/mcp` consented-action.test.ts "consent recorded ⇒ the real external request fires carrying the scoped credential" + "fail-closed: no recorded consent ⇒ no HTTP call" + "scope is never broadened implicitly" + "end-to-end: a met objective performs the real external action … and is marked done" — P5-b3
- [Presence] Every autonomous action Muse takes on the user's behalf — performed OR refused — produces a durable, append-only, rationale-bearing log entry (what / why / when / result) the user can review — `~/.muse/action-log.json` via `appendActionLog`/`queryActionLog` (newest-first, user-scoped) — `@muse/mcp` personal-action-log-store.test.ts "an autonomous consented action produces a rationale-bearing log entry the user can query" + "a fail-closed refusal is also logged" + append-only / corrupt-quarantine — P6-b1
- [Presence] One-tap undo of a logged autonomous action reverses it where reversible AND records a durable memory veto so the same trigger no longer auto-acts — the veto overrides prior consent, enforced fail-closed in the real act path — `undoLoggedAction` + `performConsentedAction(vetoFile)` over `~/.muse/vetoes.json` — `@muse/mcp` undo-action.test.ts "a veto recorded by an undo overrides prior consent and blocks the same trigger from recurring" + "veto overrides consent directly in performConsentedAction (no HTTP)" + "irreversible action still records the veto" + exact-scope — P6-b2
- [Presence] Muse learns from correction — a recorded veto is surfaced into every subsequent agent run as a `[Learned Avoidance]` directive so it stops PROPOSING the corrected class anywhere, not only at the consented-action gate; conservative (zero vetoes ⇒ exact no-op) and fail-open — `applyVetoAvoidance` wired live into the agent-runtime context pipeline behind a duck-typed `VetoAvoidanceProvider` — any `runtime.run` with a veto provider configured — `@muse/agent-core` veto-avoidance.test.ts "a recorded veto is carried into a later agent run's context; none → no-op" + gating / fail-open / injection-sanitised — P7-b1
- [Presence] Learned avoidances are reviewable and clearable so a correction is not permanent-by-accident — the user lists "what Muse learned not to do" and one-tap clears one; after clear the avoidance directive no longer injects and the consented-action gate no longer blocks — `queryVetoes` / `removeVeto` over `~/.muse/vetoes.json` — `@muse/mcp` personal-veto-store.test.ts "review lists the avoidance; clear removes it and the directive no longer injects" + user-scoped newest-first review + precise clear + missing-id no-op — P7-b2
- [Anticipation] Muse synthesises ONE coherent situational briefing — soonest-first upcoming items + delegated objectives flagged needs-you / still-tracking, finished ones excluded — instead of N disconnected notices; silent when nothing is worth saying — `composeSituationalBriefing` (proactive briefing surface) — `@muse/mcp` situational-briefing.test.ts "composes imminent + escalated + active into ONE briefing, soonest-first" + "returns undefined when there is nothing worth saying" + NaN-drop / whitespace-collapse — P8-b1
- [Anticipation] The situational briefing is delivered proactively on the REAL channel API (not a fake registry) once per situation-window — `runDueSituationalBriefing` over a real `TelegramProvider`'s HTTP send + a real last-fired dedupe sidecar — proactive briefing daemon with a messaging provider + objectives configured — `@muse/mcp` situational-briefing-loop.test.ts "POSTs ONE synthesised briefing over the real provider's HTTP send and dedupes within the window" + nothing-to-say silent + re-briefs after the window — P8-b2
- [Autonomy] A registered standing objective is autonomously re-evaluated by a REAL running server — an apps/api `setInterval` daemon rider drives `runDueObjectives` on a clamped cadence with single-flight + fail-soft + unref discipline (parallel to the followup/proactive ticks) — `startObjectivesTick` daemon — `@muse/api` objectives-tick.test.ts "a tick fires runDueObjectives on a due objective: acted + durably marked done" + single-flight + fail-soft-survives + wild-interval-clamped — P9-b1
- [Autonomy] A configured real server autonomously decides + acts on standing objectives — the objectives + situational-briefing daemons are env-gated/registered in the apps/api daemon set (off by default) with a concrete model evaluator (`createModelObjectiveEvaluator`, strict-parse + conservative fail-soft) + messaging actuator; the evaluator genuinely decides a real objective's condition with the loop's mandated local qwen3:8b — `start{Objectives,SituationalBriefing}DaemonIfConfigured` + server.ts — `@muse/api` objectives-daemon.test.ts + situational-briefing-daemon.test.ts (env-gate/register/stop discipline, 8/8) + `@muse/mcp` objective-evaluator.test.ts (4/4) + live qwen3:8b round-trip of `createModelObjectiveEvaluator` deciding met/unmet/unmeetable (goal 398) — P9-b2
- [Anticipation] The situational briefing is grounded in the user's REAL imminent personal tasks (not objective-status only) — `deriveBriefingImminent` (mirrors the proactive task-imminence rule) feeds live due-soon tasks per tick into the briefing's `Upcoming:`, so a configured server tells the user what is coming up — `startSituationalBriefingTick` imminentProvider wired in the daemon when `tasksFile` is set — `@muse/mcp` briefing-imminent.test.ts (open/due-soon/proactive selection, leadMinutes, missing-store) + `@muse/api` situational-briefing-tick.test.ts "P8-b3: a real imminent task grounds the briefing's Upcoming alongside objective status" — P8-b3
- [Anticipation] The briefing's `Upcoming:` also includes the user's REAL imminent calendar events (timed/in-window/`[no-proactive]`-respected), unioned with tasks soonest-first — `deriveCalendarBriefingImminent` (mirrors the proactive calendar rule) + the briefing daemon's imminentProvider unions tasks + `ServerOptions.calendar` when configured — `@muse/mcp` briefing-imminent.test.ts "deriveCalendarBriefingImminent … selects only timed, in-window, non-opted-out events" + range/fail-soft + `@muse/api` situational-briefing-tick.test.ts "P8-b4: a real imminent calendar event is grounded into the briefing's Upcoming, unioned with tasks" — P8-b4
- [Autonomy] The user can register / list / cancel a standing objective from the CLI — the actual entry point to the P5→P9 delegated-autonomy chain (the daemon ticks the same `~/.muse/objectives.json`) — `muse objectives add|list|cancel` (local mode) — `@muse/cli` commands-objectives.test.ts "add → list → cancel → list reflects through the real ~/.muse/objectives.json store" + invalid-kind hint + cancel-missing + user-scoping — P5-b1
- [Presence] The objectives daemon's autonomous actions are now P6-accountable — every met/escalated objective the daemon acts on appends a rationale-bearing entry (what / why=spec / when / result=performed / objectiveId) to the reviewable action log, so the user can audit what Muse did on their behalf unattended — `createMessagingObjectiveActuator(actionLogFile)` wired via `ServerOptions.actionLogFile` (`resolveActionLogFile` → `~/.muse/action-log.json`) — `@muse/mcp` objective-evaluator.test.ts "when actionLogFile is set, each autonomous action is appended as a reviewable rationale-bearing entry" + "no actionLogFile ⇒ unchanged behaviour" — P6-b1
- [Presence] The user can READ the accountability log from the CLI — completing P6-b1's "queryable by the user": newest-first, user-scoped (or `--user all`), `--result` / `--limit` filters, friendly empty message — `muse actions` (local mode over the same `~/.muse/action-log.json` the daemon writes) — `@muse/cli` commands-actions.test.ts "lists recorded autonomous actions newest-first with rationale" + result-filter/user-scope/limit/empty/invalid-hint — P6-b1
- [Autonomy] In one orchestration run, workers can each execute on a DIFFERENT local model — `AgentWorker.model` is an optional per-worker override the orchestrator applies on dispatch (`withSelectedWorker` replaces `input.model` with the worker's), so a fast model takes a lookup while a high-capability model takes the reasoning; a worker without an override runs byte-identically on the run-default model — `MultiAgentOrchestrator.run` (backs `muse orchestrate` + `POST /api/multi-agent/orchestrate`) — `@muse/multi-agent` multi-agent.test.ts "dispatches each worker on its own model override and runs an override-less worker on the run default — workers execute on different local models in one run (P10 s1)" (parallel run: fast→qwen3:1.7b, heavy→qwen3:8b distinct, plain→run-default) + `smoke:broad` orchestrate endpoints green (byte-identical single-model path) — P10 s1
- [Autonomy] A deterministic tier router + capacity-aware planner decides which local model each orchestration task runs on — `classifyTier` routes simple lookups (what is / define / convert / 몇) to the fast tier and anything with a reasoning signal (analyze / design / why / 비교) or no clear lookup signal to heavy (default-heavy so reasoning is never silently downgraded; reasoning-first so a both-signal task stays heavy); `planTieredRun` assigns per-tier models + parallel mode when the host holds both tiers, and collapses to the single heavy model sequentially when it cannot — failing OPEN to single-heavy if the capacity probe throws — `@muse/multi-agent/tiering.ts` (decision layer feeding the P10 s1 `AgentWorker.model` dispatch) — `@muse/multi-agent` tiering.test.ts (classifyTier fast/heavy/default-heavy/both-signal + planTieredRun parallel-per-tier / low-capacity-collapse / probe-throw-fail-open) — P10 s2+s3
- [Autonomy] `muse ask --tiered` auto-routes a single question to a fast vs high-capability local model — `routeAskTierModel` classifies the query via the P10 `classifyTier` (lookups → fast, reasoning → heavy, default-heavy) and resolves the tier models from `MUSE_FAST_MODEL` / `MUSE_HEAVY_MODEL` (each defaulting to the configured model); off by default and an explicit `--model` overrides it, so the plain ask path is byte-identical — `muse ask --tiered` (local) — `@muse/cli` commands-ask.test.ts "resolveAskTierModels / routeAskTierModel" (env fallback / lookup→fast / reasoning→heavy / ambiguous→heavy) + program.test.ts "muse ask --tiered routes the question to the fast vs heavy model and surfaces the chosen tier" (drives the real command: reasoning query → heavy model note, lookup query → fast model note) — P10 s4 (ask path)
- [Presence] `muse show` rejects a 0-byte image file with a clear "is empty (0 bytes) — nothing to render" error (exit 1) instead of emitting an empty iTerm2 OSC-1337 inline sequence (a silent no-op on iTerm/WezTerm/Ghostty) or having the OS viewer "open" an empty file — so a truncated / failed download surfaces an actionable message — `muse show <path>` (local) — `@muse/cli` program.test.ts "muse show rejects a 0-byte file with a clear error instead of emitting an empty inline sequence" (drives the real command with TERM_PROGRAM=iTerm.app; asserts the error + that NO OSC-1337 sequence was emitted + exit 1) — polish
- [Autonomy] `muse orchestrate --tiered` runs each worker on a fast vs high-capability local model chosen from its role — the server classifies each enabled spec's `description` via the P10 `classifyTier` (lookup → fast, reasoning → heavy, default-heavy) and dispatches it on the resolved tier model (`MUSE_FAST_MODEL` / `MUSE_HEAVY_MODEL`, each defaulting to the run model) via the s1 `AgentWorker.model`; absent `--tiered` every worker runs on the run-default (unchanged), and the orchestrate response now surfaces each step's `model` so the tiering is observable — `muse orchestrate run --tiered` / `POST /api/multi-agent/orchestrate {tiered:true}` — `@muse/api` multi-agent-tiered.test.ts (buildSpecWorkers: two specs → two distinct tier models in one parallel run; unrecognised role → heavy; no-tier → run-default) + resolveOrchestrateTierModels env-fallback + `@muse/cli` commands-orchestrate.test.ts "--tiered sends tiered: true" + `smoke:broad` orchestrate endpoints green — P10 s4 (orchestrate explicit path)
- [Autonomy] In ONE live orchestration run, two workers provably executed on two DISTINCT local Qwen tiers — `muse orchestrate --tiered` against a real running server + real Ollama: a lookup-role spec routed to the fast model and an analyze-role spec to the heavy model, each returning real output, the response's per-worker `model` fields differing — `POST /api/multi-agent/orchestrate {tiered:true}` (live) — `scripts/smoke-live-llm.mjs` "POST /api/multi-agent/orchestrate --tiered (live) — two workers run on two distinct local Qwen tiers" (PASS with fast=ollama/qwen3:8b heavy=ollama/qwen3.6:35b-a3b; skips when <2 local qwen models) — P10 s5 (two-tier live)
- [Autonomy] A tiered orchestration run collapses to the single high-capability model (sequential) on a host that can't hold both tiers — the orchestrate server wires `planTieredRun`'s capacity probe (`MUSE_TIER_SINGLE_MODEL_HOST` truthy ⇒ can't hold both), so a `muse orchestrate --tiered` run on a constrained host runs every worker on the heavy model sequentially instead of thrashing two large models; fail-open to single-heavy if the probe throws; default (unset) keeps the two-tier split — `POST /api/multi-agent/orchestrate {tiered:true}` (server) — `@muse/api` multi-agent-tiered.test.ts (buildTieredOrchestration: canHoldBoth=true → two tiers; false → all-heavy collapse; probe throws → fail-open collapse) + resolveTierCapacityProbe env-parse — P10 s4+s5 (collapse child; completes the parent) — completes 680
- [Presence] Muse senses real-world weather — `muse weather <location>` geocodes a place name and reports current conditions (condition / temp / feels-like / humidity / wind) via Open-Meteo (free, no API key, read-only world-sensing — no outbound-safety gate); `--json` emits the resolved location + conditions; unknown place ⇒ clear not-found error + exit 1 — `muse weather` (local) — `@muse/cli` weather.test.ts (describeWeatherCode WMO mapping incl. unknown-code; formatWeather; OpenMeteoWeatherProvider geocode + currentWeather over faked fetch; command: seeded location → answer reflects the HTTP-faked forecast; unknown place → not-found exit 1) + live open-meteo schema validated — P12 (weather provider + direct surface)
- [Anticipation] The proactive situational briefing is grounded in real weather — when a non-empty briefing fires, the daemon fetches current conditions for `MUSE_WEATHER_LOCATION` via the (Open-Meteo) `WeatherProvider` and adds a `Weather: <place>: <condition>, <temp>` line; weather is supplementary (never triggers a briefing alone) and fail-soft (a lookup error omits the line) — `runDueSituationalBriefing` weather grounding wired through `startSituationalBriefingTick` (`MUSE_WEATHER_LOCATION`) — `@muse/mcp` situational-briefing-loop.test.ts "grounds the briefing with a seeded location's (HTTP-faked) forecast" (real OpenMeteoWeatherProvider, HTTP boundary faked, over the real TelegramProvider send; + "weather alone never triggers / no wasted fetch") — P12 (briefing grounding; completes P12 with goal 688)
- [Presence] Muse resolves a person's name to a recipient — `muse contacts add|list|resolve` over `~/.muse/contacts.json` + the pure `resolveContact`: a unique name/alias match resolves to its email/handle, an exact match wins over a substring one (Bob not Bobby), and TWO matches report AMBIGUOUS (the candidates, exit 1) while no match reports not-found — never a guessed recipient (the recipient-resolution backbone for outbound-safety rule 3) — `muse contacts` (local, read-only) — `@muse/mcp` personal-contacts-store.test.ts (round-trip add/query/remove + resolve resolved/alias/exact-over-substring/ambiguous/unknown) + `@muse/cli` commands-contacts.test.ts (add→list→resolve; ambiguous → candidates + exit 1, no guessed recipient on stdout; not-found exit 1) — P13
- [Presence] PDFs dropped in the notes dir are ingested into Muse's RAG corpus — `muse notes reindex` / `muse ask` now extract `.pdf` text via `pdf-parse` (the same path as `muse read`) through `extractDocumentText`, chunk + embed it like a markdown note, so a question retrieves the relevant PDF chunk and a DECOY document is ranked out — `muse ask` / `muse notes` over a notes dir containing PDFs — `@muse/cli` commands-notes-rag.test.ts ("extractDocumentText" — parses PDF text, rejects raw-byte structure; "reindexNotes ingests PDFs alongside markdown (P14)" — a constructed PDF + decoy md reindexed via a deterministic fake embedder, the PDF chunk carries the EXTRACTED text (no `endobj`) and ranks above the decoy for a matching query) — P14 (PDF RAG ingest; live grounded-answer-over-corpus pending a local embed model)
- [Presence] Muse answers a question grounded in a real PDF, decoy excluded — LIVE: a PDF reindexed into the notes RAG with the real local `nomic-embed-text` embedder, then `muse ask` via real qwen3:8b returns an answer grounded in the PDF's figure with the PDF chunk top-ranked and an unrelated decoy document scored far lower (excluded) — `muse notes reindex` + `muse ask` (local, real Ollama) — `scripts/smoke-live-llm.mjs` "muse ask grounds an answer in a real PDF and excludes a decoy (P14)" (reindex a temp PDF+decoy, ask, assert PDF top chunk carries the extracted figure + the answer cites it; skips when the CLI isn't built or no nomic-embed-text) — completes P14 (with 692 retrieval integration + 088 muse read)
- [Presence] Muse reads + triages your Gmail inbox — `muse inbox` lists the most-recent inbox messages via `GmailEmailProvider` (Gmail REST API, Bearer access token from `MUSE_GMAIL_TOKEN`, no SDK / no new dep) behind the model-neutral `EmailProvider` abstraction, with a `summarizeInbox` triage line ("N messages, M unread" + unread subjects) and an unread marker per message; read-only (no outbound-safety gate); `--json` for structured output; missing token / auth-rejected (401/403) → clear hint, exit 1 — `muse inbox` (local) — `@muse/mcp` email-provider.test.ts (contract-faithful Gmail-HTTP fake: list+metadata parse, unread from labelIds, Bearer sent, 401 auth error, empty inbox; summarizeInbox counts + unread subjects) + `@muse/cli` commands-inbox.test.ts (triage listing / --json / auth-error exit 1 / missing-token hint) — P11 (read/triage/summarise; briefing-feed pending)
- [Anticipation] The proactive situational briefing surfaces unread email — when a non-empty brief fires AND an email provider is configured (`MUSE_GMAIL_TOKEN`), the daemon fetches recent inbox messages and adds an `Inbox: N unread — “subject” (sender), …` line via `unreadBriefingLine`; supplementary (never triggers a brief alone), fail-soft (a fetch error omits the line), and silent on a clean inbox (0 unread) — `runDueSituationalBriefing` email grounding wired through `startSituationalBriefingTick` — `@muse/mcp` situational-briefing-loop.test.ts "grounds the briefing with unread inbox items from the email provider (P11)" (contract-faithful over the real TelegramProvider send; read message excluded) + email-provider.test.ts unreadBriefingLine (≤3 named subjects + sender display-name + “+N more”; undefined when all read) — completes P11 read bullet (with 694)
- [Autonomy] Muse sends an email ONLY draft-first + confirmation-gated (outbound-safety) — `sendEmailWithApproval` resolves the recipient via `resolveContact` (ambiguous/unknown/no-email ⇒ NO send, returns candidates to clarify), builds the exact draft, runs a FAIL-CLOSED approval gate (deny / gate-throw/timeout ⇒ NO send), and only on explicit confirm does `GmailEmailProvider.send` fire (real Gmail REST `messages/send`, Bearer, base64url MIME); every outcome (sent/refused/failed) is action-logged — `muse email send --to --subject --body` — `@muse/mcp` email-send.test.ts (CONFIRM→one real send POST carrying the body + Bearer + `performed` log; DENY→0 sends; gate-throw→0 sends; AMBIGUOUS→0 sends+candidates; UNKNOWN→0; handle-only→0; contract-faithful, never a fake registry) + `@muse/cli` commands-email.test.ts (confirm/deny/ambiguous at the surface) — P11 send (completes P11)
- [Autonomy] Muse performs a state-changing web action (submit/book) ONLY confirmation-gated, never autonomous (outbound-safety) — `performWebActionWithApproval` runs a FAIL-CLOSED approval gate showing the exact action (summary + request); deny / gate-throw/timeout ⇒ NO HTTP, and only on explicit confirm does the request fire via the injected transport, every outcome action-logged (performed/refused/failed) — `muse web-action --url --summary [--method --body]` — `@muse/mcp` web-action.test.ts (CONFIRM→one real request carrying the method+body + `performed` log; DENY→0 HTTP; gate-throw→0; never-autonomous→0; contract-faithful, records the request, never a fake flag) + `@muse/cli` commands-web-action.test.ts (confirm/deny at the surface) — P15 (banking/payments out of scope)
- [Autonomy] Muse controls smart-home devices ONLY confirmation-gated (opt-in Home Assistant) — `performHomeActionWithApproval` builds a Home Assistant REST service call (`POST /api/services/<domain>/<service>`, Bearer long-lived token, `entity_id`+data body via `buildHomeAssistantServiceCall`) and routes it through the fail-closed `performWebActionWithApproval` gate: deny / absent confirm ⇒ NO call; only on confirm does the service fire, action-logged — `muse home call <domain.service> --entity <id>` (opt-in via MUSE_HOMEASSISTANT_URL/TOKEN; local REST, no SDK/dep) — `@muse/mcp` smart-home.test.ts (buildHomeAssistantServiceCall URL+entity_id body+Bearer+data-merge; performHomeActionWithApproval CONFIRM→one real HA POST + performed log, DENY→0 calls) + `@muse/cli` commands-home.test.ts (confirm/deny/malformed-service-id) — P16 (completes the P11–P16 actuator map; banking/payments out of scope)
- [Autonomy] The agent ACTS on email mid-conversation, gated — `createEmailSendTool` exposes an `email_send` execute-risk agent tool whose execute reuses the proven fail-closed `sendEmailWithApproval` (resolve recipient via `resolveContact` → draft-first approval gate → real `GmailEmailProvider.send` → action-log), so an agent run that decides to email a contact only sends after explicit confirmation; deny / ambiguous-recipient ⇒ NO send — registered into `createAgentRuntime` (runtime tool) — `@muse/mcp` email-tool.test.ts (tool definition + execute confirm/deny/ambiguous) + `@muse/api` p17-email-tool-agent-seam.test.ts (a REAL createAgentRuntime run: model emits email_send tool-call → CONFIRM fires one real Gmail send / DENY / ambiguous ⇒ 0 sends; contract-faithful, mutation-proven) — P17 first slice (conversational actuation)
- [Autonomy] The agent ACTS on the web mid-conversation, gated — `createWebActionTool` exposes a `web_action` execute-risk agent tool whose execute reuses the fail-closed `performWebActionWithApproval` (draft-first approval gate, action-logged), so an agent run that decides to submit/book only fires after explicit confirmation; deny / absent ⇒ NO request — registered into `createAgentRuntime` (runtime tool) — `@muse/mcp` web-action-tool.test.ts (definition + execute confirm/deny/missing-url) + `@muse/api` p17-web-action-tool-agent-seam.test.ts (REAL createAgentRuntime run: model emits web_action tool-call → CONFIRM fires one recorded request / DENY ⇒ 0; contract-faithful, mutation-proven) — P17 (web action as agent tool; smart-home tool pending)
- [Autonomy] The agent controls smart-home devices mid-conversation, gated — `createHomeActionTool` exposes a `home_action` execute-risk agent tool whose execute reuses the fail-closed `performHomeActionWithApproval` (Home Assistant service call, approval gate, action-logged), so an agent run that decides to e.g. turn off the lights only fires after explicit confirmation; deny / absent ⇒ NO call — registered into `createAgentRuntime` — `@muse/mcp` smart-home-tool.test.ts (definition + execute confirm/deny/malformed-service) + `@muse/api` p17-home-action-tool-agent-seam.test.ts (REAL createAgentRuntime run: model emits home_action tool-call → CONFIRM fires one HA service POST / DENY ⇒ 0; contract-faithful, mutation-proven) — P17 (smart-home agent tool — all three actuators now conversational; flips the "other actuators are gated agent tools" bullet with 707)
- [Autonomy] The gated actuators are reachable from a LIVE `muse ask` conversation — `buildActuatorTools` (apps/cli) builds the env-configured actuator MuseTools (web_action always; email_send iff MUSE_GMAIL_TOKEN; home_action iff MUSE_HOMEASSISTANT_URL+TOKEN), each carrying a clack `confirm` as its fail-closed gate, and `createMuseRuntimeAssembly` gained an `extraTools` injection so the CLI feeds them into the shared runtime registry without putting interactive gates in the headless assembly; `muse ask --with-tools --actuators` sets `localMode` (only under `--actuators`, so no other execute-risk surface is newly exposed) and the model can invoke them mid-turn — off by default, confirm shows the exact draft, deny ⇒ NO effect — `muse ask --with-tools --actuators "..."` — `@muse/cli` actuator-tools.test.ts (env→toolset selection; every actuator is execute-risk; a REAL createAgentRuntime run: model emits web_action → CONFIRM fires one recorded request / DENY ⇒ 0; mutation-proven — a gate ignoring the confirm makes DENY fire) + `@muse/autoconfigure` autoconfigure.test.ts (extraTools merged into toolRegistry) — P17 (live agent surface for conversational actuation)
- [Autonomy] `muse ask --actuators` tells you which state-changing powers are armed before the turn — `summarizeActuators(env)` reports the armed actuators (web_action always; email_send iff MUSE_GMAIL_TOKEN; home_action iff MUSE_HOMEASSISTANT_URL+TOKEN) and an actionable config hint for each unavailable one, and `formatActuatorBanner` renders a stderr banner (armed list + the confirm-safety reminder + one hint line per unavailable actuator) emitted before the agent run, so the user sees exactly what the conversation can trigger and how to arm the rest — `muse ask --with-tools --actuators "..."` (stderr banner; dog-fooded) — `@muse/cli` actuator-tools.test.ts (summarizeActuators armed/unavailable+hints for no-env / all-env / HA-needs-both; a drift-lock asserting the armed set always equals the names buildActuatorTools actually constructs; formatActuatorBanner content) — P17 (armed-state visibility hardens the live conversational-actuation surface)
- [Safety] When a channel message makes Muse attempt a risky (write/execute) tool, the in-chat approval prompt now shows the DRAFT, not just the tool name — `createChannelApprovalGate` calls `summarizeToolDraft(name, arguments)` to render a short, channel-safe summary (email_send → recipient + subject, body omitted; web_action → method + URL; home_action → service + entity; unknown → up to 3 scalar key=value pairs) so the user sees WHAT before replying to approve, satisfying outbound-safety draft-first on the channel surface; still fail-closed (read passes; risky denies + posts; posting-throws still denies) — inbound channel chat (Telegram/etc.) — `@muse/messaging` channel-approval-gate.test.ts (read-pass; risky deny+post containing recipient+subject but NOT the body; fail-closed on send-throw; summarizeToolDraft per-actuator + generic + no-args) — P1 (two-way channel; previously the gate had no direct test and leaked only the tool name)
- [Autonomy] `muse setup status` (a.k.a. `muse setup`) now reports actuator readiness — `readActuatorReadiness(env)` reports whether each gated state-changing actuator can be armed (web_action always; email_send iff MUSE_GMAIL_TOKEN; home_action iff MUSE_HOMEASSISTANT_URL+TOKEN) with status ok when any provider-backed actuator is wired, and a nextStep hinting how to enable the rest + that they're opt-in via `muse ask --with-tools --actuators`; surfaced in the SetupStatusSnapshot (shared by the CLI text render + REST/web JSON) and rendered as `actuators — email ✓/✗, web ✓, home ✓/✗` — `muse setup status` (dog-fooded with MUSE_GMAIL_TOKEN → email ✓, home ✗ + hint) — `@muse/autoconfigure` setup-status.test.ts (readActuatorReadiness: no-env info+hints / gmail→ok still-hints-home / HA-needs-both / all-configured drops nextStep) — P17 (the biggest recent capability was invisible to the setup doctor; now onboarding surfaces it)
- [Perception] `muse vision <url>` no longer silently feeds a non-image page to the model — `loadImageAsBase64`'s http(s) branch now checks the response content-type and rejects clearly-textual responses (text/*, application/json|xml|xhtml) with a clear "not an image" error, so a 200 HTML error/login/paywall page surfaces a real message instead of base64-garbage; image/*, octet-stream, and absent content-types still pass (hosts that omit the type) — `muse vision https://…` — `@muse/cli` commands-vision.test.ts (the helper had zero coverage: resolveVisionModel branches; buildOllamaVisionBody shape; formatOllamaVisionFailure 404-vs-other; loadImageAsBase64 data-URL valid/non-base64/empty/malformed, http image-bytes, non-OK status, textual-content-type REJECTED (mutation-proven), octet-stream/absent allowed, local file) — P3 (sensory input robustness; vision goal 087)
- [Reliability] A mid-stream Ollama error no longer becomes a silently truncated answer — once Ollama returns 200 and starts streaming NDJSON, a mid-generation failure (`{"error":"…"}` line: OOM, context overflow, model eviction under load) is now surfaced as a retryable `ModelProviderError` stream error event and the stream stops, instead of being parsed as a message-less chunk and dropped (which left the user a truncated reply with no error). The project's sole provider is local Qwen-via-Ollama, so this is the primary request/response path — `OllamaProvider.stream` (`@muse/model`) — `@muse/model` model.test.ts ("mid-stream error line": an `{error}` NDJSON line yields a retryable error event, stops the stream, emits no `done`; mutation-proven) + verified live: `pnpm smoke:live` EXIT=0 real qwen3.6:35b-a3b/qwen3:8b round-trip still green — P0 (core LLM-path robustness)
- [Reliability] Quiet-hours config accepts the natural `HH:MM` form, so it no longer silently disables itself on the common `22:00-07:00` input — `parseQuietHours` (apps/api reminder-tick, shared by all six quiet-hours-gated daemons: reminder/proactive/pattern/situational-briefing/objectives/followup) previously matched only bare hours (`22-7`) and returned undefined for `22:00-07:00`, turning quiet hours OFF without warning (3am notifications); it now also parses `HH:MM-HH:MM` (minutes validated 0-59 then rounded down to the hour-granular window) — `MUSE_REMINDER_QUIET_HOURS=22:00-07:00` (and the per-daemon MUSE_*_QUIET_HOURS vars) — `@muse/api` reminder-tick.test.ts (parseQuietHours accepts 22:00-07:00 / 23:30-06:15 / 9:05-17:45; rejects :60 minutes and single-digit minutes; same-hour HH:MM ambiguous→undefined; mutation-proven: the old hour-only regex fails these) — P8 (proactive quiet-hours robustness)
- [Safety] When a remote channel message makes Muse attempt a risky (write/execute) tool, the fail-closed refusal is now RECORDED to the action log (visible via `muse actions`), closing an outbound-safety gap (the contract requires every action — sent OR refused — to leave a rationale-bearing trail; channel-gate denials previously left none) — `createChannelApprovalGate` gained an injected `recordRefusal` hook (fail-soft: a throwing recorder never flips the deny; @muse/messaging stays free of an @muse/mcp dep), wired in apps/api server.ts via `createChannelRefusalRecorder` → `appendActionLog` a `refused` entry (what = `Muse wanted to run "<tool>" (<risk>) — <draft>`, why = inbound-message + awaiting in-chat approval, userId = providerId:source) — inbound channel chat (Telegram/etc.) → `muse actions` — `@muse/messaging` channel-approval-gate.test.ts (records on risky deny w/ tool/risk/draft/userId, not on read, stays fail-closed when the recorder throws; mutation-proven) + `@muse/api` channel-refusal-recorder.test.ts (appends a refused entry; userId fallback; delegates to the injected append) — P17 (audit foundation for the remote approve-completion round-trip, still pending)
- [Accountability] `muse actions` no longer looks empty when the only actions are channel-triggered — channel-refused tools (719) land under a `provider:source` bucket (e.g. `telegram:42`), but the command defaults to `--user local`, so a plain `muse actions` showed "No recorded actions." while remote refusals existed elsewhere. Now, when the scoped bucket is empty AND other buckets hold entries, the empty message names the other bucket(s) and points at `--user all` (e.g. "No recorded actions for 'local' — 1 under other bucket(s) (telegram:42); try `--user all`"); a fully-empty log still prints the plain message, and a `--result` filter that merely empties a non-empty bucket does NOT mis-suggest — `muse actions` — `@muse/cli` commands-actions.test.ts (cross-bucket hint surfaces telegram:42 + --user all reveals it; fully-empty unchanged; --result-emptied bucket does not mis-suggest) — P6 (accountability read surface)
- [Safety] The in-chat approval prompt no longer makes a false promise — it previously told the user to "reply to approve before it can run", but no approve-completion round-trip exists (replying does nothing), a misleading affordance on a fail-closed safety surface. The prompt now states the truth: the risky tool was NOT executed, Muse won't run a state-changing action from a chat message on its own, it needs explicit approval, and it's been logged for review (719) — `createChannelApprovalGate` posted text — inbound channel chat — `@muse/messaging` channel-approval-gate.test.ts (asserts "NOT executed" present + "reply to approve" absent) + `@muse/messaging` test/ HTTP-path + `@muse/api` p1-seam.test.ts (updated to the honest wording) — P1 (channel two-way; honesty of the gate prompt)
- [Perception] `muse calendar import <file.ics>` now lands TZID events at the correct time instead of hours off — the ICS parser previously ignored a `DTSTART;TZID=America/New_York:…` param and read the wall-clock as UTC (a 9am NY meeting imported as 9am UTC = 4am NY). It now converts an IANA-zoned wall-clock to the real UTC instant via the built-in `Intl` (zero dep), DST-aware (two refinement passes); `Z`-suffixed values stay UTC and an unknown zone falls back to the prior UTC reading (no regression, no dropped events) — `muse calendar import` — `@muse/cli` ics-parser.test.ts (America/New_York winter EST→14:00Z + summer EDT→13:00Z DST; Asia/Seoul→00:00Z; quoted TZID; Z wins over TZID; unknown zone → UTC fallback; mutation-proven) — P0 (calendar perception accuracy)
- [Knowledge] `muse notes reindex` no longer produces oversized chunks that overflow the embedder — `chunkText` packed paragraphs to `<= chunkChars` but let a SINGLE paragraph larger than `chunkChars` (a wall of text, code block, or minified blob) through as one giant chunk, which exceeds nomic-embed-text's context and gets silently truncated, dropping retrieval recall for everything past the cutoff. It now hard-wraps an oversized paragraph first (preferring the last whitespace in the window so words aren't split; hard-cutting an unbreakable run), guaranteeing every chunk `<= chunkChars` with no content lost — `muse notes reindex` / `muse ask` grounding — `@muse/cli` commands-notes-rag.test.ts (chunkText: small paras packed within bound; oversized para split at word boundary, every chunk ≤ chunkChars, no content lost; unbreakable 500-char run hard-cut into ceil(500/80) pieces; empty/whitespace → []; mutation-proven) — P14 (notes/PDF RAG grounding quality)
- [Reliability] A mid-stream error from an OpenAI-compatible backend no longer becomes a silently truncated answer — `parseOpenAIStream` (the SSE stream shared by every `OpenAICompatibleProvider`: OpenRouter / vLLM / LM Studio / a Qwen served over OpenAI-compat) read a post-200 `{"error":{...}}` chunk as a delta-less message and dropped it, ending with a partial/empty reply and no error. It now surfaces the chunk as a retryable `ModelProviderError` stream error event and stops (string or `{message}` error shapes; mirrors the native-Ollama mid-stream fix 716) — `OpenAICompatibleProvider.stream` (`@muse/model`) — `@muse/model` model.test.ts ("surfaces a mid-stream `{error}` SSE chunk": the error event carries the message + retryable, no `done` emitted after; mutation-proven) — P0 (LLM-path robustness across compat backends)
- [Knowledge] The `markdown_table` tool no longer renders a nested object/array cell as useless `[object Object]` — `formatMarkdownTableCell` used `String(value)`, so a row value like `{lat:1,lng:2}` became `[object Object]` (and an array lost structure as a comma-join) in a table the model shows the user; it now renders a nested object/array cell as compact JSON (`{"lat":1,"lng":2}`, `["a","b"]`), primitives unchanged, pipes/newlines still escaped — `markdown_table` agent tool — `@muse/tools` tools.test.ts (nested object→`{"lat":1,"lng":2}` + array→`["a","b"]`, no `[object Object]`; mutation-proven; existing primitive/escape/truncation cases unchanged) — P0 (LLM-facing formatting tool quality)
- [Autonomy] `muse approvals` shows the live worklist of channel actions awaiting your approval — when an inbound channel message (Telegram/etc.) makes the agent attempt a risky tool, the fail-closed gate now ALSO persists it to a pending-approval store (structured tool + arguments + a 24h TTL), distinct from the immutable action-log audit: `muse approvals` lists only the un-expired, un-actioned items (newest first) and `muse approvals clear <id>` dismisses one (auto-pruning expired). This is the structured substrate the approve-completion round-trip will re-run from — `muse approvals` / `muse approvals clear <id>` — `@muse/messaging` pending-approval-store.test.ts (record/list-unexpired/scope/clear/tolerant-read; expiry mutation-proven) + `@muse/api` channel-pending-recorder.test.ts (gate refusal → pending entry w/ args + TTL) + `@muse/cli` commands-approvals.test.ts (list newest-first / empty / expired-hidden / clear+unknown-id-exit-1 / --json) — P17 (remote conversational-actuation foundation; the re-run completion bullet stays open)
- [Autonomy] `muse approvals approve <id>` completes the remote-actuation round-trip — it re-runs a pending channel approval's exact gated tool (the one the inbound-channel gate refused, 728) through the SAME proven actuator orchestration (709 `buildActuatorTools`: email_send/web_action/home_action) with a clack confirm showing the draft, then clears the entry on success so a second approve can't re-fire (replay-guard); a declined confirm leaves it pending, an unknown/expired id is a no-op, and a non-actuator tool is reported rather than run — `muse approvals approve <id>` — `@muse/cli` commands-approvals.test.ts (`approvePendingApproval`: CONFIRM→one request fires + cleared + replay→not-found; DENY→no request + stays pending; unknown/expired→not-found; non-actuator→no-tool; clear-on-success mutation-proven) — P17 (remote conversational actuation: CLI-approve completion; the in-chat-reply auto-path stays open)
- [Autonomy] A bare "yes"/"approve" reply on a channel now gets an actionable acknowledgement instead of a confused agent turn — the inbound runner detects an approval reply (`isApprovalReply`, conservative whole-message match incl. Korean) and, when a pending approval exists for that exact channel (728 store, scoped), replies "<tool>: <draft> is awaiting your approval — `muse approvals approve <id>` / `muse approvals clear <id>`" rather than running the LLM on the word "yes"; a non-approval message or no-pending falls through to the normal agent turn — inbound channel chat (Telegram/etc.) — `@muse/messaging` is-approval-reply.test.ts (affirmations incl. emoji/Korean; rejects "yes but…"/"yesterday"/sentences; mutation-proven whole-message strictness) + `@muse/api` inbound-approval-handler.test.ts (ack on approval+pending w/ approve/clear cmds; undefined for non-approval / no-pending / wrong-channel-scope / expired) — P17 (in-chat approval bridge; the in-chat AUTO-execution remains the open bullet — needs server-side actuator registration)
- [Autonomy] OPT-IN in-chat approval completion closes the remote round-trip: with `MUSE_INBOUND_AUTO_APPROVE=true`, an inbound channel reply ("yes"/"approve") to the draft-bearing prompt RE-RUNS the pending gated tool in-chat (via a shared `runActuatorByName` dispatcher in @muse/mcp → the proven email_send/web_action/home_action `*WithApproval` orchestrations, auto-approve since the reply IS the confirm of the already-shown draft), reports the result, and clears it on success (replay-guard) — so a whole Telegram/chat conversation can trigger a gated actuator end-to-end. Default OFF (completion stays on the deliberate `muse approvals approve` CLI confirm); only a SINGLE un-expired pending auto-runs (multiple → lists ids, never guesses) — inbound channel chat (opt-in) — `@muse/mcp` run-actuator-by-name.test.ts (each actuator approve→fires/deny→declined/unavailable/unknown-tool, HTTP-faked) + `@muse/api` inbound-approval-handler.test.ts (autoRun single→runs+clears+replay-empty / failed→stays+CLI-hint / multiple→ambiguous-no-run; mutation-proven single-pending guard) — P17 (remote conversational actuation: round-trip complete in-chat, opt-in)
- [Reliability] `muse today` lists due reminders in true chronological order — the local-briefing reminder fetch sorted by `dueAt.localeCompare` (lexicographic ISO), which mis-orders mixed-precision / timezone-offset dueAt strings (e.g. a `…-05:00` reminder that is actually later sorting before an earlier `…Z` one), so the morning briefing could surface a later reminder as the most imminent. It now sorts by parsed instant via the store's canonical `compareRemindersByDueAt` (same order as `muse reminders`) — `muse today` / `muse today --local` — `@muse/cli` commands-today.test.ts (readDueReminders orders a `-05:00` offset reminder after an earlier `Z` one; mutation-proven — the lexicographic sort inverts it) — P8 (proactive briefing ordering)
- [Reliability] The `/api/today` briefing (the default `muse today` path) + the CLI followup list now order reminders AND followups by parsed instant, completing the 732 fix — `today-routes.ts` (API) sorted reminders by `dueAt.localeCompare` and followups by `scheduledFor.localeCompare`, and `commands-today.ts` still sorted followups lexicographically (732 fixed only its reminders); all four now reuse the canonical `compareRemindersByDueAt` / `compareFollowupsByScheduledFor` (Date.parse instants), so a mixed-precision / timezone-offset timestamp can't surface a later item as more imminent — `muse today` (remote + --local) / `GET /api/today` — `@muse/api` server.today.test.ts (route returns reminders + followups in instant order for a `-05:00`-offset case; mutation-proven) + `@muse/cli` commands-today.test.ts (readDueFollowups instant order) — P8 (proactive briefing ordering, API + CLI complete)
- [Reliability] `muse remind list` and `muse followup list` — the user's direct window into each queue — now order by parsed instant, finishing the 732 bug class (the last two `localeCompare`-on-a-timestamp sites): `commands-remind.ts` sorted `--local` reminders by `dueAt.localeCompare` and `commands-followup.ts` sorted followups by `scheduledFor.localeCompare`, so a mixed-precision / timezone-offset value (`…-05:00` later instant vs earlier `…Z`) listed a later item above a more-imminent one; both now reuse the canonical `compareRemindersByDueAt` / `compareFollowupsByScheduledFor` (Date.parse instants, `createdAt`-desc/id-asc tie-break) so every reminder/followup ordering surface agrees — `muse remind list --local` / `muse followup list` — `@muse/cli` commands-remind.test.ts + commands-followup.test.ts (each lists a `-05:00`-offset item in real-instant order via the real CLI program + temp file; both mutation-proven — the lexicographic sort inverts them) — P8 (proactive briefing ordering, list surfaces complete)
- [Reliability] Scheduled-job templates render midnight correctly — `dateParts` (scheduler) formatted the hour with `hour12: false`, which in en-US selects the h24 cycle and renders midnight as `24`, so a job firing at local midnight produced `{{time}}` → `24:00:00` and `{{datetime}}` → `2026-05-22 24:00:00` (ambiguous — 24:00 reads as the NEXT day) in the MCP tool args (`resolveTemplateJson`) and agent prompts (`renderTemplateVariables`) it dispatches. Now uses `hourCycle: "h23"` (0–23) so midnight is `00:00:00` — scheduled `mcp_tool`/`agent` jobs with `{{time}}`/`{{datetime}}` in their toolArguments/prompt — `@muse/scheduler` scheduler-helpers.test.ts (midnight {{time}}→00:00:00, {{datetime}}→…00:00:00, Asia/Seoul local-midnight case; mutation-proven — hour12:false yields the literal 24:00:00) — P-scheduler (job template correctness)
- [Reliability] Re-injecting a context section across multi-turn runs no longer silently drops the agent's other context — `appendSystemSection` (the shared primitive every transform uses: active-context/inbox/episodic/user-memory/prompt-layers/exemplars) replaced a previous same-marker block with `split(marker)[0]`, which kept only the text BEFORE the marker and re-appended at the end, discarding every section that came AFTER it (a threaded session re-injecting active-context dropped the inbox/episodic blocks following it). Now strips ONLY that marker's own block (marker→next-marker/end) and preserves the rest — any agent run that re-injects memory across turns (channel-threaded sessions, multi-step loops) — `@muse/agent-core` system-memory-helpers.test.ts (re-apply an earlier section preserves the later one; mutation-proven — split(marker)[0] drops it) — P0 (cross-session context integrity)
- [Reliability] MCP server auto-reconnect now backs off correctly instead of hammering a down server forever — `McpManager.reconnect()` reset `reconnectAttempts` to 0 before each retry, so a failed reconnect's `scheduleReconnect` always re-set attempts to 1: the exponential backoff never grew past `initialDelayMs` and the `maxAttempts` terminal guard (`attempts > maxAttempts`) never tripped, retrying a permanently-dead MCP server indefinitely at the fastest interval. It now carries the accumulated attempt count into the interim snapshot so backoff grows (100→200→400ms…) and goes terminal at maxAttempts; a successful reconnect still resets to 0 — any configured MCP server with `reconnect.enabled` — `@muse/mcp` mcp.test.ts (repeated-failure backoff grows 1→2→3 then terminal at maxAttempts; mutation-proven) — P-mcp (transport resilience)
- [Reliability] The agent's working-hours / active-context awareness treats midnight as hour 0, not 24 — `formatCurrentTime` (agent-core time-helpers) formatted localHour with `hour12: false`, which in en-US picks the h24 cycle and yields `localHour = 24` at midnight; `isWorkingHours` then mis-judged any window starting at hour 0 (e.g. "0-8": `24 >= 0 && 24 < 8` → false), so 00:00–00:59 was wrongly reported OUTSIDE the user's active hours. Now uses `hourCycle: "h23"` (0–23) — same fix class as 735 in a different file — any agent turn that injects the [Active Context] working-hours signal — `@muse/agent-core` time-helpers.test.ts (localHour 0 at UTC + Asia/Seoul midnight; isWorkingHours(midnight,{start:0,end:8})===true; mutation-proven) — P8 (ambient time awareness)
- [Reliability] A typo in `MUSE_RESPONSE_LOCALES` no longer silently disables locale-gated response filters — `responseLocales` (autoconfigure) defaulted to both locales only when the var was UNSET; a set-but-unrecognized value (e.g. "english", "fr,de") parsed to a non-empty list whose entries all filtered out, yielding an EMPTY set that turned off both the casual-lure-strip and greeting-strip filters and flipped the redaction default to Korean — all silently. It now falls back to `{ko,en}` whenever no recognized locale resolves (treats a typo like "unset") — any runtime configured via `MUSE_RESPONSE_LOCALES` — `@muse/autoconfigure` response-filters.test.ts (unrecognized values fall back to both; valid-among-invalid kept; mutation-proven) — P-config (response-filter robustness)
- [Reliability] Telegram replies in MarkdownV2/HTML parse_mode near the 4096-char limit are no longer dropped whole — `TelegramProvider.send` clamped to 4096 BEFORE escaping, but escaping expands the body (MarkdownV2 `\`-escapes → 2x, HTML `&`→`&amp;` → 5x), so a near-limit special-char-dense reply escaped to >4096 and Telegram 400'd the entire sendMessage (the exact drop the clamp was meant to prevent; the pre-escape validate guard missed it too). New `clampForTelegram` clamps the SOURCE so the ESCAPED transmitted text stays ≤4096 (pass-through when it already fits, else truncate by the worst-case expansion factor) — Telegram replies with `MUSE_TELEGRAM_PARSE_MODE` set — `@muse/messaging` messaging.test.ts (MarkdownV2/HTML dense bodies escape to ≤4096, no dangling half-escape; mutation-proven) — P1 (channel delivery robustness)
- [Reliability] `muse scheduler next` orders its upcoming preview (scheduler jobs + pending reminders, merged) by parsed instant, not lexicographic `when` — it sorted `when` (a job's nextRunAt OR a reminder's free-form dueAt) with `localeCompare` then `.slice(limit)`, so a `…-05:00` dueAt whose real instant is later sorted before an earlier `…Z` one AND the slice could drop a genuinely-sooner item; now uses `comparePreviewEntriesByWhen` (Date.parse instants, string fallback, label tiebreak). Completes the lexicographic-timestamp-sort class (732/733/734) — `muse scheduler next` — `@muse/cli` commands-scheduler-setup.test.ts (offset reminder + mixed job/reminder instant order; mutation-proven) — P8 (scheduled-preview ordering)
- [Reliability] macOS calendar all-day events read back as all-day, not timed — `MacOsCalendarProvider`'s AppleScript list query never emitted the all-day flag and `parseListOutput` hardcoded `allDay: false`, so every macOS event (including ones created all-day) surfaced as timed with spurious clock-time semantics in `muse calendar`/`muse today` (Google + CalDAV detect all-day correctly; macOS was the outlier). The list query now emits `allday event of evt` as a 6th field and the parser reads it (legacy output without the field stays not-all-day) — `muse calendar`/`muse today` on macOS — `@muse/calendar` calendar.test.ts (fake-osascript timed vs all-day records parse allDay correctly; mutation-proven) — P-calendar (all-day fidelity)
- [Reliability] `muse cost daily` / `muse cost top` against Postgres no longer shuffle same-day/same-cost rows across reloads — `KyselyTokenCostQuery` SQL lacked the final `model`/`run_id` ORDER BY tiebreak that `InMemoryTokenCostQuery` documents + unit-tests, so under the Qwen-only $0-cost mandate (every row ties on cost) production returned rows in Postgres's arbitrary aggregate order while the in-memory path was stable. Both now agree: `daily` → cost DESC then model ASC, `topExpensive` → cost DESC, tokens DESC then run_id ASC — `muse cost` (Postgres backend) — `@muse/observability` token-cost-postgres.test.ts (gated `MUSE_DB_POSTGRES_TEST=1`, testcontainers Postgres: same-cost rows return model/runId ASC) — P-observability (cost-query determinism, InMemory↔Kysely parity)
- [Reliability] A typo'd `MUSE_FOLLOWUP_MAX_PER_TICK` / `MUSE_OBJECTIVES_MAX_PER_TICK` no longer silently disables the proactive firing loops — `runDueFollowups`/`runDueObjectives` capped per-tick work with `Math.max(1, options.maxPerTick ?? DEFAULT)`, but `??` doesn't catch NaN and the tick-daemon parses the cap via raw `Number(env)`, so a non-numeric knob (`"5x"`, `"ten"`) became NaN → `Math.max(1,NaN)=NaN` → `.slice(0,NaN)=[]` → zero followups/objectives fired every tick, silently, forever. Both loops (and the objective loop's `maxAttempts`) now fall back to the default for non-finite caps, matching the scheduler's `clampInterval` guard — followup/objective daemons under any `MUSE_*_MAX_PER_TICK` value — `@muse/mcp` mcp.test.ts (maxPerTick:NaN fires the default cap, not zero; mutation-proven) — P-proactive (daemon robustness)
- [Reliability] Typo'd `MUSE_PROACTIVE_PATTERN_*` knobs no longer silently disable or spam proactive pattern notices — `selectFireablePatterns` (@muse/memory) read cooldownMs/minConfidence/maxPerTick with `?? DEFAULT`, which doesn't catch NaN, and the daemon parses them via raw `Number(env)`; a non-numeric value became NaN → `slice(0,NaN)`=[] fired zero (maxPerTick), or `confidence < NaN`/`elapsed < NaN` (always false) removed the confidence floor + cooldown → spam. All three now fall back to defaults for non-finite values (matching 746 + the context-ref store guard) — proactive pattern daemon under any `MUSE_PROACTIVE_PATTERN_*` value — `@muse/memory` pattern-orchestration.test.ts (maxPerTick:NaN fires default cap not zero; cooldownMs:NaN keeps the 24h cooldown; mutation-proven) — P-proactive (pattern-firing robustness)
- [Reliability] Korean greetings are now recognized as casual chit-chat, so Muse doesn't staple a `[Sources]` block onto a reply to "안녕"/"고마워"/"하이" — `isCasualPromptText` matched greetings with `^(안녕|…|하이)\b`, but `\b` is an ASCII word boundary that never matches after a Hangul (non-`\w`) char, so every Korean greeting was a dead branch (only the English thanks + unanchored gratitude substrings worked). Now uses a Unicode-aware boundary `(?![\p{L}\p{N}])` under the `u` flag — matches `안녕`/`네`/`하이`/`thanks` but not `네이버`/`thanksgiving` — any casual Korean greeting to Muse — `@muse/agent-core` is-casual-prompt-text.test.ts (Hangul greetings casual, no false-positive on 네이버; mutation-proven) — P-presence (Korean casual-conversation UX)
- [Reliability] Structured-output (`response_format: json`) recovers the valid JSON even when a small model wraps it in a bracketed prose preamble — `normalizeStructuredOutput` took only the first balanced block from the earliest `{`/`[` opener, so a reply like `see [details below]: {"ok":true}` extracted the prose `[details below]` (fails JSON.parse) and silently discarded the real `{"ok":true}`. It now tries each balanced block in order and returns the first that parses (valid early values still win) — `muse ask --json` / any structured-output turn on a local Qwen model — `@muse/policy` structured-output.test.ts (bracket-preamble recovers the object; first-valid-block still preferred; mutation-proven) — P-reach (structured output robustness on small models)
- [Perception] Muse can be wired to the user's REAL logged-in Chrome in ONE call — `createChromeDevToolsMcpServer()` presets the open-source Chrome DevTools MCP (Apache-2.0; `npx chrome-devtools-mcp@latest`, attaches via `--browser-url`, default `:9222`), and the projected page-snapshot tool reaches the agent tool surface (`toMuseTools()` → `chrome-devtools.take_snapshot`) returning grounded LIVE-page content — `@muse/mcp` McpManager — chrome-devtools-mcp.test.ts (preset config + default-policy validate + contract-faithful register→connect→execute returns the live snapshot; allowlist-deny → no tools; mutation-proven default port) — P18 read-first (slice 1: connector + tool projection; the agent-run grounded answer flips the bullet)
- [Perception] The agent ANSWERS grounded in the user's live Chrome page — an end-to-end `AgentRuntime.run()` invokes the projected `chrome-devtools.take_snapshot` MCP tool, the live-page snapshot flows back into the model input, and the final answer is grounded in it (not a cached snippet) — `@muse/autoconfigure` McpManager.toMuseTools()→ToolRegistry→AgentRuntime — chrome-devtools-agent-run.test.ts (agent runs the tool + grounds the answer in the live content; mutation-proven against the real `createMcpMuseTool` forward path) — P18 read-first (FLIPPED: slice 2 completes the bullet)
- [Safety] A state-changing action in the user's REAL logged-in Chrome is fail-close approval-gated draft-first — `chromeDevToolsToolRisk` classifies browser tools fail-close (unknown/`fill_form`/`click`→write, `evaluate_script`/`upload_file`→execute, only pure-observation→read) and `withChromeDevToolsRisk` re-stamps the projected MCP tools so the AgentRuntime `toolApprovalGate` fires; a denied OR throwing (timeout) gate means the form is NEVER submitted in the browser, while read perception stays ungated — `@muse/autoconfigure` AgentRuntime + McpManager — chrome-devtools-gated-action.test.ts (deny→no callTool + gate saw the draft; failing-gate→no callTool; take_snapshot ungated) + `@muse/mcp` chrome-devtools-mcp.test.ts (classifier + withChromeDevToolsRisk; mutation-proven) — P18 web-action (FLIPPED)
- [Reliability] The weather actuator recovers from transient failures instead of crashing the lookup / dropping the proactive briefing — `fetchWithRetry` retries 429 (rate-limit) + 5xx + network-reject with exponential backoff (fail-fast on permanent 4xx), wired into `OpenMeteoWeatherProvider.geocode` + `currentWeather` — `muse weather` / proactive briefing — `@muse/mcp` weather-retry.test.ts (contract-faithful fake fetch: 503,503→200 recovers in 3 calls; 429 retried; 404 fails fast; network-reject retried then rethrown; geocode/currentWeather recover from transient 503/502; mutation-proven) — P19 actuator-hardening (FLIPPED; weather is slice 1, other actuators follow)
- [Knowledge] The agent grounds an answer in a MULTI-document personal corpus and CITES the source — `rankKnowledgeChunks` cosine-ranks `{source,text}` chunks across sources (drops sub-threshold so no fabricated citation), `renderKnowledgeMatches` labels each passage `[source]`, and the read-only `knowledge_search` tool lets the agent retrieve + cite — `@muse/agent-core` AgentRuntime + knowledge_search tool — knowledge-recall-agent.test.ts (rank ordering/threshold; agent-run e2e: answers "peanuts and shellfish" AND cites `notes/health.md`, never the unrelated insurance.pdf; mutation-proven sort) — P20 knowledge (slice 1: RAG+citation engine; live notes/doc-store corpus assembly flips the bullet)
- [Knowledge] The agent answers from the user's LIVE multi-document notes corpus and cites the real source — `assembleKnowledgeCorpus` reads every note from a live `NotesProvider` into `notes/<id>` chunks + merges ingested-doc chunks (`docs/<name>`), feeding `knowledge_search` — `@muse/autoconfigure` (LocalDirNotesProvider → corpus → AgentRuntime) — knowledge-corpus-live.test.ts (real temp-dir notes provider: assembles health.md/projects.md + merged insurance.pdf, ignores non-notes, maxNotes; agent-run e2e answers "peanuts and shellfish" AND cites `notes/health.md`; mutation-proven source label) — P20 knowledge (FLIPPED: live notes store wired)
- [Perception] A continuous ambient signal drives a proactive notice with NO explicit invoke — `runAmbientNoticeTick` reads an ambient signal source (frontmost app / window / selected / clipboard), `deriveAmbientNotices` matches user rules (ALL named fields, case-insensitive substring), and the not-yet-fired notices are delivered through the real `ProactiveNoticeSink` (fire-once dedupe, fail-soft) — `@muse/mcp` ambient tick → ProactiveNoticeSink — ambient-notice-loop.test.ts (simulated active-window signal → real sink delivery; ALL-fields matching; dedupe; throwing-source fail-soft; mutation-proven `.every`) — P20 perception (FLIPPED)
- [Knowledge] `knowledge_search` is now REACHABLE in a real `muse ask --with-tools` run over the user's live notes — `createNotesKnowledgeSearchTool` (lazy per-query corpus from `notesRegistry.primary()` + Ollama embed) is wired into `createMuseRuntimeAssembly`'s tool registry, opt-in via `MUSE_KNOWLEDGE_SEARCH_ENABLED` — `@muse/autoconfigure` createMuseRuntimeAssembly — knowledge-search-wiring.test.ts (live-notes execute returns cited passage + picks up a freshly-added note; assembly exposes the tool when enabled / hidden by default; mutation-proven query plumbing) — P20 knowledge (production wiring: engine→reachable)
- [Reliability] Email inbox reads survive transient failures while sends never double-send — shared `fetchWithRetry` (extracted from weather) backs `GmailEmailProvider.get()` (listRecent / inbox triage) so a transient 429/5xx is retried, while `sendEmail` stays single-shot (a retried POST would double-deliver) — `muse inbox` / proactive briefing — `@muse/mcp` email-provider-retry.test.ts (read recovers from 503 in 3 calls; exhaustion surfaces a clear error; send 503 throws after exactly ONE call; mutation-proven) — P19 actuator-hardening (email, slice 2)
- [Knowledge] Long notes / ingested docs are chunked into passages so RAG retrieves + CITES the relevant passage instead of truncating the document — `chunkText` (paragraph-preferring, hard-splits oversize) feeds `assembleKnowledgeCorpus`, emitting `notes/<id>#n` chunks for long notes (short notes stay `notes/<id>`) — `knowledge_search` — `@muse/agent-core` knowledge-chunking.test.ts + `@muse/autoconfigure` knowledge-chunking-live.test.ts (long note → #1/#2; e2e cites `notes/long.md#2` with the fact the old truncate dropped; mutation-proven) — P20 knowledge (chunking deepening)
- [Perception] A real, dog-food-able ambient source feeds the proactive-notice tick — `FileAmbientSignalSource` reads `~/.muse/ambient.json` (written by a zero-dep OS helper) into the signal fields, `parseAmbientNoticeRules` parses user rules (dropping pattern-less/invalid), and a real file drives a proactive notice end-to-end via the sink — `@muse/mcp` runAmbientNoticeTick — ambient-file-source.test.ts (file→signal + fail-open; rules parse/drop-invalid; real file → notice delivered; mutation-proven) — P20 perception (real-source wiring)
- [Perception] Continuous ambient perception fires once on the rising edge and re-arms when the context clears (no per-tick spam, but re-notifies a recurring context) — `createAmbientNoticeRunner` is a stateful edge-triggered runner delivering through the real `ProactiveNoticeSink` — `@muse/mcp` runner.tick() — ambient-notice-runner.test.ts (rising-edge fire / steady-state quiet / clear→re-arm→re-fire; fail-soft; mutation-proven edge guard) — P20 perception (continuous-dedup deepening)
- [Perception] The API server proactively notifies from ambient context with NO user invoke — `startAmbientDaemonIfConfigured` (env-gated `MUSE_AMBIENT_*`) ticks `FileAmbientSignalSource` → edge-triggered `createAmbientNoticeRunner` → messaging registry, so a real `~/.muse/ambient.json` matching a rule delivers a message to the configured provider/destination — `@muse/api` server tick daemon — ambient-tick.test.ts (real file → tickOnce → capturing provider receives "Standup at 14:00", edge-dedup on 2nd tick; env-gated registration; mutation-proven rules gate) — P20 perception (production daemon, end-to-end)
- [Knowledge] knowledge_search spans the user's open TASKS as well as notes — `assembleKnowledgeCorpus` pulls open tasks (title + notes) as `task/<id>` chunks (done excluded), and the assembly wires `tasksRegistry.primary()` into the tool, so a semantic query finds + cites a task fact ("Acme contract due Friday") alongside notes — `@muse/autoconfigure` knowledge_search — knowledge-tasks-source.test.ts (real LocalFileTasksProvider: open-only chunks, done excluded; e2e cites `task/t1`; mutation-proven open filter) — P20 knowledge (multi-source corpus: notes + tasks)
- [Knowledge] knowledge_search now spans recent+upcoming CALENDAR events too (notes + tasks + calendar) — `assembleKnowledgeCorpus` pulls events in a `[-7d,+30d]` window as `event/<id>` chunks (title @ location on date + notes), windowed to avoid noise; the assembly wires `calendarRegistry` — `@muse/autoconfigure` knowledge_search — knowledge-calendar-source.test.ts (windowed corpus excludes out-of-window events; e2e cites `event/evX`; mutation-proven window) — P20 knowledge (multi-source corpus: notes + tasks + calendar)
- [Knowledge] knowledge_search spans CONTACTS too — completing the unified personal corpus (notes + tasks + calendar + contacts) — `assembleKnowledgeCorpus` emits each contact as a `contact/<id>` chunk (name/email/handle/aliases) and the assembly wires `queryContacts(resolveContactsFile)`, so one semantic query answers + cites a person, deadline, meeting, or note from one tool — `@muse/autoconfigure` knowledge_search — knowledge-contacts-source.test.ts (real contacts store: contact/c1 chunk; e2e cites `contact/c1` bob@acme.com; mutation-proven source label) — P20 knowledge (unified corpus: notes + tasks + calendar + contacts)
- [Knowledge] knowledge_search no longer re-embeds the whole corpus on every query — `createCachingEmbedder` memoizes text→vector (dedupes concurrent calls, evicts failed embeds so a transient Ollama blip isn't cached, bounded FIFO), wrapping the Ollama embedder in the assembly, so a stable corpus is embedded ONCE and repeat queries embed only the query + new chunks — `@muse/agent-core` knowledge_search — caching-embedder.test.ts (embed-once + cache-hit / concurrent-dedupe / FIFO-evict / failure-not-cached; mutation-proven) — P20 knowledge (responsiveness)
- [Safety] chrome-devtools MCP tools are risk-restamped in the REAL assembly — `createMuseRuntimeAssembly` wraps the projected MCP tools with `withChromeDevToolsRisk`, so a chrome-devtools server connected via `~/.muse/mcp.json` has its state-changing fill/submit gated fail-close (not the external server's untrusted "read" default) instead of running ungated in the user's logged-in browser — `@muse/autoconfigure` toolRegistry — chrome-devtools-assembly-wiring.test.ts (fake connector through the real assembly: `fill_form`→write, `take_snapshot`→read; mutation-proven) — P18 web-action (assembly-wiring security gap closed)
- [Reach] Web control of the user's real logged-in Chrome is turnkey — `MUSE_CHROME_DEVTOOLS_ENABLED=true` auto-registers the Chrome DevTools MCP preset (auto-connect; `--browser-url` default `:9222` or `MUSE_CHROME_DEVTOOLS_BROWSER_URL`) with NO hand-written mcp.json, and combined with the assembly's fail-close risk re-stamp (770) it's reachable AND gated — `@muse/autoconfigure` assembleMcpStack — mcp-stack-chrome-devtools.test.ts (enabled→preset auto-connect entry + custom URL; opt-in default off; mutation-proven gate) — P18 web-control (turnkey wiring)
- [Anticipation] The situational briefing proactively surfaces a related note/task for what's next — `runDueSituationalBriefing` calls an injected `relatedKnowledge` enricher with the TOP imminent item's title and adds a `Related: [source] …` line (supplementary, fail-soft, never triggers a brief alone), joining imminent-context with the personal corpus — `@muse/mcp` situational briefing — situational-briefing-related.test.ts (compose Related line; loop calls enricher with top title + delivers the line via real registry; throwing enricher fail-soft; mutation-proven) — P20 (anticipation × knowledge join)
- [Anticipation] The running briefing daemon proactively surfaces a related note/task/event/contact for what's next — `createKnowledgeEnricher` (unified live corpus + cached Ollama embed) is injected into the situational-briefing daemon (env-gated `MUSE_BRIEFING_RELATED_KNOWLEDGE_ENABLED`), so a real brief gains a `Related: [source] …` line for the top imminent item — `@muse/autoconfigure` enricher + `@muse/api` briefing daemon — knowledge-enricher.test.ts (top cited line / threshold / empty) + situational-briefing-tick-related.test.ts (tick forwards → brief gains the line); mutation-proven — P20 (anticipation × knowledge, live wiring)
- [Anticipation] The real-time ambient notice surfaces a related note for what you're LOOKING at — `createAmbientNoticeRunner` gains an injected enricher that appends `— Related: [source] …` keyed on the active window/app, wired into the ambient daemon via a shared `buildKnowledgeEnricherIfEnabled` helper (gated `MUSE_BRIEFING_RELATED_KNOWLEDGE_ENABLED`), so BOTH proactive channels (scheduled briefing 773 + real-time ambient) surface what you wrote about your context — `@muse/mcp` ambient runner + `@muse/api` daemon — ambient-notice-enrich.test.ts + ambient-tick-enrich.test.ts (append keyed on signal / fail-soft; tick forwards → notice gains the line); mutation-proven — P20 (anticipation × knowledge, ambient channel)
- [Anticipation] The briefing's "Related" line surfaces genuine context (notes/contacts), not an echo of what's already under Upcoming — `createKnowledgeEnricher` gains `excludeSourcePrefixes` (ranks top-5, returns the first non-excluded match) and the briefing daemon excludes `event/`+`task/` (its imminent surface), so a calendar-imminent item no longer re-lists itself as Related; the ambient channel keeps the full corpus — `@muse/autoconfigure` enricher + briefing daemon — knowledge-enricher-exclude.test.ts (without exclusion → event echo; with → the note; mutation-proven) — P20 (anticipation quality)
- [Perception] Web-watch trigger detection — `detectWatchTrigger(prev, curr, rule)` decides from two page-text snapshots whether a watch condition just became true (term appears / disappears / any change), edge-triggered (no re-fire while it persists; baseline-aware on first observation) — foundation of "monitor this page and ping me when X" composing P18 Chrome perception + proactive notices — `@muse/mcp` web-watch — web-watch.test.ts (appears rising-edge/first-obs/persist/absent; disappears needs baseline; onAnyChange; no-condition; case-insensitivity; mutation-proven) — P21 web-watch (slice 1: detector; wired polling tick flips the bullet)
- [Perception] Web-watch — "monitor this page and ping me when X" — `createWebWatchRunner` polls each watch's page, runs `detectWatchTrigger` against the per-watch previous snapshot (edge-triggered, baseline held across ticks), and delivers a proactive notice on the rising edge; a failed snapshot is skipped without losing the baseline; read-only (never acts) — `@muse/mcp` web-watch runner → ProactiveNoticeSink — web-watch-runner.test.ts (processing→shipped→shipped delivers exactly ONE notice on the edge, none steady; snapshot-fail keeps baseline; mutation-proven) — P21 web-watch (FLIPPED; live MCP-snapshot daemon is follow-on)
- [Perception] Web-watches are configurable + run over PUBLIC pages via non-intrusive HTTP — `webWatchesFromConfig` parses `{id,url,title,message,rule}` specs into runnable WebWatches whose `createHttpSnapshot` HTTP-GETs the page (retry-hardened via fetchWithRetry; permanent failure → undefined so the runner skips, keeping its baseline) — `@muse/mcp` — web-watch-config.test.ts (parse + drop-invalid + non-array/malformed→[]; HTTP snapshot fetch / 404→undefined; e2e config-built watch fires once on the page transition; mutation-proven rule-drop) — P21 web-watch (config + HTTP source; apps/api daemon is follow-on)
- [Perception] Web-watch RUNS in production — `startWebWatchDaemonIfConfigured` (env-gated `MUSE_WEB_WATCH_ENABLED`/provider/destination/`MUSE_WEB_WATCH_CONFIG`) starts an unref'd tick (default 15 min, clamped 1 min–6 h) that polls each configured page, edge-detects the watched condition, and delivers a proactive notice through the messaging registry; re-entrancy-guarded, quiet-hours-aware, `onClose`-stopped — `apps/api` web-watch-tick → MessagingProviderRegistry — web-watch-tick.test.ts (config-built HTTP watch processing→shipped→shipped delivers exactly ONE message on the edge, none steady; quiet-hours suppresses; daemon gate on/off; mutation-proven quiet-hours) + smoke:broad 51/0 — P21 web-watch (production daemon; authenticated-page snapshot is follow-on)
- [Perception] Web-watch targets the value, not the noisy page — `WatchRule.extract` (a regex applied to both snapshots, capture-group-1-else-whole-match) narrows the snapshot to the region of interest before edge-detection, so a rotating ad / timestamp / session-id can't false-fire `onAnyChange` and `appears`/`disappears` match only inside the region; no match → empty region, invalid regex fails open to the whole text — `@muse/mcp` web-watch (`detectWatchTrigger` + `webWatchesFromConfig`) — web-watch-extract.test.ts (onAnyChange ignores noise-only change / fires on region change; appears matches the status region not the "free shipping" banner; invalid-regex fail-open; e2e config watch over HTTP `Price: (\$\d+)` fires once on the price change, never on banner noise; mutation-proven extraction) — P21 web-watch (daily-driver hardening; bullet already [x]+audited)
- [Perception] Web-watch fires on a numeric threshold — the price-drop alert — `WatchRule.below`/`above` parse the number from the (extracted) region and fire on the rising edge of crossing the threshold (once on the crossing, never re-firing while past it; first-observation-already-past fires); `parseWatchNumber` strips thousands separators, no parseable number → no fire — `@muse/mcp` web-watch (`detectWatchTrigger` + `webWatchesFromConfig`) — web-watch-threshold.test.ts (below downward-crossing edge / no re-fire while under / above mirror / 1,299 parse / no-number no-fire / first-obs-below; e2e config `below:40`+extract over HTTP $45→$44→$38 fires once under 40; non-finite below dropped; mutation-proven rising-edge) — P21 web-watch (daily-driver hardening; bullet already [x]+audited)
- [Perception] Muse reads smart-home state — "is the front door locked?" / "living-room temperature?" — `readHomeAssistantState` GETs Home Assistant `/api/states/<id>` with the Bearer token through `fetchWithRetry` (recovers from transient 429/5xx; permanent 404 / malformed body → undefined, never throws); surfaced as `muse home state <entity>` + the `home_state` read-only (ungated) agent tool. Reads retry; the write path stays single-shot (a retried POST could double-act) — `@muse/mcp` smart-home + `apps/cli` commands-home — smart-home-state.test.ts (6: GET+Bearer / transient-503 recovery / 404→undefined / malformed→undefined / tool risk:read) + commands-home.test.ts (muse home state prints state; 404→exit1; mutation-proven retry) — P19 (daily-harden actuators: smart-home read, the per-actuator follow-on after weather 753) + P20 perception
- [Perception] Proactive home monitoring — "ping me if the front door is left unlocked / the freezer rises above -15" — `homeWatchesFromConfig` builds web-watch `WebWatch`es whose snapshot is a Home Assistant entity's live `state` (`createHomeStateSnapshot`), so the proven edge-triggered detector + numeric `below`/`above` + `ProactiveNoticeSink` monitor a home sensor exactly as a web page; read-only (never actuates); a failed HA read skips without a false fire — `@muse/mcp` home-watch (composes 783 read + web-watch runner; shared `parseWatchRule`) — home-watch.test.ts (door locked→unlocked fires once on edge / freezer -18→-16→-12 crosses above -15 / invalid+malformed dropped / transient-503 skip keeps baseline; mutation-proven snapshot adapter) — P20 perception + P19 (783 hardened HA read) — daemon wiring is follow-on
- [Perception] Proactive home monitoring RUNS in production — `startHomeWatchDaemonIfConfigured` (env-gated `MUSE_HOME_WATCH_ENABLED`/provider/destination/`MUSE_HOME_WATCH_CONFIG` + HA creds) builds `homeWatchesFromConfig` watches over Home Assistant entity states and reuses `startWebWatchTick` to poll + deliver a proactive notice on the edge (e.g. front-door-unlocked) through the messaging registry; HA-creds-gated, `onClose`-stopped — `apps/api` tick-daemons → MessagingProviderRegistry — home-watch-tick.test.ts (config builds HA-state watches firing once on locked→unlocked through real messaging; daemon gate on/off incl. missing-creds; mutation-proven creds gate) + smoke:broad 51/0 — P20 perception + P19 (783 HA read) production daemon
- [Knowledge] knowledge_search answers from recent EMAIL with citation — "what did Jane email me about the project deadline?" — `assembleKnowledgeCorpus` gains an `emailSource` (structural `listRecent`, satisfied by `GmailEmailProvider`): each recent message becomes an `email/<id>` chunk (from+subject+snippet, capped by maxEmails, truncated), ranked + cited alongside notes/tasks/calendar/contacts; a throwing source degrades to no email chunks — `@muse/autoconfigure` knowledge-corpus (+ production knowledge_search gated MUSE_GMAIL_TOKEN) — knowledge-email-source.test.ts (email→email/<id> chunk / maxEmails cap / throwing-source degrade / e2e knowledge_search cites [email/m1]; mutation-proven degrade) — P20 knowledge depth
- [Actuators] Web action reports the truth on a server rejection — a booking/form submit that the third party rejects with 4xx/5xx is reported `failed` (with the HTTP status in detail), NOT a false `performed` success the user would act on; the request fires once and is never retried (double-action risk); also strengthens the smart-home WRITE path that reuses the primitive — `@muse/mcp` web-action — web-action.test.ts (500→failed not performed, fired once, logged failed; 403→not performed; 201 still performed; mutation-proven ok-check) — P19 (daily-harden actuators: web action honest outcome) + outbound-safety honesty
- [Perception] Google Calendar read survives a transient blip — the events read retries 429/5xx (and network rejects) with exponential backoff instead of dropping the calendar from the briefing on a momentary Google failure; GET-only (a write is never retried — double-create/delete risk); a non-retryable 403/404 fails fast — `@muse/calendar` google-provider (reuses `isRetryableCalendarStatus`) — calendar.test.ts (503→200 recovers in 2 calls / 403 fails fast in 1 call; mutation-proven retry) — P19 (daily-harden actuators: calendar read, per-actuator follow-on)
- [Perception] Live macOS active-window perception — Muse reads the frontmost app + window title via `osascript` (no launchd helper writing ~/.muse/ambient.json), so an ambient notice fires off the LIVE desktop; `parseActiveWindowSignal` returns undefined on empty output (no frontmost app / no Accessibility permission) and any spawn failure → undefined (never throws); opt-in `MUSE_AMBIENT_SOURCE=macos` on darwin — `@muse/mcp` macos-ambient-source + `apps/api` ambient daemon — macos-ambient-source.test.ts (parse app+window/app-only/empty→undefined; injected-runner snapshot; throw→undefined; e2e Calendar/standup signal → proactive notice via real sink, edge-deduped; mutation-proven empty-app guard) — P20 continuous perception (the OS active-window source follow-on)
- [Perception] The situational briefing proactively flags a noteworthy home state — "Home: Front door is unlocked" rides the morning brief when a configured entity is in an alert state, surfaced unasked alongside weather/inbox/upcoming; `resolveHomeAlertLine` reads each entity and returns undefined when everything's safe (never narrates normal), skipping a per-entity read failure — `@muse/mcp` smart-home + situational-briefing(-loop) + `apps/api` briefing tick — home-alert-briefing.test.ts (alert states joined / all-safe→undefined / per-entity 404 skipped; e2e runDueSituationalBriefing delivers "Home: Front door is unlocked" via real registry; mutation-proven match) — P20/P8 proactive briefing (composes 783 HA read)
- [Perception] CalDAV calendar read survives a transient blip — the listEvents REPORT read retries 429/5xx (and network rejects) with exponential backoff instead of dropping the calendar (iCloud/Fastmail/Proton/Nextcloud) on a momentary failure; read-only (PUT/DELETE writes never retried); a non-retryable 401/403 fails fast — `@muse/calendar` caldav-provider (reuses `isRetryableCalendarStatus`) — calendar.test.ts (503→200 recovers in 2 calls / 401 fails fast in 1 call; mutation-proven retry) — P19 (calendar-read hardening complete across both backends: Google 788 + CalDAV)
- [Perception] Home alerts reach the briefing in production — `startSituationalBriefingDaemonIfConfigured` builds a `homeAlert` resolver from HA creds + `MUSE_BRIEFING_HOME_ALERTS` (parsed by `parseHomeAlertChecks`, fail-open) so a running server's brief flags "Home: Front door is unlocked"; gated — absent creds/config the brief is unchanged — `@muse/mcp` parseHomeAlertChecks + `apps/api` briefing daemon — home-alert-config.test.ts (parse valid/drop invalid/filter non-string states; e2e exact daemon composition delivers the Home line via real registry; mutation-proven drop guard) + smoke:broad 51/0 — P20/P8 (791 reaches production)
- [Reach] Proactive notices survive a transient messaging blip — the web-watch / home-watch / ambient tick sinks dispatch through `sendWithRetry` (3 attempts, backoff; permanent error short-circuits; no re-send after success) instead of single-shot `registry.send`, so a one-off Telegram 503 no longer silently drops "front door unlocked" / "price dropped" — `apps/api` web-watch-tick + ambient-tick — web-watch-tick-retry.test.ts (a fired notice is delivered after a transient 503 via a real registry; mutation-proven vs single-shot) + smoke:broad 51/0 — P19 (reliability of the proactive-notice reach)
- [Perception] The brief gives a rain heads-up — "Weather: 18°C, clear — rain likely ~15:00 (moderate rain, 70%)" — `OpenMeteoWeatherProvider.rainOutlook` reads the hourly forecast and returns the next future hour at/above a 50% probability within 12h; `resolveWeatherLine` folds `formatRainHeadsUp` onto the existing current-weather line (fail-soft — a forecast blip keeps the base line), so the briefing daemon gains it with no wiring change — `@muse/mcp` weather — weather-forecast.test.ts (next-rain hour / dry→undefined / past+horizon ignored / e2e resolveWeatherLine appends heads-up; mutation-proven threshold) — P19 weather actuator → P20/P8 brief
- [Perception] Web-watch monitors an authenticated page — a per-watch `headers` (copied Cookie / Bearer) is sent on the snapshot fetch so "watch my logged-in order page / dashboard" works without hijacking the browser; `createHttpSnapshot` merges headers into the fetch init, `webWatchesFromConfig` parses them fail-open (malformed → unauthenticated watch still built) — `@muse/mcp` web-watch — web-watch-auth-headers.test.ts (Cookie+Authorization sent / no-headers plain GET / config carries cookie / malformed ignored; mutation-proven header merge) — P21 web-watch (authenticated targets, the deferred-snapshot need met via request headers)
- [Reach] Proactive PATTERN notices survive a transient blip — `runDuePatternNotices` dispatches the learned-pattern suggestion through `sendWithRetry` instead of single-shot `registry.send`, so a one-off Telegram 503 no longer drops "you usually journal Tuesday 21:30 — start now?" (which would otherwise wait a whole daemon tick); cooldown still recorded only after a successful send — `@muse/mcp` pattern-firing-loop — pattern-firing-retry.test.ts (a REAL fireable pattern from mtime fixtures is delivered after a 503 via a real registry; mutation-proven vs single-shot) — P19 (proactive-reach reliability complete: briefing/reminders/web-watch/home-watch/ambient/pattern all retry)
- [Knowledge] Muse knows your people's birthdays — `muse contacts add --birthday MM-DD` stores it on the contact and `muse contacts birthdays [--within N]` lists the upcoming ones soonest-first ("🎂 Sarah — in 2 days"); `resolveUpcomingBirthdays` is year-agnostic with a year-wrap (a past date rolls to next year) and skips missing/malformed dates — `@muse/mcp` personal-contacts-store + `apps/cli` commands-contacts — personal-contacts-store.test.ts (window/sort/wrap/skip) + commands-contacts.test.ts (add --birthday round-trips the real store → birthdays lists it; malformed rejected; none-case; mutation-proven year-wrap) — P20 (people-graph knowledge depth)
- [Knowledge] knowledge_search calls correctly first-time on the local model — its `query` param gains an example-bearing description + `additionalProperties:false` + a "use when / not when" cue (both builders), and `validateToolDefinitions` now flags any undescribed object-schema parameter (tool-calling.md rule 3) so future tools can't regress — `@muse/tools` + `@muse/agent-core` + `@muse/autoconfigure` — tools.test.ts (validator flags/passes undescribed param) + knowledge-recall-agent.test.ts (real createKnowledgeSearchTool passes validateToolDefinitions, query carries an example; mutation-proven check) — P20 + tool-calling reliability (live-selection smoke:live deferred — Ollama down)
- [Knowledge] Calendar event tools fill correctly first-time on the local model — `add`/`update`/`delete` event params (title/startsAtIso/endsAtIso/location/notes/allDay/tags/id/providerId) gain concise example-bearing descriptions ("title, e.g. 'Dentist appointment'"; "'tomorrow 3pm' / '내일 오후 3시'"), so the small model fills the highest-stakes write args without guessing — `@muse/mcp` loopback-calendar — loopback-calendar-tool-schema.test.ts (real calendar tools pass validateToolDefinitions with zero undescribed_parameter; add.title has an example; mutation-proven) — P20 + tool-calling reliability (live arg-fill smoke:live deferred — Ollama down)
- [Knowledge] Notes tools fill correctly first-time on the local model — `list`/`read`/`search`/`save`/`append` params (query/title/body/folder/id/overwrite/limit/providerId) gain concise example-bearing descriptions ("query, e.g. 'Q3 launch plan'"; "title, e.g. 'Meeting notes 2026-05-23'"), so the small model fills note args without guessing — `@muse/mcp` loopback-notes-registry — loopback-notes-tool-schema.test.ts (real notes tools pass validateToolDefinitions zero undescribed_parameter; save.title has an example; mutation-proven) + 10-iter regression sweep (pnpm check 0, lint 0/0, smoke:broad 51/0) — P20 + tool-calling reliability (live arg-fill smoke:live deferred — Ollama down)
- [Knowledge] The brief proactively flags upcoming birthdays — "Birthdays: Sarah tomorrow; Bob in 3 days" rides the morning brief unasked, soonest-first within a window (default 7 days), silent when none — `formatBirthdayBriefLine` over 798's `resolveUpcomingBirthdays`, wired into the briefing loop + daemon (reads the contacts file) — `@muse/mcp` personal-contacts-store + situational-briefing(-loop) + `apps/api` briefing tick/daemon — birthday-briefing.test.ts (today/tomorrow/in-N-days soonest-first / none→undefined; e2e runDueSituationalBriefing delivers "Birthdays: Sarah tomorrow" via real registry; mutation-proven empty-guard) — P20/P8 proactive briefing (completes 798)
- [Knowledge] Notion notes read survives a rate-limit blip — `NotionNotesProvider` reads (list/read/search/block-children) retry a transient 429/5xx with exponential backoff instead of dropping the notes; idempotency-flagged so writes (create/append/replace/delete) are never retried (duplicate-page risk); a permanent 401 fails fast — `@muse/mcp` notes-providers-notion (injectable retry) — notion-notes-retry.test.ts (429→200 recovers in 2 calls / 401 fails fast in 1 / save-create not retried; mutation-proven) — P19 (read-retry posture extended to the Notion backend)
- [Knowledge] Notion tasks read survives a rate-limit blip — `NotionTasksProvider` reads (list/search) retry a transient 429/5xx with exponential backoff instead of dropping the task list; idempotency-flagged so writes (add/complete) are never retried (duplicate-task risk); a permanent 401 fails fast — `@muse/mcp` tasks-providers-notion (injectable retry, mirrors 803) — notion-tasks-retry.test.ts (429→200 recovers in 2 calls / 401 fails fast in 1 / add not retried; mutation-proven) — P19 (Notion backend read-retry complete: notes 803 + tasks 804)
- [Perception] Muse discovers smart-home devices — "what devices do I have? / find my lights" — `listHomeAssistantStates` GETs Home Assistant `/api/states` (Bearer, retry-hardened, optional domain filter), surfaced as `muse home entities [--domain X]` + the `home_entities` read tool; completes the triad (discover ids → home_state read → home_action control) — `@muse/mcp` smart-home + `apps/cli` commands-home — smart-home-entities.test.ts (GET+Bearer parse / domain filter / 503 retry / fail→[] / tool risk:read) + commands-home.test.ts (muse home entities --domain lists only that domain; unreachable→none; mutation-proven filter) — P20 smart-home perception
- [Perception] The agent can answer home questions in conversation — `home_state` + `home_entities` (read-only) are wired into the runtime tool registry, gated on Home Assistant creds, so `muse ask` can resolve "is my front door locked? / what lights do I have?" not just the CLI; surfaced by the relevance filter, never in the execute-gated actuator set — `@muse/autoconfigure` runtime assembly — home-read-wiring.test.ts (real createMuseRuntimeAssembly exposes both risk:read tools with HA creds / absent without; mutation-proven creds gate) — P20 smart-home perception (805 follow-on; live tool-selection smoke:live deferred — Ollama down)
- [Perception] The agent answers weather on demand — a `weather` read tool ("what's the weather in Seoul? / will it rain?") wired unconditionally into the runtime (open-meteo, keyless), returning the current weather + the 795 rain heads-up via `resolveWeatherLine`; empty/un-geocodable location → found:false (no guess) — `@muse/mcp` weather-tool + `@muse/autoconfigure` runtime — weather-tool.test.ts (risk:read / found line w/ temp / empty+ungeocodable→found:false / location param has example; mutation-proven empty-guard) + weather-tool-wiring.test.ts (real assembly exposes it) — P20 perception (live tool-selection smoke:live deferred — Ollama down)
- [Perception] The agent reads the inbox on demand — an `email_recent` read tool ("any new email from Jane? / what's unread?") wired into the runtime gated on MUSE_GMAIL_TOKEN, listing sender/subject/unread/snippet via the hardened EmailProvider.listRecent; `unreadOnly` filter, limit clamp 1–50, provider error → empty list (never throws) — `@muse/mcp` email-tool + `@muse/autoconfigure` runtime — email-read-tool.test.ts (risk:read / list / unreadOnly filter / limit clamp / error-degrade; mutation-proven) + email-read-wiring.test.ts (real assembly exposes it gated on token) — P20 perception (live tool-selection smoke:live deferred — Ollama down)
- [Knowledge] Web-fetch + context tools fill correctly first-time — the `muse.fetch` get/head `url` param (the agent's "read this URL") and the `muse.context` active/ref params gain example-bearing descriptions ("Absolute http(s) URL …, e.g. 'https://example.com/page'"), so the local model fills the URL without guessing — `@muse/mcp` loopback-fetch + loopback-context — loopback-fetch-context-tool-schema.test.ts (real servers pass validateToolDefinitions zero undescribed_parameter; get.url mentions http; mutation-proven) — P20 + tool-calling reliability
- [Knowledge] The agent looks up people — a `find_contact` read tool ("what's Jane's email? / who is Bob?") wired unconditionally into the runtime over the local contacts store; resolves an exact name to email/handle, returns the candidate names for an ambiguous name (never a guess — outbound-safety recipient resolution), found:false for unknown/empty — `@muse/mcp` contacts-tool + `@muse/autoconfigure` runtime — contacts-tool.test.ts (risk:read / resolve / ambiguous→candidates / unknown+empty→found:false; mutation-proven ambiguous branch) + contacts-find-wiring.test.ts (real assembly exposes it) — P20 perception (live tool-selection smoke:live deferred — Ollama down)
- [Knowledge] Tasks + reminders tools fill correctly first-time — `muse.tasks`/`muse.tasks-multi` (add/list/complete/search) and reminders (due/search/snooze/fire/clear) params (title/notes/tags/query/id/status/providerId) gain example-bearing descriptions ("title, e.g. 'Buy milk'"), so the model fills these high-traffic write actuators without guessing — `@muse/mcp` loopback-tasks(+registry) + loopback-reminders — tasks-reminders-tool-schema.test.ts (real servers pass validateToolDefinitions zero undescribed_parameter; tasks add.title has an example; mutation-proven) — P20 + tool-calling reliability
- [Perception] "What's the weather?" works with no location — the `weather` tool defaults to the user's configured home (MUSE_WEATHER_LOCATION) when `location` is omitted, making `location` optional so the local model can answer in one no-arg call; an explicit place still overrides — `@muse/mcp` weather-tool + `@muse/autoconfigure` runtime — weather-tool.test.ts (no-arg uses home + required absent / explicit overrides / no-default keeps required; mutation-proven fallback) — P20 perception + one-shot tool-calling (live selection smoke:live deferred — Ollama down)
- [Knowledge] The agent answers "when's X's birthday?" — `find_contact` now surfaces the contact's `birthday` (omitted when absent) alongside email/handle, composing 798 (contact birthdays) + 810 (find_contact); description + keywords mention birthday so the model selects it — `@muse/mcp` contacts-tool — contacts-tool.test.ts (resolved contact returns birthday / omitted when absent; mutation-proven) — P20 perception (live selection smoke:live deferred — Ollama down)
- [Perception] The brief flags tasks due today + overdue — "Due: Pay rent (overdue); Buy milk (today); Call mom (tomorrow)" rides the morning brief, overdue first, within a window (default 1 day), silent when none — `resolveTasksDueLine` over the loopback-tasks store's dueAt, wired into the briefing loop + daemon (reads the tasks file) — `@muse/mcp` personal-tasks-store + situational-briefing(-loop) + `apps/api` briefing tick/daemon — tasks-due-briefing.test.ts (overdue→today→tomorrow / skip done+no-due+beyond-window / none→undefined; e2e runDueSituationalBriefing delivers "Due: Pay rent (overdue)" via real registry; mutation-proven window) — P20/P8 proactive briefing
- [Knowledge] The agent captures a person mid-conversation — an `add_contact` write tool ("I just met Bob, save bob@x.com") persists name + email/handle (+ optional birthday) to the local contacts store via `addContact`; refuses an unnamed / unreachable contact and a malformed birthday — `@muse/mcp` contacts-tool + `@muse/autoconfigure` runtime — contacts-tool.test.ts (risk:write / saves through capturing save / refuses empty+unreachable / rejects bad birthday; mutation-proven reachable guard) + contacts-find-wiring.test.ts (real assembly exposes add_contact) — P20 (completes the contacts read+write agent pair; live selection smoke:live deferred — Ollama down)
- [Perception] `muse weather` (no arg) shows your home weather + rain heads-up — the CLI weather command defaults `location` to MUSE_WEATHER_LOCATION and appends the 795 rain heads-up ("…22°C — rain likely ~15:00"), matching the agent tool (813) + brief (795); precise not-found/forecast errors kept — `apps/cli` weather — weather.test.ts (no-arg uses home / no-home→usage error / rain heads-up appended / existing seeded+unknown unchanged; mutation-proven default) — P20 perception (CLI UX consistency)
- [Perception] The agent reads a full email — "read me Jane's email" — `read_email` (risk:read) fetches one message by id (from `email_recent`, which now returns ids) via `GmailEmailProvider.getMessage` (format=full, retry-hardened) and `extractPlainTextBody` (text/plain part, ignores HTML, snippet fallback); a 404/unreachable → found:false (never throws) — `@muse/mcp` email-provider + email-tool + `@muse/autoconfigure` runtime — email-read-message.test.ts (extractPlainTextBody direct/multipart-prefers-plain/none / getMessage parse + 404→undefined / tool risk:read + found:false; mutation-proven text/plain preference) + email-read-wiring.test.ts (real assembly exposes read_email gated) — P20 perception (live selection smoke:live deferred — Ollama down)
- [Knowledge] Home tools surface for natural prompts — "is the front door locked? / turn on the lights" now expose home_state/home_action/home_entities via a new `home` domain in DefaultToolFilter (the tools were `system`, whose keywords missed inflected "locked"/"lights"), keeping the right tool in reach in one shot while an unrelated prompt surfaces none — `@muse/agent-core` tool-filter + `@muse/mcp` smart-home-tool — home-tool-relevance.test.ts (locked→home_state / lights→home_action / devices→home_entities / unrelated→none; mutation-proven home domain) — P20 + tool-calling reliability (live selection smoke:live deferred — Ollama down)
- [Knowledge] Weather + email tools surface for natural/inflected prompts — "is it raining? / 오늘 날씨 어때? / any new emails?" now expose weather + email_recent (word-boundary matching previously dropped "raining"/"emails"); unrelated prompts surface neither — `@muse/mcp` weather-tool + email-tool keywords — perception-tool-relevance.test.ts (raining/Korean→weather, emails→email_recent, unrelated→none; mutation-proven plural keyword) — P20 + tool-calling reliability (extends 819; live selection smoke:live deferred — Ollama down)
- [Knowledge] Calendar/tasks/notes tools surface for plural prompts — "what are my events? / any meetings? / show my tasks" now expose the loopback calendar/tasks tools via plural + natural keywords in DEFAULT_DOMAIN_KEYWORDS (word-boundary matching dropped "events"/"tasks" vs the singulars); unrelated prompts surface none — `@muse/agent-core` tool-filter — loopback-domain-relevance.test.ts (events/meetings→calendar, tasks→tasks, unrelated→none; mutation-proven events plural) + 10-iter regression sweep (pnpm check 0, lint 0/0, smoke:broad 51/0) — P20 + tool-calling reliability (completes 819/820 across all domains; live selection smoke:live deferred — Ollama down)
- [Knowledge] The agent removes a contact (fail-close) — a `remove_contact` write tool ("forget that contact") resolves the name and deletes by id; an ambiguous name returns the candidates and removes NOTHING (never deletes a guess), unknown/empty → removed:false — `@muse/mcp` contacts-tool + `@muse/autoconfigure` runtime — contacts-tool.test.ts (resolved→removed by id / ambiguous→candidates no-remove / unknown+empty→no-remove; mutation-proven ambiguous guard) + contacts-find-wiring.test.ts (real assembly exposes remove_contact) — P20 (completes the contacts CRUD agent triad: find/add/remove; live selection smoke:live deferred — Ollama down)
- [Perception] Chrome DevTools tools no longer flood every prompt — `withChromeDevToolsRisk` stamps `domain: "web"` on the ~30 chrome-devtools.* tools (MCP-projected tools were domain-less → always-on) + a `web` domain in DefaultToolFilter, so browser tools surface only on web/browser prompts and an unrelated prompt sees none — keeps the local model's one-shot selection viable when P18 is enabled — `@muse/mcp` chrome-devtools-mcp + `@muse/agent-core` tool-filter — chrome-devtools-mcp.test.ts (domain "web" stamped) + chrome-tool-relevance.test.ts (browser prompt surfaces / unrelated→none; mutation-proven domain stamp) — P18 + tool-calling reliability (live selection smoke:live deferred — Ollama down)
- [Reach] Inbox read survives a single bad message — `GmailEmailProvider.listRecent` loops over each message's metadata fetch; previously one message's retry-exhausted 5xx or 200-with-malformed-body threw out of the whole call, dropping the entire inbox (briefing line + `muse inbox` went silent over one blip). Now a transient/malformed single-message failure is skipped (the rest return) while a permanent `GmailAuthError` (401/403) still propagates so a dead token is never masked as a short list — `@muse/mcp` email-provider — email-provider-retry.test.ts (malformed-200→others survive / retry-exhausted-500→skipped / 401-mid-batch→propagated; mutation-proven per-message guard) — P19 email actuator hardening (follow-on to 753 weather)
- [Reach] The agent searches the inbox — a `search_email` read tool finds messages MATCHING a query (sender/subject/keyword) via Gmail's server-side `q=` param ("find the email from the bank about my statement"), complementing `email_recent` (latest N) and `read_email` (one by id); the two read tools carry disjoint "use this not that" descriptions so the local model picks the right one. Reuses 824's per-message resilience via a shared `summariesForIds` — `@muse/mcp` email-provider + email-tool, `@muse/autoconfigure` wiring — email-provider-retry.test.ts (q= param sent / blank→no-HTTP / bad-match skipped; mutation-proven) + email-search-tool.test.ts (required query / trim+clamp / error→empty / disjoint desc) + email-search-relevance.test.ts (search prompt surfaces it / unrelated→none) — P17 conversational-actuation read surface (live model selection [UNVERIFIED-LIVE] — Ollama down)
- [Perception] Web-watch monitors a page behind the user's login — `createChromeSnapshot(connection, url)` reads a page through the user's REAL logged-in Chrome via the Chrome DevTools MCP `navigate_page`+`take_snapshot` seam (read-only), and `webWatchesFromConfig` builds `source:"chrome"` watches (skipped, never downgraded to HTTP, when no connection); previously web-watch could only HTTP-GET public pages and would see the login wall, not the content — composes P18 Chrome perception with P21 watching — `@muse/mcp` web-watch — web-watch-chrome.test.ts (navigate-then-snapshot order / throw→undefined / empty→undefined / connection-required / END-TO-END processing→SHIPPED→SHIPPED fires exactly one edge notice via the real runner; mutation-proven navigate call + chrome branch) — P18+P21 composition (daemon Chrome-connection wiring is the follow-on; no smoke:live — not a model-facing tool)
- [Reach] The agent answers "am I free?" — a `muse.calendar.availability` read tool computes free/busy over a time window ("am I free at 3pm?", "find a 30-min gap this afternoon"): a pure `computeAvailability` clamps events to the window (all-day blocks the span), merges overlapping+adjacent busy blocks, and returns `fullyFree` + the busy blocks + the free gaps (optionally ≥ minMinutes); previously Muse could only `list` events and had to reason about gaps itself — `@muse/mcp` calendar-availability + loopback-calendar — calendar-availability.test.ts (8 pure: empty/one-meeting/merge-overlap+adjacent/clamp/minFree/all-day/point-check/invalid-safe; 3 tool: domain+read / answers free-busy / missing-fromIso→error; mutation-proven merge + minFree guards) — P17 conversational-actuation read surface (live model selection [UNVERIFIED-LIVE] — Ollama down)
- [Perception] Weather answers "will it rain Saturday?" — the `weather` tool gained an optional `when` ("tomorrow"/"Saturday"/"2026-05-30"/"내일") that returns that upcoming day's forecast (high/low + rain chance) via Open-Meteo's free multi-day daily API; without `when` it's the current weather as before — DEEPENS the existing tool (no new catalog entry, protecting one-shot selection) — `@muse/mcp` weather + weather-tool — weather-daily-forecast.test.ts (dailyForecast params+parse+503-recovery / formatDailyForecast / resolveForecastLine match+horizon+no-forecast / tool: when→forecast, past-horizon→found:false, unparseable→reason, no-when→current; mutation-proven day-match + when-branch) — P20 Perception (live smoke:live [UNVERIFIED-LIVE] — Ollama down; no new tool added)
- [Perception] Ambient perception can react to the clipboard — `MacOsActiveWindowSource` gained opt-in clipboard capture (`includeClipboard`, via `pbpaste`, trimmed + capped at 2000 chars), so a proactive ambient rule can fire on what the user just COPIED (a tracking number, code, URL) — previously the source saw only the frontmost app + window title; user opts in with `MUSE_AMBIENT_CLIPBOARD=true` — `@muse/mcp` macos-ambient-source + `apps/api` tick-daemons — macos-ambient-source.test.ts (privacy-default-off / trimmed+capped / clipboard-only signal / fail-soft / empty→nothing / END-TO-END copied-TRK drives a clipboard-keyed notice via the real runner; mutation-proven opt-in + cap guards) — P20 Perception (no LLM path → no smoke:live)
- [Knowledge] RAG citations name the source, not an opaque id — `assembleKnowledgeCorpus` now labels email/event/task/contact chunks by a HUMAN name (`email/Project deadline`, `event/Acme strategy meeting`, `contact/Bob Acme`) instead of `email/18f2a3b9c`, via a `labelSource` helper that keeps the stable type-prefix (so the briefing enricher's excludeSourcePrefixes still matches), collapses+caps the name, and falls back subject→sender→id; a grounded answer's `[source]` citation is now meaningful to the user — `@muse/autoconfigure` knowledge-corpus — knowledge-{email,calendar,tasks,contacts,enricher-exclude}-source tests (subject-not-id / sender-fallback / id-fallback / prefix-exclusion preserved; mutation-proven label + fallback) — P20 Knowledge source-citation quality (no new tool → no smoke:live)
- [Perception] "What's left on / unlocked?" — `home_entities` gained an optional case-insensitive `state` filter (combines with `domain`), so "what lights are on?" (`domain:light,state:on`) and "is anything unlocked/left open?" (`state:unlocked`) resolve server-side in ONE shot instead of the local model list-then-reasoning; omitting `state` is the prior behaviour — DEEPENS the existing tool (no new catalog entry) — `@muse/mcp` smart-home-tool — smart-home-entities.test.ts (state:ON→only the on light case-insensitive / domain+state:unlocked→0 when locked / schema declares state; mutation-proven state filter) — P20 Perception (no new tool / selection unchanged → no smoke:live)
- [Reach] "What's due today / this week?" — the `muse.tasks` list tool gained an optional `dueWithinDays` (0 = today + overdue, 7 = this week) that returns ONLY open tasks due within that window, OVERDUE included, soonest first — instead of the local model pulling all tasks and reasoning about each dueAt; a shared `selectTasksDueWithin` selector backs both this and the morning brief's tasksDue line so they can't drift — DEEPENS the existing list tool (no new catalog entry) — `@muse/mcp` personal-tasks-store + loopback-tasks — tasks-due-filter.test.ts (selector: within0/within7/dayDiff/default + tool over a real store: due-this-week / due-today / no-filter→status listing; mutation-proven open-dated guard + cutoff) + tasks-due-briefing.test.ts still green (resolveTasksDueLine refactor preserved) — P20 tasks daily-driver (no new tool / selection unchanged → no smoke:live)
- [Reach] `muse calendar free` — free/busy on the terminal: a new CLI subcommand fetches a window's events (local provider or the API, dual path like `events`), runs the tested `computeAvailability`, and prints fully-free / busy blocks / free gaps ("am I free this afternoon? / find a gap"); brings 827's agent-only availability to the terminal — `apps/cli` commands-calendar — commands-calendar.test.ts (eventsToAvailability maps+skips-unparseable / formatAvailability fully-free + busy/free / surface: `free --json` over the API events seam computes busy=1/free=2 + hits /api/calendar/events / no-events→fully-free / bad --min-minutes→error; mutation-proven fullyFree branch + skip guard) — CLI/Reach surface (no LLM path → no smoke:live)
- [Perception] The morning brief tells you the shape of your day — a new `resolveDayShapeLine` composes `computeAvailability` over now→day-end into a `Schedule:` brief line ("free 09:00–10:00, after 15:00" / "booked solid the rest of today"), wired into the briefing daemon from the calendar source; supplementary posture (rides a non-empty brief, never triggers one), undefined when no commitments remain — `@muse/mcp` situational-briefing(+loop) + `apps/api` briefing-tick/tick-daemons — day-shape-briefing.test.ts (resolveDayShapeLine: free-gaps+after / booked-solid / no-commitments→undefined / past-day-end→undefined; END-TO-END via real runDueSituationalBriefing: Schedule line rides a brief, never fires alone; mutation-proven empty-busy guard + after-render) — P20 Perception proactive brief (no LLM path → no smoke:live)
- [Knowledge] "My recent notes" — the `muse.notes` list tool now stamps each entry with `modifiedAtIso` and accepts `sort:"recent"` (newest-modified first, a TRUE global sort: collect-all → sort → slice), so "what did I note recently / my latest notes" is answerable instead of the local model facing an unordered, timestamp-less listing — DEEPENS the existing list tool (no new catalog entry) — `@muse/mcp` loopback-notes — notes-list-recent.test.ts (sort:recent→newest-first over real utimes-stamped files / every entry has modifiedAtIso / default lists all; mutation-proven sort + modifiedAtIso) — P20 notes daily-driver (@muse/mcp 889/889, lint 0/0; full check shows only the known unrelated voice-playback /tmp flake — apps/cli alone 131/131; no LLM path → no smoke:live)
- [Reach] Resolve a contact by email / handle, not just name — `resolveContact` (the recipient resolver behind `email_send` + `find_contact`) now exact-matches a contact's `email` and `handle` (leading "@" stripped) in addition to name/aliases, so "email bob@acme.com" / "who is @bobby?" resolves the right person (uniquely, even when two contacts share a name) instead of falling to clarify/unknown; email is EXACT-only (a substring like "zeta" never spuriously matches carol@zeta.com) — `@muse/mcp` personal-contacts-store — contacts-resolve-by-email.test.ts (full-email→resolved / exact-email unique despite same-name ambiguity / handle ±@ / email-substring→unknown / name unchanged; mutation-proven email + handle clauses) — P17 recipient resolution (deterministic resolver, not the LLM path → no smoke:live; @muse/mcp 894/894, full check only the known voice-playback flake)
- [Perception] The brief triages the inbox by who's writing — `unreadBriefingLine` gained an `isKnownSender` predicate that surfaces unread from people in your contacts FIRST and flags them "★" (stable sort, newest-first within groups), wired into the briefing daemon from a contacts-email snapshot via a new pure `extractEmailAddress`; previously the brief named the first-few unread by feed order, burying a real reply under newsletters — `@muse/mcp` email-provider + situational-briefing-loop + `apps/api` briefing-tick/tick-daemons — inbox-known-sender.test.ts (extractEmailAddress / known-sender named first + ★ / no-predicate→feed order / END-TO-END via real runDueSituationalBriefing the Inbox line stars the known contact first; mutation-proven known-first sort + ★) — P20 proactive brief (no LLM path → no smoke:live; mcp 899/899, check exit 0)
- [Reach] `muse today` shows each task's urgency — the daily command listed open tasks as bare titles; it now appends a relative due tag "(overdue)/(today)/(tomorrow)/(in N days)" via a pure `relativeDueTag`, in BOTH local and remote modes (dueAt already flowed on both paths; the CLI just dropped it), so a deadline today no longer looks like a someday-idea — `apps/cli` commands-today — commands-today.test.ts (relativeDueTag overdue/today/tomorrow/in-N + later-today→today + undated/unparseable→"" / formatTasks renders the tag, undated stays bare; mutation-proven overdue boundary + isFinite guard) — CLI/Reach surface (no LLM path → no smoke:live; apps/cli 131/131, check exit 0)
- [Reach] A web-watch notice survives a transient delivery failure — `createWebWatchRunner` advanced a watch's baseline BEFORE the (un-wrapped) `sink.deliver`, so a messaging blip on the edge tick consumed the edge (notice lost forever) AND aborted the remaining watches; now it delivers first, advances the baseline ONLY after a successful send (edge re-fires next tick if delivery fails), and wraps deliver per-watch so one failure doesn't blind the others — `@muse/mcp` web-watch — web-watch-runner.test.ts (transient failure→edge not consumed, re-fires + succeeds / one watch's failure doesn't abort the others; mutation-proven by reverting to the buggy baseline-before-deliver order) + existing edge/steady/snapshot-baseline tests green — P21 web-watch daily-reliability hardening (no LLM path → no smoke:live; mcp 901/901, check exit 0)
- [Perception] Ambient notices survive a transient delivery failure (no loss, no duplicates) — `createAmbientNoticeRunner` advanced its dedupe memory (`lastMatchedIds = matchedIds`) unconditionally after an un-wrapped per-notice `deliver`, so a messaging blip aborted later notices AND re-fired the already-delivered ones next tick (duplicate ambient pings); now it wraps deliver per-notice and marks a rule deduped ONLY after a successful send (failed → re-fires next tick, not lost; already-sent sibling stays deduped → no duplicate) — `@muse/mcp` ambient-notice-loop — ambient-notice-runner.test.ts (transient failure→edge not consumed re-fires+succeeds / one rule's failure neither aborts nor duplicates the others; mutation-proven by reverting to the buggy advance-regardless order) + existing edge/steady/re-arm tests green — P20 ambient-perception reliability hardening (companion to 839; no LLM path → no smoke:live; mcp 903/903, check exit 0)
- [Knowledge] knowledge_search spans pending reminders — the multi-doc corpus spanned notes/tasks/calendar/contacts/email but NOT reminders, so a fact living only as a reminder ("renew passport", "call the dentist") was unrecallable; `assembleKnowledgeCorpus` now emits each PENDING reminder as a `reminder/<text>` chunk ("<text> (due <dueAt>)", human-labelled via labelSource, fail-open), wired into the opt-in `knowledge_search` tool from the live reminders file — `@muse/autoconfigure` knowledge-corpus — knowledge-reminders-source.test.ts (reminder→chunk with text+dueAt / throwing source→no chunks / END-TO-END knowledge_search answers "anything about the dentist?" + cites [reminder/Call the dentist]; mutation-proven chunk-emit + dueAt suffix) — P20 knowledge depth (tool name/schema unchanged → no smoke:live; autoconfigure 230/230, check exit 0)
- [Reach] `muse today` shows the weather — the daily command surfaced tasks/events/notes/reminders/followups but not the weather (the #1 morning check); it now fetches a current-weather line for MUSE_WEATHER_LOCATION (Open-Meteo, no key, fail-soft) and renders a `Weather:` line, in BOTH local and remote modes + `--json`, with no server-route change (the CLI attaches it to the briefing post-retrieval) — `apps/cli` commands-today — commands-today.test.ts (formatWeatherLine renders/empty / resolveTodayWeatherLine fetches for home location / no-location→undefined; mutation-proven location guard + render) — CLI daily-driver surface (no LLM path → no smoke:live; apps/cli 131/131, check exit 0)
- [Reach] `muse inbox` flags mail from people you know — the on-demand inbox listing showed `●` for unread but no contacts awareness (unlike the 837 brief); it now appends a trailing `★` to each message from a known contact (sender email matched against the contacts graph via extractEmailAddress, fail-soft), so terminal triage shows who matters — `apps/cli` commands-inbox — commands-inbox.test.ts (formatInboxLine ★/none across unread+read / listing flags known sender + leaves unknown bare / buildInboxKnownSender matches by email over a temp contacts file / fail-soft on unreadable file; mutation-proven ★ + email-match) + existing 4 inbox tests green — CLI inbox surface (no LLM path → no smoke:live; apps/cli 131/131, check exit 0)
- [Knowledge] knowledge_search spans scheduled followups (corpus now complete) — 841 added reminders; followups (the agent's own "check on X later") were the last personal store not searchable, so "anything about the Acme deal?" missed a tracked followup; `assembleKnowledgeCorpus` now emits each SCHEDULED followup as a `followup/<summary>` chunk (human-labelled, fail-open), wired into `knowledge_search` from the live followups file — the corpus now spans notes/tasks/calendar/contacts/email/reminders/followups (every personal store) — `@muse/autoconfigure` knowledge-corpus — knowledge-followups-source.test.ts (followup→chunk / throwing source→no chunks / END-TO-END knowledge_search answers "acme renewal?" + cites [followup/...]; mutation-proven chunk-emit) — P20 knowledge depth (tool name/schema unchanged → no smoke:live; autoconfigure 233/233, full check only the known voice flake)
- [Reach] `muse weather --days N` shows the multi-day forecast — the CLI showed only current conditions + rain heads-up; 828 gave the agent a multi-day forecast but the terminal couldn't ask "what's the week look like?". `--days <n>` (1-16) now fetches dailyForecast (Open-Meteo, no key) and prints one formatDailyForecast line per day; bare `muse weather` is unchanged — `apps/cli` weather — weather.test.ts (--days 2 prints the daily block over the REAL provider+fake fetch / non-numeric --days→error; mutation-proven --days dispatch + validation) + existing 5 current-weather tests green — CLI weather surface (brings 828 to the terminal; no LLM path → no smoke:live; apps/cli 131/131, check exit 0)
- [Knowledge] `muse read <pdf> --save-to-notes` ingests a document into searchable knowledge — `muse read` could print/answer but never persisted, so a read PDF couldn't enter the corpus (knowledge_search spans notes, but a read doc never became a note); `--save-to-notes <id>` now saves the extracted text (secret-scrubbed) as a note under MUSE_NOTES_DIR, connecting P14 document-understanding to P20 search — `apps/cli` commands-read — commands-read.test.ts (buildDocumentNoteBody titles/source/page-count/secret-scrub + saveDocumentToNotes round-trips through the REAL LocalDirNotesProvider; mutation-proven redaction + save) — P14+P20 document→searchable-knowledge (FS persistence, no LLM path → no smoke:live; apps/cli 133/133, check exit 0)
- [Reach] `muse calendar add` — create an event from the terminal: the calendar CLI was read-only (events/free/quick-ranges); `add <title> --at <when> [--for <min>] [--location]` now creates an event in the LOCAL calendar via LocalCalendarProvider.createEvent, with `--at` taking ISO-8601 or a relative phrase ("tomorrow 3pm") via resolveRelativeTimePhrase — terminal parity with the agent's muse.calendar.add — `apps/cli` commands-calendar — commands-calendar.test.ts (parseEventStart ISO/relative/invalid + add writes to a real temp calendar file readable back / --for duration / unparseable --at→error no-write; mutation-proven ISO-branch + duration default) — CLI calendar write surface (no LLM path → no smoke:live; apps/cli 133/133, check exit 0)
- [Perception] `muse home entities --state` — "what's left on?" from the terminal: 831 gave the agent a state filter on home_entities but the CLI had only `--domain`; `--state <value>` (case-insensitive, combines with `--domain`) now answers "what lights are on?" / "is anything unlocked?" from the terminal — `apps/cli` commands-home — commands-home.test.ts (--state ON→only on-light case-insensitive / no-state→all / --state unlocked→none when locked, over the REAL listHomeAssistantStates + injected /api/states fetch; mutation-proven state filter) + existing 7 home tests green — CLI smart-home surface (mirrors 831; no LLM path → no smoke:live; apps/cli 133/133, check exit 0)
- [Reach] home_action is selectable for scenes & routines — `home_action` calls any HA service so scenes (scene.turn_on) / scripts (script.turn_on) were reachable but NOT selectable (keywords were home/light/lock/device, example only light.turn_off), so "run my good night routine" / "activate the bedtime scene" never surfaced the tool; added scene/scenes/script/routine/activate keywords + a by-example description teaching scene.turn_on/script.turn_on so the local model surfaces AND fills the right service in one shot — `@muse/mcp` smart-home-tool — home-tool-relevance.test.ts (scene + routine prompts surface home_action via the REAL DefaultToolFilter; unrelated→none; mutation-proven new keywords) — P18 tool-calling reliability (exposure verified; live SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp 903/903, check exit 0)
- [Reach] Calendar availability is selectable for "am I free?" (+ loopback tools can now declare keywords) — the availability tool (827) answered "am I free / find a gap / am I busy" but was UNREACHABLE for those prompts: loopback tools could only declare a `domain` (calendar heuristics miss free/busy vocab), so DefaultToolFilter dropped it. Added `keywords` to McpRemoteTool→createMcpMuseTool→createLoopbackMcpMuseTools (per-tool relevance, an infra gap) + availability declares free/busy/available/availability/gap/slot/한가 — `@muse/mcp` index+loopback+loopback-calendar — mcp-tool-keywords.test.ts (createMcpMuseTool forwards/omits keywords) + availability-relevance.test.ts (real projection→DefaultToolFilter: free/busy/gap prompts surface availability, plain calendar→list, false-positive blast limited to availability, unrelated→none; mutation-proven keyword projection at unit + end-to-end) — P18 tool-calling reliability (exposure verified; live SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp 905/905, autoconfigure 239/239, check exit 0)
- [Reach] The tasks list tool is selectable for "what's due today?" — 832 gave `list` a dueWithinDays filter but (like availability pre-850) it was UNREACHABLE for due-queries: tasks domain keywords (task/todo/reminder) miss due/overdue, so DefaultToolFilter dropped it; using the 850 per-tool keyword infra, `list` now declares due/overdue/deadline/마감 keywords (per-tool, so a "due" false-positive exposes only list, not add/complete) — `@muse/mcp` loopback-tasks — tasks-due-relevance.test.ts (real projection→DefaultToolFilter: due/overdue/this-week surface list, plain "show my tasks" still works, false-positive blast limited, unrelated→none; mutation-proven keywords) — P18 tool-calling reliability (2nd use of 850 infra; exposure verified; live SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp 905/905, check exit 0)
- [Knowledge] `muse read` handles text files, not just PDFs — it was PDF-only (always parsePdfBuffer), so .txt/.md/.log/.csv/transcripts couldn't be read/--ask'd/ingested and a binary file dumped garbage; `extractDocumentText` now routes PDF→parsePdfBuffer, binary→clear error, else UTF-8 text (one page), so print/--ask/--save-to-notes(846) all work for text docs — `apps/cli` commands-read — commands-read.test.ts (extractDocumentText reads txt/md/log, rejects binary NUL-byte / isPdfDocument extension+magic / isLikelyBinary; mutation-proven binary guard + PDF detection) — P14 document understanding beyond PDF (no new dep; no LLM path → no smoke:live; apps/cli 133/133, check exit 0)
- [Knowledge] Contacts store a phone number — "what's mom's number?" / "save mom's number" now work — the Contact model held email/handle/aliases/birthday but NO phone, so a JARVIS couldn't capture or recall the single most common contact field; added `phone` to Contact + serializeContact, made `find_contact` return it (and keyword call/text/number), `add_contact` accept it AND count it as a reachable channel (name + email|handle|phone), and `muse contacts add --phone` / `list` render it (a phone-only contact is no longer shown "(no email/handle)") — `@muse/mcp` personal-contacts-store+contacts-tool, `apps/cli` commands-contacts — contacts-tool.test.ts (find returns phone by name, add saves phone-only) + contacts-resolve-by-email.test.ts (serializeContact emits/omits phone) + commands-contacts.test.ts (--phone persists + list shows it); mutation-proven (drop find-phone → "mom's number" fails; drop phone-as-reachable → phone-only add fails) — P13 people graph (no new dep; no LLM path → no smoke:live; mcp+cli green, pnpm check exit 0, lint 0/0)
- [Reach] `muse inbox <id>` reads one email's full body — the inbox listed summaries but there was NO way to read an email's actual content from the CLI, though `EmailReader.getMessage` (full plain-text body) was built and GmailEmailProvider implemented it (unreachable surface); the listing now shows a short `[id]` per line and `muse inbox <id>` resolves it (exact/prefix) against the recent inbox then prints From/Subject/Date + body via formatEmailMessage (`--json` → full EmailMessage). Read-only, fail-soft (unknown id / no-getMessage provider / undefined → exit 1, never throws) — `apps/cli` commands-inbox — commands-inbox.test.ts (read by short id asserts headers+body, --json full object, unknown-id exit 1, list-only-provider exit 1, listing shows [id], formatEmailMessage render/trim/fallback; mutation-proven id-resolution + getMessage-guard) — P15 email reach beyond triage (no new dep; read-only Gmail, no LLM path → no smoke:live; apps/cli 133/133 1407 tests, pnpm check exit 0, lint 0/0)
- [Knowledge] Watched RSS/Atom feeds are searchable knowledge — "any news about X?" answers from a feed Muse is watching — the corpus spanned notes/tasks/calendar/contacts/email/reminders/followups but NOT feeds, so fetched feed entries were siloed from knowledge_search/muse recall; added a `feedsSource` to assembleKnowledgeCorpus (each entry → `feed/<title>` chunk, feed-name-prefixed, fail-open) threaded through knowledge_search, a fail-open `readFeedKnowledgeEntries` that reads the CLI's already-fetched `~/.muse/feeds.json` store (newest-first, capped; XML fetch/parse stays in the CLI), `resolveFeedsFile`, and index.ts wiring — `@muse/autoconfigure` knowledge-corpus + feeds-knowledge-source + provider-paths — knowledge-feeds-source.test.ts (corpus emits feed/<title> chunk, throwing source → 0 chunks, knowledge_search cites [feed/...]) + feeds-knowledge-source.test.ts (reader flattens newest-first, honours limit, fail-open on missing/malformed); mutation-proven (drop corpus feeds block → chunk+citation tests fail; reverse reader sort → newest-first+limit tests fail) — P20 deepen Knowledge (no new dep; corpus deterministic-verified, live knowledge_search SELECTION [UNVERIFIED-LIVE] — Ollama down, same as 841/844; autoconfigure 250/250, lint 0/0, check 0 non-voice failures)
- [Knowledge] `knowledge_search` is selectable for "any news about X?" — its description matches the corpus it spans — after 855 put watched feeds in the corpus, the tool the local model sees still said "personal notes (and ingested documents)" and "do not use for live web data", so the cheap Qwen would NOT pick it for news/email/calendar queries (the #1-priority one-shot-selection defect: capability built but tool unselectable); rewrote the always-on tool's description to name the real span (notes/docs/tasks/calendar/contacts/email/reminders/followups/news-feeds) + "any news about X from their feeds", replaced the misleading "live web data" steer with the precise "do not open a NEW web page" boundary, and added feeds+email arg examples — `@muse/autoconfigure` knowledge-corpus — knowledge-search-wiring.test.ts (description contains feeds/news, drops "live web data", keeps "web page" boundary; mutation-proven by restoring the old description) — P18 tool-calling reliability / makes 855 reachable (no new dep, string-only; live SELECTION [UNVERIFIED-LIVE] — Ollama down, same as 849; autoconfigure green, pnpm check exit 0, lint 0/0)
- [Reach] `web_action` is selectable for post/rsvp/reserve/apply/register — the only agentic web actuator surfaced for submit/book/form but MISSED the equally-common state-changing-web verbs (confirmed against the real DefaultToolFilter: post-a-comment/rsvp/reserve/apply/sign-up all failed to surface it), so the local model couldn't select it for those one-shot; added non-payment keywords post/reserve/rsvp/apply/register/subscribe + Korean 예약/신청 (payment verbs buy/order/checkout deliberately excluded — out of scope per outbound-safety) — `@muse/mcp` web-action-tool — web-action-relevance.test.ts (REAL web_action→REAL DefaultToolFilter: submit/book still surface, post/rsvp/reserve/apply/register now surface, unrelated→none; mutation-proven by reverting to the old 5-keyword set) — P18/P17 web reach, tool-calling reliability (no new dep; exposure deterministically verified, live SELECTION [UNVERIFIED-LIVE] — Ollama down, same as 849/850/851; pnpm check exit 0, lint 0/0)
- [Reach] Enabling Chrome DevTools MCP is no longer silently denied by an unrelated strict allowlist — MUSE_CHROME_DEVTOOLS_ENABLED=true auto-registered the preset but the allowlist (MUSE_MCP_ALLOWED_SERVERS) was read independently; a non-empty allowlist is strict (register+connect enforce length===0||includes), so a user pinning other servers AND enabling Chrome got chrome-devtools silently denied (the "enabled but unreachable" symptom); mcp-stack now appends chrome-devtools to a NON-EMPTY allowlist when Chrome is enabled (empty/absent left allow-all — the length>0 guard prevents flipping allow-all into a 1-entry strict list) — `@muse/autoconfigure` mcp-stack — mcp-stack-chrome-devtools.test.ts (REAL assembleMcpStack→securityPolicyProvider.isServerAllowed: strict+enabled allows chrome & keeps others, empty stays allow-all, not-enabled stays denied; mutation-proven both the allow logic and the length>0 guard) — P18 turnkey Chrome reach (no new dep; no LLM path → no smoke:live; autoconfigure green, pnpm check exit 0, lint 0/0)
- [Perception] `muse glance` no longer destroys the user's clipboard — it captured the selection via a synthetic Cmd+C then read the clipboard, overwriting whatever the user had copied on EVERY run (a URL/password/snippet — real repeated data loss); OSASCRIPT_SOURCE now snapshots the clipboard text before the Cmd+C and restores it after reading selectedText (try-guarded, "missing value" sentinel ⇒ no restore; non-text clipboard is an AppleScript limit, not preserved) — `apps/cli` commands-glance — commands-glance.test.ts (contract test: save BEFORE the Cmd+C and restore AFTER, by index ordering; mutation-proven by removing the restore line) — P20 ambient perception hardening (no new dep; macOS AppleScript side-effect → structural contract test per testing.md, no LLM path → no smoke:live; apps/cli green, pnpm check exit 0, lint 0/0)
- [Reach/Safety] The web-action audit log records the submitted body (outbound-safety rule 4 "exact content") — performWebActionWithApproval (behind web_action / muse web-action / smart-home writes) logged only `web action: <summary> (<method> <url>)`, dropping the request BODY which IS the exact content of a state-changing POST, so the reviewable/undo log showed WHERE but not WHAT was submitted; the log `what` now appends ` body: <redactSecretsInText, ≤500 chars>` when present, shared across the performed/refused/failed closure (recorded on refusal too) — `@muse/mcp` web-action — web-action.test.ts (REAL performWebActionWithApproval→real action-log→readActionLog: body in log for performed AND refused, secret in body scrubbed to [redacted-telegram-bot-token]; mutation-proven by dropping the body and by logging it un-redacted) — P15/outbound-safety audit fidelity (no new dep; HTTP boundary faked, no LLM path → no smoke:live; mcp green, pnpm check exit 0, lint 0/0)
- [Reach] Recurring reminders — "remind me every Monday" / "every morning" — reminders were one-shot (dueAt single timestamp, pending→fired→done), so a recurring staple meant re-creating it each time; added ReminderRecurrence (daily|weekly) + PersistedReminder.recurrence (validated+serialized), nextReminderOccurrence (advance past fire time, skip missed slots), and re-armed fireReminder (the SINGLE fire choke point all paths use: daemon loop / muse remind run/fire / muse.reminders.fire tool / REST) to advance dueAt + stay pending for recurring vs flip-to-fired for one-shot; create surfaces: `muse remind add --repeat daily|weekly` (CLI local+REST), muse.reminders.add agent tool recurrence enum param, POST /api/reminders recurrence — `@muse/mcp` personal-reminders-store + loopback-reminders, `apps/cli` commands-remind, `apps/api` reminders-routes — reminders-recurrence.test.ts (nextReminderOccurrence daily/weekly/skip-missed/unparseable + fireReminder re-arms recurring vs fires one-shot) + commands-remind.test.ts (--repeat in POST body, invalid rejected, --local round-trips into real store); mutation-proven (fireReminder ignoring recurrence fails the re-arm test) — P-reminders new daily capability (no new dep; fixed-interval not DST-aware; agent-tool recurrence-fill SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp 920/920, api 323/323, cli 0 non-voice failures, pnpm check, lint 0/0)
- [Knowledge] `muse notes delete` — prune a stale/wrong note so it stops surfacing in search + knowledge — notes could list/read/search/save/append but NOT delete, so an outdated note kept appearing in muse notes search and the knowledge_search corpus (855) with no CLI way to remove it; added a path-traversal-guarded `delete` tool to createNotesMcpServer (the engine both CLI --local AND the API routes share), an optional NotesProvider.delete? contract + LocalDirNotesProvider impl, DELETE /api/notes route, and `muse notes delete <path>` (--local via tool, remote via REST) — `@muse/mcp` loopback-notes + notes-providers(-local), `apps/api` notes-routes, `apps/cli` commands-notes — notes-delete.test.ts (delete tool removes note gone from read+list, deleted:false for missing, rejects traversal/missing-path; LocalDirNotesProvider.delete true→false) + commands-notes.test.ts (muse notes delete --local removes the real file + confirms, second delete not-found) + mcp.test.ts tool-count 5→6; mutation-proven (removing nodeUnlink leaves the file, fails the gone test) — P14/P20 notes management (no new dep; new muse.notes.delete tool SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp 924/924, api 323/323, cli 0 non-voice failures, pnpm check, lint 0/0)
- [Reach/P18] Chrome DevTools MCP curated to a daily-driver subset so a browser prompt doesn't flood the local model — withChromeDevToolsRisk stamped all ~26 chrome tools domain "web" and passed them through, and DefaultToolFilter has no count cap, so "what's on the page?" surfaced ~26 tools at once, wrecking one-shot selection (tool-calling.md ≤5-7/turn); added CHROME_DAILY_DRIVER_TOOLS allowlist (take_snapshot/take_screenshot/navigate_page/list_pages/wait_for/click/fill_form) and withChromeDevToolsRisk now drops non-curated chrome tools (web-developer surface: performance/memory/lighthouse/console/network/emulate/resize) while risk+domain-stamping the survivors — `@muse/mcp` chrome-devtools-mcp — chrome-tool-relevance.test.ts (REAL projection→REAL DefaultToolFilter: ≤7 surfaced, perceive/navigate/act essentials present, web-dev tools dropped, click stamped write fail-close) + chrome-devtools-mcp.test.ts (evaluate_script dropped) ; mutation-proven (removing the curation filter fails the ≤7/drops test) — P18 read-first one-shot selection (no new dep; EXPOSURE change verified deterministically, no LLM path → no smoke:live; mcp + autoconfigure green, pnpm check exit 0, lint 0/0)
- [Knowledge] `muse recall` no longer surfaces a deleted note — recall searches a pre-built notes-index.json (distinct from the live knowledge_search corpus), and after 862's `muse notes delete` a removed/moved note stayed in that index with no existence check, so recall surfaced deleted content until reindex (a "deleted means deleted" privacy surprise); added filterLiveNoteIndexFiles (pure, injectable-existence) and the recall action now drops index entries whose note file is gone (existsSync; index stores absolute paths) before ranking — `apps/cli` commands-recall — commands-recall.test.ts (filter drops absent file, keeps present, drops all when notes dir gone) + program.test.ts recall-mismatch integration now writes a REAL note at the indexed path; mutation-proven (filter ignoring exists fails the drop tests) — P20 knowledge correctness (no new dep; filter deterministic, recall query-embedding Ollama-gated+unchanged → no smoke:live; apps/cli green, pnpm check exit 0, lint 0/0)
- [Reach] `muse remind list` shows which reminders repeat — 861 added recurring reminders (stored, functional, shown on add) but the list view rendered only id/dueAt/text/(fired), so a recurring reminder was indistinguishable from a one-shot exactly where the user audits them; formatReminderList now appends ` (repeats daily|weekly)` when recurrence is set — `apps/cli` commands-remind — commands-remind.test.ts (create --repeat weekly + a one-shot, list --local: recurring shows "(repeats weekly)", one-shot has no suffix; mutation-proven by dropping the suffix) — P-reminders UX seam from 861 (no new dep; pure formatter+CLI, no LLM path → no smoke:live; apps/cli green, pnpm check exit 0, lint 0/0)
- [Knowledge] Contact phone reaches the knowledge corpus (853 seam) — 853 added Contact.phone (find_contact + CLI) but the knowledge-corpus contact source built chunks from name/email/handle/aliases only (ContactLike lacked phone), so "do I have a number for X" via knowledge_search couldn't answer from the corpus; ContactLike += phone? and the contact chunk now appends `phone <number>` when present (production wiring already passes full Contact objects, no wiring change) — `@muse/autoconfigure` knowledge-corpus — knowledge-contacts-source.test.ts (REAL addContact→queryContacts→assembleKnowledgeCorpus: chunk text contains "phone +1 415 555 0101"; mutation-proven by dropping the phone push) — P13/P20 knowledge completeness (no new dep; corpus deterministic, knowledge_search SELECTION Ollama-gated → no smoke:live; autoconfigure green, pnpm check exit 0, lint 0/0)
- [Reach] `muse calendar delete <id>` cancels a local calendar event — 847 added `calendar add` and CalendarProvider.deleteEvent existed + LocalCalendarProvider implemented it, but no CLI invoked it (create-but-can't-remove) and `calendar events` didn't show ids; added `muse calendar delete <id>` (resolves exact-or-unique-short-prefix over a ±10y window → deleteEvent, reports Cancelled/not-found/ambiguous) + formatCalendarEvents now shows a short `[id]` per line so the id is copyable — `apps/cli` commands-calendar + human-formatters — commands-calendar.test.ts (real LocalCalendarProvider create→delete-by-prefix→store empty; unknown id exits 1 leaves event) + human-formatters.test.ts stays green; mutation-proven (exact-only resolver fails the prefix-delete test) — P-calendar new daily capability (no new dep; local provider, no LLM path → no smoke:live; apps/cli green, pnpm check exit 0, lint 0/0)
- [Reach] `muse calendar edit <id>` reschedules/renames a local event — 867 added add+delete and CalendarProvider.updateEvent existed but no CLI invoked it (no update = the last local-calendar CRUD hole); added `muse calendar edit <id> [--at|--for|--title|--location]` (resolves the id, --at preserves the original duration unless --for overrides — the natural "move 3pm→4pm" semantic) + extracted resolveEventIdMatch (exact/unique-prefix/ambiguous/none) shared with delete + listLocalEventsWide — `apps/cli` commands-calendar — commands-calendar.test.ts (resolveEventIdMatch exact/prefix/ambiguous/none + edit --at preserves duration / --title+--for / no-fields-errors / unknown-id-exit-1 via real LocalCalendarProvider; delete tests green on the refactor) ; mutation-proven (--at not preserving duration fails the reschedule test) — P-calendar CRUD completion (no new dep; local provider, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check, lint 0/0)
- [Knowledge] `muse recall` skips removed episodes (completes 864) — 864 filtered deleted NOTES from recall but the episode source stayed exposed: `muse episode remove` drops from the store without reindexing episodes-index.json, and recall built episode candidates straight from the index with no live-store check, so a removed episode resurfaced until reindex; added filterLiveEpisodeEntries (pure, injectable liveIds) and the recall action now builds liveIds from readEpisodes(resolveEpisodesFile) and filters episode candidates before ranking (symmetric with the notes filter) — `apps/cli` commands-recall — commands-recall.test.ts (keeps only live ids, drops when store empty, keeps when all live; mutation-proven by ignoring liveIds) — P20 knowledge correctness (no new dep; filter deterministic, recall query-embedding Ollama-gated+unchanged → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Reach] `muse contacts import <file.vcf>` bulk-loads an address book — the people graph could only grow one `contacts add` at a time, so onboarding a JARVIS (whose contacts feed resolution + knowledge_search 866 + briefing birthdays) meant a sparse graph; added a pure dependency-free parseVCards (FN/EMAIL/TEL/BDAY/NICKNAME, vCard 3.0+4.0, multi-card, line-unfolding, param-stripping, first-email/tel-wins) + normalizeVCardBirthday + `muse contacts import` (adds reachable cards [name+email|phone], skips bare labels, de-dupes by email OR phone) — `apps/cli` vcard + commands-contacts — vcard.test.ts (multi-card parse/unfold/no-FN-skip + birthday-normalize 4 forms) + commands-contacts.test.ts (import 2/skip-1 to real store, re-import imports 0); mutation-proven (removing the reachability gate imports the bare label, fails the count) — P13 people-graph bulk onboarding (no new dep; local store, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Reach] `muse time [place]` world clock — a JARVIS lacked a reliable cross-timezone clock (the model guesses + gets DST wrong); added resolveTimezone (spoken alias tokyo/'new york'/LA/utc OR raw IANA zone, undefined for unknown — no guess) + formatTimeInZone (Intl, machine-tz-independent, DST-correct) + `muse time [place]` (local when omitted, unknown→exit 1, --json) — `apps/cli` timezone — timezone.test.ts (resolveTimezone aliases/IANA/unknown-undefined + formatTimeInZone Tokyo 09:00 from UTC-midnight + London DST 01:00 Jul/00:00 Jan); mutation-proven (always-true IANA validator fails the unknown-undefined test) — new daily capability (no new dep, pure Intl, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Reach] `world_time` agent tool — conversational world clock (completes 871) — 871's `muse time` was CLI-only; a JARVIS's primary surface is chat ("what time is it in Tokyo?"), so the deterministic DST-correct timezone logic was relocated apps/cli→@muse/mcp (one source of truth, CLI re-exports) and exposed as a read-risk `world_time` MuseTool (domain system, keywords time/timezone/clock/시간, required place, error-not-guess on unknown) wired always-on in autoconfigure — `@muse/mcp` world-time + `@muse/autoconfigure` index — world-time.test.ts (resolveTimezone/formatTimeInZone DST + tool handler returns zone+time, errors on unknown/empty) + world-time-relevance.test.ts (REAL tool→REAL DefaultToolFilter surfaces for "what time in tokyo", not unrelated) + apps/cli timezone.test.ts green via re-export; mutation-proven (UTC-fallback instead of error fails the error test) — P-reach/tool-calling (no new dep; exposure verified, live SELECTION [UNVERIFIED-LIVE] — Ollama down; mcp+autoconfigure+cli green, pnpm check exit 0, lint 0/0)
- [Reach] `muse contacts export [file]` — vCard out (completes the 870 import/export pair) — getting contacts OUT of Muse (backup / migrate to another app) didn't exist; added contactsToVcf (inverse of parseVCards: name/email/phone/birthday/aliases → vCard 3.0, skips nameless) + `muse contacts export` (to file or stdout) — `apps/cli` vcard + commands-contacts — vcard.test.ts (contactsToVcf round-trips through parseVCards, skips nameless, "" for none) + commands-contacts.test.ts (import→export→re-import-to-fresh-store yields same contacts via real CLI/store); mutation-proven (dropping TEL fails the round-trip) — P13 people-graph portability/data-ownership (no new dep; local store, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Reach] `muse tasks list --search <text>` — find a task by text — list filtered only by status, so finding a task by what it's about meant eyeballing the whole list (notes had search, tasks didn't); added filterTasksBySearch (pure, case-insensitive title|notes substring on the serialized records) applied after the status filter in both --local and remote modes — `apps/cli` commands-tasks — commands-tasks.test.ts (filter matches title / notes-case-insensitive / all-on-empty / none-on-no-match + `tasks list --local --search dentist --json` returns only the match from the real store); mutation-proven (filter ignoring query fails the match+integration tests) — P-tasks triage (no new dep; local store, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Reach] `muse tasks list` shows which tasks are urgent — tasks carry an `urgent` flag (set via edit --urgent, surfaced in `muse today` 838) but the list view dropped it, so an urgent task looked identical to a normal one where the user reviews tasks (same seam class as 865); formatTaskRow now prepends `⚠ ` to an urgent task's title (HumanTaskRow gains urgent?) — `apps/cli` human-formatters — human-formatters.test.ts (formatTaskList marks "⚠ Pay rent", leaves a normal task unmarked); mutation-proven (dropping the urgent badge fails it) — P-tasks UX seam (no new dep; pure formatter, no LLM path → no smoke:live; apps/cli 0 non-voice failures, pnpm check exit 0, lint 0/0)
- [Perception] `muse calendar show <id>` reads one event's full details — `muse calendar events` listed time/title/location but DROPPED notes (and had no per-event view), so an imported/synced event's notes (agenda/dial-in/prep) were unreadable from the CLI; added `muse calendar show <id>` resolving exact-or-unique-prefix (shared resolveEventIdMatch from 868) and printing title/start→end/location/tags/notes (--json raw; unknown→exit 1) — `apps/cli` commands-calendar — commands-calendar.test.ts (real LocalCalendarProvider event with notes+location shown by prefix → title/@Room 4/notes present; unknown id exits 1); mutation-proven (dropping the notes block fails it) — P-calendar read-detail (no new dep; local provider, no LLM path → no smoke:live; calendar 26/26 + mcp/autoconfigure/api green, only the known voice flake; pnpm check, lint 0/0)
- [Perception/Hardening] `muse feeds refresh` retries a transient 429/5xx — loadFeedBody (behind feeds refresh + the feed-watch runner) was single-shot, so a feed server's transient 5xx/rate-limit dropped that feed while every sibling read actuator retries-with-backoff; added a status-retry loop (isRetriableStatus 429/5xx, doubling backoff, default 2 retries) bounded by the existing wall-clock timeout — abort/network fails fast, 4xx/exhausted-5xx still throws; exported fetchWithRetry/isRetriableStatus/RetryOptions from @muse/mcp — `apps/cli` commands-feeds + `@muse/mcp` index — commands-feeds.test.ts (503-then-200 self-heals in 2 calls, persistent 503 throws after 3, 404 fails fast in 1; existing timeout/body-cap green) ; mutation-proven (removing the retry fails both) — P19 actuator hardening (no new dep; HTTP boundary faked, no LLM path → no smoke:live; feeds + mcp 928/autoconfigure 262/api 323 green, only the known voice flake, pnpm check exit 0, lint 0/0)
- [Reach/Safety] `muse export` now bundles contacts/feeds/objectives/vetoes/personas — the backup tar.gz ("every ~/.muse store") had a stale DEFAULT_EXPORT_FILES that OMITTED the people graph (contacts.json), watched feeds, standing objectives, learned-avoidances (vetoes), and custom personas, so a backup→restore LOST all of them; added the five missing stores — `apps/cli` commands-export — commands-export.test.ts (REAL buildMuseExport on a seeded temp ~/.muse → bundle manifest contains contacts/feeds/objectives/vetoes/persona/tasks; DEFAULT_EXPORT_FILES lists them); mutation-proven (dropping contacts.json fails both) — P-data-portability/backup integrity (no new dep; local fs bundling, no LLM path → no smoke:live; apps/cli green, pnpm check exit 0, lint 0/0)
- [Reach] `muse completion bash|zsh` completes subcommands, not just the verb — the completion script (authored at 066 when the CLI was flat) deferred second-level completion, so `muse calendar <TAB>` produced nothing even though the daily surface now has deep subcommand trees (calendar/tasks/contacts/reminders/proactive/followup); added collectSubcommandMap (enumerates `cmd.commands` live → parent→sorted subs, leaf commands omitted — no staleness seam) + a `COMP_CWORD==2` `case "${COMP_WORDS[1]}"` branch (bash) and a `CURRENT==3` `case "${words[2]}"` `_describe` branch (zsh) — `apps/cli` commands-completion — commands-completion.test.ts (real program with registerCalendarCommands+registerCompletionCommand → map surfaces calendar add/delete/edit/show, leaves omitted, both scripts branch at word 2 and list subs, e2e `muse completion bash` emits `calendar) COMPREPLY=(… delete …`); mutation-proven (removing the bash second-level branch fails 3/6) — P-reach CLI ergonomics (no new dep; pure render + tree introspection, no LLM path → no smoke:live; apps/cli 1509 + full pnpm check exit 0, lint 0/0)
- [Safety] episode summariser scrubs the model-bound transcript with the canonical 14-family redactor (was a weak local 3-pattern subset) — when MUSE_EPISODIC_MEMORY_ENABLED, the end-of-session summariser sends the chat to a model and scrubs secrets first, but the local redactor only matched sk-/gh[pso]_/ya29./sk-ant-/AIza, so a pasted DB connection URI, AWS key, Slack/Telegram/Discord bot token, JWT, Stripe/GitLab key, or PEM private key reached the model VERBATIM despite the docstring's machine-boundary promise; redactSecrets now delegates to @muse/shared redactSecretsInText (single source of truth), and the canonical redactor gained the google-oauth-token (ya29.) family the old local one had — `@muse/shared` index + `@muse/agent-core` episodic-summariser — shared.test.ts (ya29.→[redacted-google-oauth-token]) + episodic-summariser.test.ts (family-tagged placeholder + connection-uri/aws/slack/jwt now redacted + summariseSession spy sees [redacted-openai-key] not the live key); mutation-proven BOTH sides (drop the ya29 pattern → shared test fails; revert the delegation to weak local → 2 agent-core tests fail) — P-privacy/data-minimisation (no new dep; pure string scrub, no LLM protocol change → no smoke:live; pnpm check exit 0 shared-core, lint 0/0)
